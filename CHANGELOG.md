# CHANGELOG

PileDesign 杭基礎検討プログラムの変更履歴。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

- バージョン番号は 1.0.0 (2026-03-18) から開始。1.0.6 / 1.0.7 / 1.0.9 / 1.0.10 は欠番（スキップ）。
- 各バージョンの全コミットは `git log <前バンプ>..<バンプ>` で参照可能（各項目の `hash` が手掛かり）。
- 本ファイルは 2026-08-11 に git 履歴から 1.0.0 まで遡って再構成した。以後はバージョン更新時に追記する。
- **本ファイルは開発者向け**。利用者向けの更新履歴は `Graphics_r1/Help/help.html` の「プログラム更新履歴概要」章にある。
  バージョンを上げたら**両方**に追記すること（`VersionVisibilityTests` が版の抜けを検出する）。

## [Unreleased]

### 修正

- **杭を選択した状態で水平解析を実行すると、スレッドの例外で落ちていました。**
  「この型の CollectionView は、Dispatcher スレッドとは異なるスレッドから
  その SourceCollection への変更をサポートしません」という例外です。
  解析モデルの組立は背景スレッドで走り、その中で杭へ FEM の参照
  （杭頭回転ばねなど）を書き込みます。杭を選択していると、その通知が
  プロパティパネルの再構築を呼び、UI にバインドされたコレクションを
  背景スレッドから触ってしまっていました。
  **杭を選択しているときだけ再現する**ので気付きにくい不具合です。
  プロパティパネルの再構築を UI スレッドへ回すようにしました。

- **水平地盤反力が計算されていないと、剛性マトリクスの話で止まっていました。**
  「剛性マトリクスにゼロ/負の対角成分が N 個検出されました」という例外だけが出て、
  何を直せばよいか分かりませんでした。地盤ばねの元になる水平地盤反力が空だと
  地盤節点の Ux/Uy に剛性が付かず、こうなります。
  解析の前に見つけて、**どの土層-杭セットで、何をすればよいか**を出すようにしました
  （「地盤の土層が入力されているか確認し、杭要素分割 (F4) をやり直してください」）。

- **Undo の複製で土層-杭セットを作り直すのをやめ、同じものを指すようにしました。**
  1.0.30-beta で「Undo すると杭要素分割が消える」を直した際、複製側に
  手書きの `SoilPile.DeepCopy` で入れ直していました。ところが土層-杭セットは
  水平地盤反力・杭周鉛直力・荷重-沈下曲線と派生値を大量に抱えており、
  手書きの複製はそれらを完全には写せません（`PileZDataItem.DeepCopy` は
  3 つのプロパティを落とします）。写し損ねると**地盤ばねが 0 の解析モデル**ができます。
  同じインスタンスを指せば写し損ねようがありません。
  Undo で戻せるのは「土層-杭セットの集合」までですが、中身の Undo は
  杭要素分割ウィンドウが自前で持っています。

- **解析のショートカットキーが、実行できないとき黙って何も起きませんでした。**
  `Window.InputBindings` は実行できない状態だと何もしません。ボタンなら灰色と説明で
  分かりますが、キーには押した感触が無く「壊れている」としか見えません
  （群杭沈下 `F7` を、土層も矩形荷重も無い状態で押したときに実際にそうなりました）。
  `F5` `F6` `Shift + F6` `F7` は、実行できないとき**理由を出す**ようにしました
  （「群杭荷重（矩形荷重）が定義されていません。荷重タブで矩形荷重を追加してください。」など）。
  実行できるときは何もせず、これまでどおり InputBindings が実行します
  （`CanExecute` を迂回しないため）。
  - `GuardBeforePressTests.BlockedAnalysisKeysExplainWhy` が、
    説明の対象から漏れた解析キーを検出します。

## [1.0.30-beta] — 2026-09-05

### 修正

- **Undo すると杭要素分割（土層-杭セット）が丸ごと消えていました。**
  `InputModel.DeepCopy` は SoilPiles を JSON 直列化から外していますが（重いため）、
  **複製側へ入れ直していませんでした**。Undo はこの複製を現在の入力に差し替えるので、
  一度でも Undo すると
  - 土層-杭セットが消える（「分割済み」のフラグだけが残る）
  - 単杭沈下の荷重-沈下曲線も一緒に消える
  - 基礎梁考慮沈下が「単杭沈下解析が未実行の杭があります」と言い出す
  - 水平解析の杭先端 P-S ばね（沈下解析の曲線を流用する設定）も効かなくなる

  という状態になっていました。複製に手書きの `SoilPile.DeepCopy` で入れ直します
  （曲線は参照で持つので、費用は杭数ぶんのフィールドコピーだけです）。
  あわせて `SoilPile.DeepCopy` を、下位のコレクションが空でも落ちないようにしました
  （Undo のたびに全 SoilPile がこの経路を通ります）。
  `UndoKeepsElementDivisionTests` で固定しました。

- **e 関数型を選ぶと、安全限界 N-M の低減後曲線が描かれませんでした。**
  低減後を低減前と同一にしていたため（低減率 β も軸力制限も課さない扱い）、
  2 本が完全に重なり、画面では低減後が消えたようにしか見えませんでした。
  耐力の算定式と低減の扱いは別の話なので、**バイリニア型と同じように低減率と軸力制限を課す**
  ようにしました（場所打ちRC杭・場所打ち鋼管コンクリート杭）。
  - **e 関数型を選んでいる場合、低減後安全限界の N-M と検定比が変わります**
    （レベル2・耐震グレードA の曲げは低減後安全限界を使うため）。
  - ヘルプの「低減・軸力制限を課さない」という記述も実装に合わせて直しました。
  - `UltimateNMReductionTests` で、どちらの算定式でも低減後が低減前より小さく、
    軸力制限で切り詰められることを固定しました（旧実装に戻すと落ちます）。

- **杭断面ウィンドウで材料のモデル化を変えても、N-M・N-Q が変わりませんでした。**
  杭体ウィンドウは `InputModel.PileBodies` の複製を編集しており、断面ウィンドウは
  その複製側の断面を受け取ります。そのため `ApplyConcreteModelOptions` のキャッシュ破棄
  （`CurrentInputModel` の断面をたどる）がこの断面に届かず、
  **前の設定で計算済みの曲線が描かれ続けて**いました。
  再描画の前にこの断面のキャッシュを捨てるようにしました。

### 追加

- **杭断面ウィンドウから、材料のモデル化を変えられるようにしました。**
  N-M・N-Q・M-φ の形はこれらの選択で変わりますが、変えるには基本設定を開き直す必要があり、
  曲線を見ながら効きを確かめられませんでした。新設した「材料」ペインで変えると、
  その場で曲線が描き直されます。
  - **その断面に効く項目だけ**を出します（効かない項目を出すと、選び直しても曲線が変わらず
    「壊れている」と読めるため）。
    - 場所打ちRC … コンクリートのモデル化（Ec の ξ・引張・圧縮・安全限界の σ-ε）、
      鉄筋 1.1F、告示1113 の許容圧縮・許容せん断、ファイバー M-φ
    - 場所打ち鋼管コンクリート … 上記（せん断を除く）＋ 鋼管 1.1F ＋ KCTB の 3 項目
    - 既製コンクリート杭 … 鋼材のヤング係数（n=5）、ファイバー M-φ
    - コンクリート充填鋼管部 … 充填コンクリートのモデル化、告示1113 の許容圧縮、ファイバー M-φ
    - 鋼管部 … 出す項目なし（`SteelPipeSection` は別系統で、これらの影響を受けません）
  - 値の置き場所は基本設定と同じで、**この画面のキャンセルでは戻りません**。
    解析に効く項目は基本設定と同じく解析結果を消してよいかの確認が出ます。
  - 杭断面ウィンドウにスモークテストがなかったので新設しました
    （XAML の `StaticResource` キー誤りと Binding パスの打ち間違いはビルドを通ります）。

- **場所打ち鉄筋コンクリート杭の杭頭 安全限界回転角 θu を基本設定で変更できるようにしました。**
  既定は 1/100 rad（従来のハードコード値と同じ）。
  杭頭固定の場合にこの杭種だけ規定があるもので、他の杭種にはありません
  （杭頭半固定は工法ごとの閾値によります）。
  従来は `EvaluationService` に定数として埋め込まれており、
  ヘルプにも判定規則だけが書かれていて典拠がありませんでした。

- **沈下の入力を触っただけで「水平解析の再解析が必要です」と出ていました。**
  群杭沈下の入力（矩形荷重・沈下用の土層・荷重面）は水平解析がまったく読まないのに、
  入力の編集を一律に「解析結果が陳腐化した」と記録していたためです。
  「水平解析 → 矩形荷重を入れる → 群杭沈下解析」という手順で**必ず**出ていました。
  編集がどの解析に効くかを記録し、沈下だけの編集では水平解析の再解析を促さないようにしました
  （矩形荷重の編集・沈下用土層の編集・**登録済み土層のコピー**が対象）。
  - 印が立ったままだと実害もありました。解析結果のスナップショットを取り直さなくなるため、
    沈下解析が入力側に書く産物（コンタの格子）が結果表示に渡らず、**コンタが出ません**でした。
  - **群杭沈下をまだ実行していないときは何も言いません。** 陳腐化するものが無いためです
    （水平解析だけ済ませて沈下の入力を用意している最中に、土層をコピーした瞬間
     「沈下解析の再実行が必要です」と出ていました）。
  - 「沈下の入力は水平解析に効かない」という前提そのものを
    `AnalysisInputScopeTests.TheHorizontalAnalysisDoesNotReadTheSettlementInput` で固定しました
    （FEM が群杭沈下の入力を読み始めたら落ちます）。
  - 同じ見落としが増えないよう、**群杭沈下の入力を触るのに効く範囲を書かずに Undo を積んでいる箇所**を
    `SettlementEditsDeclareTheirScope` が検出します（1 つずつ見つけていたので網にしました）。

- **群杭沈下のコンタが表示されないことがありました。**
  コンタの格子（X・Y 座標）を入力モデルの `SettlementGridX/Y` から読んでいたためです。
  あれは解析が**現在の入力**に書くもので、解析時のスナップショットには移りません。
  表示・計算書はスナップショットを読むので、沈下だけ再実行すると軸が空のままになり、
  描画側が「データなし」と判断して**既存のコンタを消して**いました。
  軸は沈下値そのものから決まるので、**表示中のケースの結果から引く**ようにしました。
  ケースごとに軸が変わっても点が落ちません。

- **沈下種別で「群杭」が選べなくなることがありました。**
  一覧を各解析済みフラグのセッターが自分の分だけ足し引きしていたため、
  (1) 表示内容を沈下反力などに切り替えると一覧ごと差し替えられて戻らない、
  (2) フラグが既に true のまま解析し直しても値が変わらずセッターが走らない、
  のどちらでも群杭が消えたままになりました。一覧は解析済みフラグだけで決まるので、
  継ぎ足しをやめて**毎回組み直す**ようにしました。

- **群杭沈下解析のボタンに、古い「実行できない理由」が出たままになっていました。**
  矩形荷重を追加したあとも「群杭荷重（矩形荷重）が定義されていません」と表示され続けます。
  ボタンの可否は `CommandManager.RequerySuggested`（クリックやフォーカス移動のたび）で
  問い直していたのに、**理由の文言には通知が無く**、バインドされた時点の文字列が
  残っていたためです（判定そのものは正しく、ボタンは押せる状態でした）。
  可否と理由を同じ信号で更新するようにしました。
  - 杭配置グリッドの「杭体番号」「地盤番号」のホバー説明も同じ作りだったので、
    番号を変えたら作り直されるようにしました。
  - `ToolTipNotificationTests` を追加しました。XAML の `ToolTip="{Binding X}"` の X が
    通知される作りかを全画面ぶん検査します（中身が固定の説明文だけ除外リストに入れています）。

- **解析後に材料のモデル化オプションを変えると、画面のステータスにも出るようにしました。**
  応答値は解析時のもの、限界曲線は変更後のオプションという**混ざった図・表**になるためです。
  計算書には同じ照合による注意書きがあったのに、画面には何も出ていませんでした。
  解析時の署名（`AnaModel.ConcreteOptionsSignature`）と現在の署名を比べています。

### 変更

- **解析のショートカットキーを付け替えました。**
  - 単杭沈下解析（基礎梁考慮）: `F7` → **`Shift + F6`**（単杭沈下解析 `F6` の隣に寄せました）
  - 群杭沈下解析（一般）: `F8` → **`F7`**
  - `F8` は従来どおり**群杭沈下タブへの移動**です。ショートカット一覧が
    これを「群杭沈下解析実行」と誤記していたので直しました
    （実際には解析は実行されず、タブが切り替わるだけです）。
  - コマンドパレットとヘルプの記載も揃えました。あわせて、ヘルプの
    「群杭沈下解析（一般）ボタンには灰色化を設けていない」という記述が
    現状と食い違っていた（いまは実行できないと灰色になり理由が出る）ので直しました。

- **ショートカット一覧とヘルプの記載を実態に合わせました。**
  - `F5` `F6` `Shift + F6` は「◯◯解析実行」と書いていましたが、実際は**ウィンドウを開く**だけで、
    解析はウィンドウの中で実行します。押した瞬間に走ると読めるので「〜ウィンドウを開く」に改めました
    （キーを押しただけで解析が走るのは `F7` の群杭沈下だけです）。
  - 水平解析ウィンドウの中の `F5`（解析実行）と `Ctrl + F5`（追加解析実行）が
    どちらにも載っていなかったので追加しました。
  - ダイアログ共通のキー（`Ctrl + Enter` = OK・保存、`Esc` = キャンセル、`Ctrl + Z / Y`）は
    ヘルプにしか無かったので、アプリの一覧にも載せました。

- **アイコンだけのボタンに、読み上げ用の名前 (`AutomationProperties.Name`) を付けました。**（163 箇所）
  文字を持たないボタンは、名前が無いと UI オートメーションから「名前の無いボタン」に見えます。
  名前はリボンの `Header`（短い表示名）を優先し、無いものは `ToolTip` の 1 行目を使います。
  `Content` に文字を持つボタンは WPF が本文から名前を作るので対象外です。
  - 材料が何も無いボタン（`Header` も `ToolTip` も持たないもの）が 63 箇所残っています。
    文言を決める判断が要るので、機械的には付けていません。
  - `AutomationNameTests` が、名前の材料があるのに付いていないボタンを検出します。

- **PC鋼材の安全限界引張ひずみ度 (-0.02) に出典を書きました。**
  値は以前から -0.02 でしたが、`0.02` が既定引数に直接書かれていて典拠がありませんでした。
  定数に名前を付け、「基礎部材の強度と変形性能」6.2.3 を明記し、
  `MaterialLimitStrainTests` で値を固定しました（PHC/PRC の安全限界 N-M と M-φ の終点を決める値です）。

- **単杭沈下の荷重-沈下曲線を、保存ファイルに 1 回だけ書くようにしました。**
  以前は入力の一部（`ElementDivision.SoilPiles[].LoadDisplacements`）として書き出しており、
  現在の入力と解析時のスナップショットの**両方**に同じ曲線が入っていました。
  ファイルに専用の節（`SinglePileSettlementResult`）を設け、入力の側には書き出しません。
  - **曲線の置き場所（`SoilPile.LoadDisplacements`）は変えていません。** 基礎梁考慮沈下の
    杭頭ばねと、水平解析の杭先端 P-S ばねが**次の解析の入力として**これを読むためです。
    結果側へ動かすと数値の出る経路に手が入るので、保存の仕方だけを変えました。
  - 保存と Undo が軽くなります（曲線が JSON 往復から外れます）。
  - 対応付けは（地盤番号, 杭体番号, Z）で行います。土層-杭セットを索く鍵と同じです。
  - 旧ファイルは受け取り専用のプロパティが読み、読込時に本体へ移して空にします。
    **開いて保存し直すと、曲線は新しい節にだけ入ります。**
  - `SinglePileSettlementPersistenceTests`（1 回だけ書く・読込で戻る・旧ファイルが開ける・
    順番ではなく鍵で対応付ける）を追加しました。

- **各杭の群杭沈下量も、結果から引くようにしました。**
  `PileLayoutDataItem.GroupPileSettlement` は解析が書き込む複製で、入力として保存もされて
  いました。表示中のケースの結果を読む**計算プロパティ**に変え、書き込みを全廃しました
  （解析・ケース同期・破棄の 7 箇所）。
  - 保存ファイルから杭ごとの沈下量が消えます（結果は `GroupSettlementResult` に 1 回だけ）。
  - ケースを切り替えると、同期を待たずに表示が追随します。
  - 結果を破棄するのに杭を全部なめて 0 を書く必要も無くなりました。
  - 旧ファイルは受け取り専用のプロパティが読み、移行が結果へ移して空にします
    （ケース記録と同じ形）。`NothingReadsThePileSettlementMirror` はこの受け取り口を
    移行以外が触っていないことの検査に付け替えました。

- **群杭沈下の結果を、入力モデルから独立した型 (`GroupSettlementResult`) にしました。**
  ケース記録は `InputModel.PileGroupSettlement` の中にあり、結果でありながら入力の一部
  という状態でした。そのため次の 3 つが起きていました。
  - 保存ファイルに沈下の結果が**二重に**入る（現在の入力と、解析時のスナップショットの両方）。
  - **Undo が入力ごと巻き戻すので、入力を 1 つ直して戻すと沈下の結果まで消える。**
  - スナップショットへ写す処理を別に書く必要があり、写し忘れた経路では
    結果表示に沈下が出ない（実際に 1 度起きています）。

  結果の実体は 1 つにし、現在の入力と解析時のスナップショットの
  `PileGroupSettlement.Result` が**同じインスタンス**を指すようにしました
  （`[JsonIgnore]` なので JSON 往復では複製されません）。
  保存はファイルの `GroupSettlementResult` の節が受け持ちます。
  - **旧ファイルはこれまでどおり開けます。** 入力側の `"CaseRecords"` は受け取り専用の
    プロパティが読み、`LegacySettlementMigration` が結果側へ移して受け取り口を空にします
    （System.Text.Json には「読めるが書き出さない」プロパティが作れないため、この形にしています）。
    **開いて保存し直すと、結果は新しい節にだけ入ります。**
  - 群杭沈下解析の直後に、コンタの複製（旧ファイル互換のために残しているもの）へ
    結果を書き戻していた箇所を 1 つ直しました。保存ファイルにコンタが二重に入っていました。
  - `GroupSettlementResultOwnershipTests`（保存は 1 回だけ・旧ファイルが開ける・
    Undo で消えない）と `SettlementMirrorTests.NothingWritesResultsIntoTheGridMirror` を
    追加しました。いずれも旧実装に戻すと落ちます。
  - なお、各杭の `GroupPileSettlement`（杭配置グリッドに出す沈下量）と
    単杭沈下の結果 (`SoilPile.LoadDisplacements`) は**まだ入力モデルの中**にあります。

- **場所打ち系の終局せん断耐力が、帯筋の入力値で計算されるようになりました。**
  断面を作るときに帯筋を渡していなかったため、`PileSection` が持つ Q-N 曲線
  （画面のグラフ・計算書の図）は pw = 0.002 / σwy = 295 という**仮の値**で計算されていました。
  検定は別の経路（実際の入力を渡す）を通っていたので、
  **画面のグラフと検定の限界値が食い違う**状態でした。
  - 検定の値は変わりません（もともと実際の帯筋で計算していたため）。
    **画面と計算書の Q-N 曲線が、帯筋の入力に追随するようになります。**
  - `UltimateNMReductionTests.UltimateShear_UsesTheActualHoops` で固定しました。

- **材料のモデル化オプションを変えたとき、断面が自分で古いキャッシュを捨てるようにしました。**
  従来は入力モデル (`CurrentInputModel.PileBodies`) をたどってキャッシュを破棄していましたが、
  **ダイアログは入力の複製を編集している**ため、この走査は複製側の断面に届きません
  （杭断面ウィンドウで N-M・N-Q が変わらなかった不具合はこれが原因でした）。
  断面が曲線を使うときに `ConcreteModelOptions.Version` を見て、
  自分のキャッシュが古ければ捨てます。断面がどのモデルにぶら下がっていても取り違えません。
  - オプションに版数を持たせました（`Signature()` は文字列を組み立てるので、
    曲線を引くたびに呼ぶには重いためです）。
  - `RecalculateConcreteE` は Ec が変わったらキャッシュを捨てるようにしました。
    Ec は値そのものを持つ状態なので、版数の判定では直りません。
  - `MaterialOptionCacheTests`（どの入力モデルにも属さない断面がオプションに追随すること）を
    追加しました。旧実装に戻すと落ちます。

- **画面から見えないリソースキーを参照していた箇所を直しました。**
  結果テーブルウィンドウの CSV 出力アイコンが、他の画面にしか無い `SvgIconStyle16` を
  参照していて、大きさの指定が効いていませんでした
  （`DynamicResource` は見つからなくても落ちないので気付けません）。
  - `SvgIconStyle16` / `SvgIconStyle24` を `Styles.xaml` へ 1 つだけ置きました。
    4 つの画面が同じキーで別々に持っており、無効時の淡色化があるのは 1 つだけ、
    別の 1 つは大きさしか指定していない、という食い違いがありました。
  - `FormulaControlStyle` の画面ごとの複製 5 件を削除しました（共通版と同一内容でした）。
  - `GranularityClassTextBlockStyle` は、杭要素分割ウィンドウ（土質で色分け）と
    地盤ウィンドウ（粒度区分で色分け）で**別物が同じ名前**だったので、前者を
    `SoilTypeTextBlockStyle` に改名しました。
  - `XamlResourceKeyScopeTests` を追加しました。全 47 画面の XAML について、
    参照しているキーがその画面から見えるかを検査します
    （画面ごとのスモークテストは 7 画面にしかありません）。

- **警告ベースラインを 6 種類ぶん厳しくしました。**
  `CS0162` `CS0649` `CS4014` は既に 0 件だったので許容リストから外しただけです。
  `CS8605` `CS8612` `CS8629` は 16 箇所を直して 0 件にしました。以後はエラーで止まります。
  - `WordDocumentUtils.AddImageToBody` を削除しました。DPI の既定値 (null) をそのまま
    除算に使っており、**引数を省略すると必ず落ちる**状態でしたが、呼び出し元がありませんでした。
  - 梁のツールチップは、節点が引けない梁で落ちる可能性がありました（ツールチップを出さないようにしました）。
  - 杭伏図の選択深さの描画は、深さが無いときに落ちる可能性がありました（描かないようにしました）。

- **使われていない引数・到達しないコードを整理しました。**（挙動は変わりません）
  - `ColorBar.GetColorForValue(value, min, max)` → `GetGradientColor(ratio)`。
    値と範囲を渡していましたが、中では 0〜1 の位置しか使っていませんでした。
  - `GridDimHelper.AddGridDimensions` から使っていない 2 つの座標を落としました。
  - `MainWindow.Update3DValuePolyLines`（84 行）と
    `VerticalLoadTransferMethod.ApplyDisplacementBoundaryConditions`（35 行）を削除。
    どちらも呼び出し元がありませんでした。
  - `DrawDeformedPileSections` の使っていない引数を落としました。

## [1.0.29-beta] — 2026-09-03

### 追加

- **杭体のせん断を検定するようにしました。**
  せん断耐力の Q-N 曲線はグラフと計算書に描くだけで、**検定は曲げしか行っていませんでした**。
  限界状態の割り当ては曲げと同じにしています。
  - 長期（VL 単独ケース）→ 使用限界
  - レベル1 → 損傷限界
  - レベル2 → 安全限界（耐震性能グレード S では損傷限界）

  応答値は各要素 i端・j端の |Q| = √(Fy² + Fz²)、限界値は Q-N 曲線を
  その荷重ケースの軸力で補間した値です（曲げと同じ軸力を使います）。
  検定比・支配ケースの並べ替えにもそのまま入ります。

- **沈下検討を「単杭沈下」と「単杭＋群杭沈下」から選べるようにしました。**
  基本設定に「沈下検討の対象」を追加しました（既定は**単杭＋群杭沈下**）。
  群杭沈下解析を実行していなければ群杭分は 0 なので、どちらでも単杭沈下と同じ値になります。
  - **杭頭変形角（沈下）の検定も、ここで選んだ沈下量の差**で求めます。
    従来は群杭沈下の分だけで求めていたため、杭ごとに違う単杭沈下の差が入っていませんでした。
  - 計算書の杭沈下表（単杭／群杭／合計の 3 列）に、どちらを設計値として扱っているかを注記します
    （3 列並ぶだけでは読み手が判断できないため）。

- **杭頭 2 点間の変形角を検定するようにしました。**
  すべての杭頭の組について θ = |Uz_i − Uz_j| ÷ (2 点間の水平距離) を求め、
  その**最大値**を限界値と比べます。基礎の回転・不同沈下による変形角です。
  全ペアを表に並べると読めないので、1 つの荷重条件につき最大値 1 件だけを出し、
  どの組で最大になったかを対象名（例「杭No.5 − 杭No.6」）で示します。
  - 限界値は**基本設定で変更できます**。既定は
    **使用限界 1.0×10⁻³ / 損傷限界 5.0×10⁻³ / 終局限界 7.0×10⁻³ rad**（1/1000・1/200・1/143）。
    割り当ては他の検定と同じで、長期＝使用限界、レベル1＝損傷限界、
    レベル2＝終局限界（グレードS は損傷限界）です。
    値はファイルに保存され、Undo にも載ります。
    入力欄の下の「設計用限界値の目安 (基礎指針'19 表5.3.8)」を押すと、
    指針の目安（常時荷重の即時沈下 1×10⁻³・圧密沈下 2×10⁻³、レベル1 5×10⁻³、
    レベル2 グレードS 5×10⁻³ / グレードA 7×10⁻³ rad）を表で確認できます。
    旧いファイル（値を持たない）では既定値に落とします
    （0 のまま使うと、どんな変形角でも NG になるため）。
  - **2 系統を別々の検定項目として出します。**
    「杭頭変形角」は水平解析の杭頭鉛直変位から、
    「杭頭変形角（群杭沈下）」は群杭沈下解析の沈下量から求めます（後者は使用限界のみ）。
    前者は地震時の基礎の回転、後者は常時荷重による即時沈下の不同分で、別の量です。
    杭基礎では圧密沈下は生じない（圧密沈下の検討が要るのは直接基礎を圧密層に載せる場合）ため、
    常時荷重の使用限界には表の即時沈下 1×10⁻³ rad を用います。
  - 同じ位置にある杭の組（距離 0）は角が定義できないので飛ばします。

- **長期（常時）の曲げも検定するようにしました。**
  従来は曲げが損傷限界・安全限界だけで、使用限界の N-M 曲線は描くだけでした。
  せん断と同じく VL 単独ケース（LoadCase.Level == 0）を使用限界で照査します。
  - 長期は水平荷重が無いため、土圧などが無ければ通常は問題になりません。
    **計算書では既定で出力しません。**「水平検討 → 検定結果（NGのみ）」の下に
    「長期（常時）も検定する」チェックを追加したので、必要なときだけ出せます。
  - 出力しない設定のときは、**落とした件数を注記に書きます**
    （黙って消すと「検定していない」のか「検定して OK だった」のか読めないため）。
  - 画面の検定表には、設定にかかわらず表示されます。

- **支持力の検定を計算書 (docx) に出力するようにしました。**
  画面の検定表には出ていましたが、計算書には載っていませんでした。
  水平解析の検定とは別の節「杭の鉛直支持力 検定」として出します
  （支持力は低減の有無で変わらないため、低減前/低減後の 2 枚に混ぜると
  同じ行が重複し、検定比の降順で読んだときに支配ケースを読み違えます）。
  NG だけでなく全項目を検定比の降順で載せます。
  計算書出力ウィンドウの「支持力」に、出力の有無を切り替えるチェックを追加しました。

- **PHC杭・PRC杭の Q-N 図に、せん断耐力の内訳を点線で重ねるようにしました。**
  これらのせん断耐力は「斜めひび割れ」と「縦ひび割れ」の 2 式で求めて小さい方を採りますが、
  採用値だけを見てもどちらが効いているか分かりませんでした。
  内訳 2 本を点線で描くことで、どの軸力域でどちらの破壊モードが決めているかが読めます。
  内訳は低減係数 β を掛ける前の値なので、2 本の小さい方が「低減前」の破線に一致します。
  - 画面: 図に出ている限界状態（損傷限界と、グレードA のとき安全限界）
  - 計算書: 使用・損傷・安全の 3 限界状態
  - SC杭・場所打ち系・鋼管杭は 2 式に分かれないため対象外です。
  - あわせて、PHC と PRC に同じ式が 2 組ずつ重複していたのを基底クラスに集約し、
    採用値も内訳から min を採る形にしました（図の点線と実線がずれないようにするため）。
    数値は変わりません。
  - 杭断面ウィンドウの N-Q タブにも同じ内訳を追加しました（描画経路が別だったため漏れていました）。
  - 破壊モードの呼び名を出典「基礎部材の強度と変形性能」に合わせ、
    「斜め引張破壊 / ウェブ破壊」から**「斜めひび割れ / 縦ひび割れ」**に改めました。
    旧称は `TerminologyTests` の引退名に登録しています。

### 変更

- 杭断面ウィンドウの N-Q 描画メソッドから、使われていなかった引数
  （軸力の範囲 `NMin` / `NMax`・分割数 `nDiv`）を外しました。
  軸力の掃引範囲は各杭種の断面計算が自分で決めており、渡した値は無視されていました。
  あわせて、これらを組み立てるためだけにあった「軸力しきい値が 2 個以上あるか」の分岐も外しています
  （しきい値を持たない断面では、M/Q 比を変えても N-Q 図が黙って描き直されませんでした）。
- 宣言されているだけでどこからも代入されない断面のフィールドを削除しました
  （`PrecastPileTopSection.Sigma0E` / `Rs`、`PrecastPileSection.Rs`、
  `AbstractPileSection.DeltaCurvature`、`InsituConcrete.Epsilon`）。
  今回のせん断の誤りと同じ形（宣言だけで常に 0）が残っていたものです。数値への影響はありません。
- SC杭の使用限界・損傷限界せん断が軸力に依存しない理由（せん断を鋼管が負担する扱いで、
  コンクリートの σG が入らない）をコードに明記しました。

### 修正

- **N-M 曲線から限界値を読む補間が、検定と計算書で違いました。**
  N-M 曲線は閉じた包絡線で、1 つの軸力を圧縮側と引張側で 2 度横切ります。
  検定は「横切った区間のうち最大」を採りますが、計算書の限界線だけ
  「N で並べ替えて最初に挟まる区間を採る」実装で、**圧縮側と引張側の点を結んだ値**を
  返しうる形でした。さらに範囲外の軸力を端の値へ丸めており、
  軸力制限の外にも「端の耐力がある」線を引いていました。
  実装を `PileSection.InterpolateLimitAtAxialForce` に一本化し、範囲外は値を返さない
  （線を引かない・検定の項目を作らない）ようにしました。
  - **計算書の応力ダイアグラムに重なる限界線が変わります。**
  - 挙動は `LimitInterpolationTests` で固定しました（最大の枝を採る／範囲外は NaN／垂直区間）。

- **せん断の検定が、解析した形状と関係のない M/(Q·d) = 3.0 固定で行われていました。**
  せん断耐力は M/(Q·d) に依存します（場所打ちRC の (M/Qd + 1.7)・(M/Qd + 0.12)、既製杭の α）。
  画面のグラフと杭断面ウィンドウは利用者が値を変えられますが、
  検定と計算書は既定値 3.0 に固定されていました。
  杭 1 本ぶんの最大 M と最大 Q から **M/(Q·d) = maxM/(maxQ·d)** を荷重ケースごとに求め、
  その値で限界値を引くようにしました。
  - **せん断の検定比が変わります。** 計算例では M/(Q·d) が 1.5〜1.9 に出るため、
    安全限界せん断が 1,215.7 → 1,458.1 kN（+20%）のように**大きくなります**
    （従来の 3.0 固定は安全側に外していたことになります）。
  - 画面の検定表と計算書の検定表に **軸力・M/(Q·d) の列**を追加しました。
    限界値をどの前提で求めたかが行ごとに読めます。
  - 計算書の N-Q 図は杭体区間ごとに 1 枚で、杭・荷重ケースごとの M/(Q·d) を反映できないため、
    既定値で描いている旨を図の下に注記しました。
  - キャッシュのキーに使う丸めた値で曲線も作るようにしています。
    生の値で作ると、同じキーに丸められる 2 つの値のうち先にキャッシュへ入れたほうが採用され、
    **結果が実行ごとに 0.1 kN 揺れました**（実際に発生）。

- **同じ「低減後損傷限界」でも、画面・計算書・検定で違う曲線を使っていました。**
  損傷限界は地震動レベルで低減係数が変わります（レベル1 は β2 を乗じない、レベル2 は β1×β2）。
  ところがキャッシュ済みのプロパティ `FactoredDamageNM` / `FactoredDamageNQ` はレベルを持たず、
  経路ごとに別々の選び方をしていました。
  - 画面のグラフ … 耐震性能グレードでレベルを切り替え（A→1 / S→2）
  - 計算書の N-M 図・N-Q 図・応力ダイアグラムの限界線 … **レベル2 固定**
  - 検定（せん断）… レベルを見ていなかった

  曲線の取り出しを `PileSection.GetFactoredDamageNM(level)` と
  新設した `GetQNCurvesForLevel(level)` に一本化し、全経路が同じ規則で選ぶようにしました。
  **グレードA の計算書に載る損傷限界の曲線が変わります**（レベル1 の曲線になり、β2 を乗じないぶん大きくなります）。

- **場所打ち鉄筋コンクリート杭の安全限界せん断が、帯筋の入力を無視した仮値で計算される経路がありました。**
  断面クラスが帯筋の入力を持たないため、キャッシュ済みの Q-N 曲線は
  **pw = 0.002 / σwy = 295 の仮値**で作られています。実際の帯筋（径・ピッチ・材質）を
  反映した曲線は `ComputeQNForMonQd` が作りますが、計算書と検定はキャッシュ側を読んでいました。
  上の一本化でどちらも実際の帯筋を使うようになります。
  - **安全限界せん断の値が変わります。** 計算例10 では 1,246.1 → 1,215.7 kN（−2.4%）。
  - 再発防止に `LimitCurveSourceTests` を追加しました。
    キャッシュ済みの Q-N プロパティを `PileSection` の外から読むと落ちます。

- **検定が、杭ごとの軸力ではなく「その杭体を使う最初の 1 本」の軸力で行われていました。**
  杭体番号（PileBodyNo）は断面と区間の仕様の番号で、**同じ杭体を複数の杭が使います**。
  検定は杭体番号で杭を引いていたため、最初に見つかった 1 本の軸力を全部の杭に適用していました。
  限界値は N-M・Q-N 曲線を軸力で補間して決めるので、**他の杭は別の杭の軸力で引いた限界値と
  比べられていた**ことになります。梁から杭を引くように改めました
  （水平解析側が既に持っている対応と同じものです）。
  - **曲げ・せん断の検定比が変わります。** 計算例3.5 では、軸力 671 kN 固定で
    安全限界M = 728.6 kN·m だった区間が、正しい軸力 3,094 kN で 614.1 kN·m になりました
    （16% 小さい＝従来は危険側）。
  - 再発防止に `Items_UseEachPilesOwnAxialForce` を追加しました
    （杭体を共有する杭が別々の軸力で検定されることを固定。旧実装に戻すと落ちることを確認済み）。

- **検定の「対象」列が、杭体番号を「杭配置No.」と名乗っていました。**
  同じ杭体を使う杭が複数あると、どの杭の行かまったく区別できませんでした
  （同じ表記の行が本数分だけ並びます）。杭配置番号を持たせ、
  **「杭No.12 / 杭体No.7 / 要素3 / i端」**の形に改めました。杭を特定できないときは
  「杭体No.7 / 要素3 / i端」と名乗ります。計算書の検定テキストも同じ表記です。
  杭頭回転角の行も「杭体No.4」に改めました。
  計算書の検定表の「対象」列も同じ表記にしました（要素名＋端の「beam i端」では
  どの杭のどこか読めず、行がすべて同じ表記になっていました）。

- **SC杭の低減後 Q-N 曲線で、軸力とせん断耐力が入れ替わっていました。**
  軸力制限を掛ける処理 `ApplyAxialForceLimitsToNQ` だけがタプルを (N, Q) の順と名付けており、
  渡していたのは (Q, N) でした。このためせん断耐力の列を軸力とみなして制限に掛け、
  軸力の列をせん断耐力として返していました。
  **SC杭の低減後せん断の検定比とグラフが変わります**（従来は負のせん断耐力が出るなど、
  値として成立していませんでした）。低減前は経路が違うため影響ありません。
  - 再発防止に、断面タイプ × 限界状態で「軸力に依存する/しない」を固定する
    `ShearAxialDependenceTableTests` を追加しました。断面タイプを増やすと、
    この表に載せるまでテストが落ちます。

- **群杭沈下の結果が、解析結果セット（解析時の入力ごと切り離した複製）に乗っていませんでした。**
  沈下の結果は入力モデルの中に格納されているため、水平解析のあとに入力を編集してから
  沈下を実行すると、結果表示が読むスナップショットに沈下の結果が入りませんでした。
  沈下解析の完了時に、沈下の結果（ケース記録・グリッド・各杭の沈下量）だけを
  スナップショットへ写すようにしました（記録は杭を No で参照する自己完結したデータなので、
  複製してもモデルをまたぐ参照は生まれません）。
  - 群杭沈下の結果テーブル（反復／一般）と反復ログの出所を、現在の入力から
    **解析時のスナップショット**に改めました。解析後に杭を足しても結果の行が崩れません。
  - 沈下結果の破棄は、現在の入力とスナップショットの両方から行います
    （片方だけだと「削除されます」と言いながら結果テーブルに残ります）。

- **PHC杭・PRC杭のせん断耐力に軸力が効いていませんでした。**
  斜め引張破壊のせん断応力度は σG = σe + σ0e（有効プレストレス＋平均軸応力度 N/Ae）で決まりますが、
  σ0e を保持するフィールドが宣言されているだけでどこからも代入されておらず、常に 0 でした。
  このため Q-N 曲線が軸力によらず一定（水平線）になっていました。
  曲げ側は同じ σ0e を引数で受け取っており、せん断側だけが取り残されていた形です。
  N の掃引範囲自体が「σG が 4.0〜Fc/3.5 を動く」ように決められていることからも、
  軸力依存が本来の意図であることが分かります。
  σ0e を曲げ側と同じく軸力ごとに引数で渡すよう改め、未使用のフィールドは削除しました。
  - **検定比が変わります。** PHC-700-標準 使用限界せん断（低減前）の例:
    A種 (σe=4.0) 旧 72 kN 一定 → 新 72〜105 kN、
    C種 (σe=10.0) 旧 106 kN 一定 → 新 72〜107 kN。
    圧縮軸力が載っているときは旧値が過小評価（安全側）、
    引張側では旧値が過大評価（危険側）でした。B種・C種は引張域が掃引範囲に入ります。
  - 縦ひび割れ側は τV に σG を含まないため、従来どおり軸力に依存しません。
  - SC杭は別式で、安全限界せん断は元から軸力依存でした。場所打ち系も影響を受けません。
  - 再発防止に `Precast_ShearDiagonalTension_VariesWithAxialForce` を追加しました
    （斜め引張破壊が軸力で単調増加すること、ウェブ破壊が軸力に依存しないことを固定）。

## [1.0.28-beta] — 2026-09-02

### 追加

- **場所打ち鋼管コンクリート杭を KCTB（TB工法）の設計法で検討できるようにしました。**
  基本設定に「KCTB 場所打ち鋼管コンクリート杭（BCJ評定-FD0356-08）の設計法に従う」を追加し、
  評定が定める材料モデル化と許容応力度を一括で設定します
  （個別の項目を切替えると、このチェックは自動で追随します）。
  - **終局の圧縮縁ひずみを εcu = 5,000μ とします**（既定の 3,000μ から変更）。
    鋼管によるコンクリートの拘束効果を踏まえた値で、建設省総合技術開発プロジェクト
    「新建築構造体系の開発」基礎WG 最終報告書（平成12年3月）資料4-7 において、
    実大曲げ試験との照合により 3,000μ では限界曲率を過小評価し、7,000μ では実験値を
    上回るため、5,000μ が安全側であると結論されています。
  - **許容時（使用限界・損傷限界）の判定を鋼管とコンクリートで行います**（鉄筋は判定に用いません）。
    「圧縮側コンクリートが許容圧縮応力度に達する」か「圧縮側または引張側の鋼管が
    基準強度 F に達する」の早い方です。鉄筋は耐力への寄与（断面積分）には従来どおり参入します。
  - 解析用 M-φ の作り方（折れ点の取り方）は「基礎部材の強度と変形性能」のままです。
    Technical Note Vol.1-5 の降伏時は N-M 耐力線 My の定義であって、M-φ の折れ点を
    規定するものではないためです。ただし εcu が変わることで M-φ の終点は動くため、
    解析結果には影響します。
  - あわせて、コンクリートの引張無視・圧縮側折れ点 0.85Fc・鉄筋と鋼管の 1.1F 完全バイリニア・
    許容圧縮の告示1113(第8) 区分 1 を設定します。
    「安全限界の耐力」の e関数法とは併用できないため同時に解除します。
  - 鋼管杭のコンクリート充填鋼管部は対象外です。

- **許容時 N-M の算定方式を「ファイバーモデル」と「単純累加」から選べるようにしました。**
  上のチェックが入っているときに選択できます。
  - ファイバーモデル（既定）: 断面を微小要素に分割し、平面保持のもとで応力を積分します
    （ジャパンパイル Technical Note Vol.1-5）。
  - 単純累加: 鋼管部と鉄筋コンクリート部の許容耐力を累加します。評定書は鋼管コンクリート部の
    設計を日本建築学会「鉄骨鉄筋コンクリート構造計算規準・同解説」(2014) 4章2節に準拠して
    行うと定めています。
  - 使用限界・損傷限界の NM 曲線（検定の耐力側）のみに効き、安全限界・M-φ・解析には
    影響しないため、切替えても解析結果は保持されます。

- **KCTB の適用範囲を解析前に検査するようにしました。**
  鋼管外径 φ700〜2700mm・板厚 9〜25mm とその組合せ、外径に応じた板厚の下限、
  φ2600・φ2700 は SKK490 のみ、腐食しろ 1mm、Fc 18〜45 N/mm²、鋼管長の上限を検査し、
  外れている項目をプリフライトの注意として表示します。範囲外でも計算は中断しません。

### 変更

- **KCTB のオプションで、評定書が定めていることと定めていないことを画面に明記しました。**
  - 「BCJ評定-FD0356-08 が定める設計法に従う」のチェックで**何が変わるか**を、
    ツールチップではなく画面に常時表示しました（許容応力度・本体部の設計法・適用範囲の検査の
    3 つだけで、それ以外は変わらないこと）。
  - **単純累加を選んだときに効く設定・効かない設定**を一覧にしました。単純累加は断面を
    積分しないため応力度〜ひずみ度関係を使わず、コンクリートの引張の扱い・圧縮側の折れ点・
    鉄筋/鋼管の 1.1F は許容時 N-M に効きません。効くのは許容圧縮応力度（告示1113）と
    ヤング係数 Ec（換算断面積 Ae とヤング係数比 n に入るため）だけです。
  - 単純累加のときは**「許容時の判定材料」を選べないようにしました**。断面を積分しないため
    この選択は結果に効かず、選べたままだと効かない設定を触らせることになるためです。
  - 評定書が定めていない量（ヤング係数 Ec の算定式、主筋・鋼管の許容応力度）が残ることも
    明記しました。

- **「評定範囲外」という表現を「評定書に規定が無い」に改めました。**
  評定書は終局の圧縮縁ひずみについて 3,000μ とも 5,000μ とも書いていません。
  既定の 3,000μ も評定書の規定ではない（「基礎部材の強度と変形性能」による値）ので、
  5,000μ だけを「範囲外」と書くのは非対称で、評定に反するかのような誤解を招いていました。
  「評定に従う／反する」項目ではなく、どの文献に依るかを設計者が選ぶ項目である、
  という書き方に統一しています。

- **プロジェクト情報を基本設定から別ウィンドウに分けました。**
  プロジェクト番号・名称・標高記号・Z=0 の標高は、最初に一度入れたら以後ほとんど
  触らないのに、基本設定の先頭を占めていて材料オプションへ到達する邪魔になっていました。
  リボンの「解析条件/解析」タブに「プロジェクト 情報」ボタンを追加しています。
  保存先はこれまでどおりで、ファイルの互換性に影響はありません。

- **計算書の「材料モデル化の選択」に、既製杭のヤング係数の出所を追加しました。**
  EI・EA と N-M 曲線に効く選択なのに、計算書に記録されていませんでした。

- **基本設定の項目を「解析に効くもの」と「検定の耐力側だけに効くもの」で並べ直しました。**
  これまで材料別・限界状態別・用途別の 3 つの分類軸が混ざっており、
  「これを変えると解析結果が消えるのか」がグループごとの ※ 注記を読まないと分かりませんでした。
  操作上いちばん大事なこの区別を帯見出しに上げ、注記をそこへ集約しています。
  - 解析に影響する項目: 場所打ちコンクリート杭のコンクリートのモデル化 ／
    鋼材のモデル化（PC鋼材・鉄筋・鋼管）／ 解析用 M-φ 関係
  - 検定の耐力側だけに効く項目: 場所打ちコンクリート杭のコンクリートの使用限界・損傷限界強度
    （長期・短期許容応力度）／ 場所打ちコンクリート杭の安全限界状態のひずみ度〜応力度関係
  - 工法固有の設計法: KCTB 場所打ち鋼管コンクリート杭（TB 工法）
  - 1 項目しかなかった「鋼材のヤング係数」グループは「鋼材のモデル化」に統合しました。
  - 「許容耐力・終局強度の準拠」は、許容応力度（使用・損傷限界）と
    安全限界の応力度〜ひずみ度関係が同居していたため 2 つに分けました。

- **各オプションの説明を、具体値で書くようにしました。**
  - 「PC鋼材・鉄筋・鋼管のヤング係数の出所」→
    **「既製コンクリート杭の PC鋼材・鉄筋・鋼管のヤング係数」**。
    このオプションが効くのは既製杭だけで（場所打ち系はカタログ・指針とも E = 205,000 のため
    変わりません）、カタログ値と指針の値を杭種ごとに並記しました。
    PHC・PRC は n = 5 固定（Ep = Er = 5·Ec）、SC は Es = 205,000 です。
  - 「0 ≤ ε ≤ 0.003」のように **εcu を値で直書きしていた箇所を全廃**しました。
    KCTB では 5,000μ になるため、式は `0 ≤ ε ≤ εcu` とし、
    εcu の値は注記と計算書本文で示します。

- **KCTB の「許容時 N-M の算定」の選択肢名を改めました。**
  「ファイバーモデルで算定する」→「**断面を分割して積分する**」。
  すぐ上の「解析用 M-φ 関係」にも同名の選択肢があって別物を指していたことに加え、
  既定（基礎部材の強度と変形性能）の許容時 N-M も同じ断面積分なので、
  この名前では KCTB との違い（限界状態の判定条件）を表せていませんでした。
  計算書の選択欄も「ファイバーモデル」→「断面分割積分」に変えています。

### 修正

- **SKK490 の基準強度が 315 N/mm² になっていました（結果が変わります）。**
  315 は JIS A 5525 の**規格降伏点**で、**基準強度**は 325 N/mm²（平成12年建設省告示第2464号）です。
  基準強度は許容応力度（長期 F/1.5・短期 F）と材料強度（1.1F）の基準になる量なので、
  規格降伏点を入れていたぶん鋼管の耐力を約 3% 過小に評価していました。
  ジャパンパイル Technical Note Vol.1-5 (2022年11月) 表1 も SKK490 の基準強度を 325 としています。
  SKK400 は規格降伏点・基準強度とも 235 で変更ありません。
  - **SKK490 を使う鋼管杭・場所打ち鋼管コンクリート杭・SC杭の耐力と解析結果が変わります。**
    計算例3.5（鋼管杭基礎）では安全限界曲げが 709.8 → 728.6 kN·m（+2.6%）、
    応答値は変位・ばね反力とも 0.2% 以内の変化にとどまります。
  - 検定の合否（OK/NG）が入れ替わった項目はありませんでした。
  - `CapacityCurveRegressionTests.SteelPipeGrade_F_IsSpecifiedStrengthNotYieldPoint` で値を固定しました。

- **場所打ち鋼管コンクリート杭の許容せん断力が、正しい値のおよそ半分でした（結果が変わります）。**
  鋼管の断面積を `π/4·(d² − (d − ts)²)` で求めており、内径から板厚を片側しか引いていませんでした。
  外径 d・板厚 ts の管の内径は `d − 2ts` なので、これは板厚 ts/2 の管の断面積にあたります。
  - 使用限界せん断 Qs・損傷限界せん断 Qd が **約 1.99 倍**になります
    （φ1000×t12・SKK400・腐食1mm で Qs 774 → 1,539 kN、Qd 1,161 → 2,309 kN）。
    これまでは安全側に外していたことになります。
  - 同じ誤りが安全限界せん断の軸力分担にもありました。コンクリートと鋼管の全塑性軸力の比で
    軸力を分けるところで鋼管側が過小だったため、軸力の大きい領域で安全限界せん断が
    数 % 緩和側へ動きます（返り値の式そのものは元から正しい断面積を使っており不変）。
  - 同じファイルの安全限界せん断の本体は当初から `π·ts·(d − ts)` で正しく、
    使用限界・損傷限界だけが食い違っていました。
    KCTB 評定書の (16) 式 `sQ = (sA/2)·sfs` でも sA は鋼管の全断面積です。
  - 断面積の式を `PipeShearArea` に括り出し、閉形式との突合を
    `CapacityCurveRegressionTests.Sprc_ServiceShear_UsesFullPipeArea` で固定しました。
  - せん断は M-φ に入らず、検定項目にもないため、**解析結果と検定結果は変わりません**。
    変わるのは Q-N 曲線と、計算書のせん断図に重なる限界線です。

- 基本設定ウィンドウで、モデルの値が外から書き戻されたときに
  「解析用 M-φ 関係」のラジオボタンだけが追随していませんでした
  （`FundamentalInput_PropertyChanged` に `UseFiberMPhi` の分岐が無かった）。

## [1.0.27-beta] — 2026-08-31

### 変更

- **計算書の書体を、役割ごとに分けました。**
  これまで本文・見出し・表・注記がすべて ＭＳ ゴシック 10.5pt で、
  どこが何なのか書体からは読み取れませんでした。
  - 本文: 游明朝 10.5pt（数行以上の説明文は明朝が読みやすいため）
  - 見出し: 游ゴシック Medium（本文と書体が変わり、階層が一目で分かります）
  - 表の中身: ＭＳ ゴシック（**等幅**。半角数字が固定ピッチなので桁が揃います）
  - 表下の注記「※ …」: 游ゴシック 9pt（本文の続きと読み違えないように）
  - 検定結果テキスト・解析ログ: 等幅のまま（桁を揃えて読ませる固定行のため）
  - ※ 游書体は Windows 8.1 / Office 2016 以降に入っています。
    それより古い環境では別の書体で表示されます。

- **計算書の「沈下解析杭モデル」「水平抵抗解析杭モデル」の図を作り直しました。**
  図の大きさを 150×100mm から 150×190mm に広げ、横方向を
  「左＝土層の諸元 / 中＝杭とばね / 右＝N値柱状図」に並べ直しています。
  これまでは杭の絵が図の中央に数 mm 幅で描かれるだけで、
  左半分がまるごと空白でした。
  - 左に土層ごとの帯を新設し、層番号・土層名・層厚・下端深度・単位体積重量・
    N値・粘着力 Cu を並べました。層が薄くて入りきらない場合は行を減らし、
    それでも入らない場合は引き出し線で図の外に出します。
  - 地表面線・地下水位線・深度目盛・凡例を追加しました。
    層の境界線がどの深さなのか、これまで図からは読めませんでした。
  - N値柱状図の目盛は、最大 N 値に合わせて自動で伸びます。
    これまで 60 で固定しており、それを超える支持層は図の外にはみ出していました。
  - 図の前後で改ページします（図・図番号・注記だけで 1 ページになります）。
- **主要な表に表番号と表題を付けました。** 表目次に載るのが 9 件だけで、
  地盤・荷重・杭体・杭配置・根入部といった入力データの表には番号がなく、
  本文から「表 3.2 参照」と参照できませんでした。
- **検定結果を、罫線を並べたテキストから Word の表に置き換えました。**
  画面用の見た目がそのまま紙面に出ていました。
  併せて、検定比の大きい順に並べ、最大検定比と支配ケースを冒頭に示します。
- **杭のモデル図を、杭ごとに出力するようにしました。**
  支持力の表は全杭を出すのに図は先頭の 1 本だけで、
  杭体や地盤が複数あるモデルで表と図が食い違っていました。
  杭が 2 本以上あるときは、図番号に地盤と杭体の符号を添えます。
- **「液状化の検討」という同じ名前の見出しが 2 つあった**のを解消しました。
  地盤情報の下にあるデータ側を「液状化の判定結果」に改めています。
- **解析の実行後に条件を変えて計算書を出した場合に、注意書きを出すようにしました。**
  基礎のねじれ拘束と杭頭の接続仮定は、切り替えても解析結果が破棄されないため、
  解析に使った条件と計算書の記載がずれることがありました。
  材料モデル化オプションには同じ注意書きが既にあります。
- **土層表に「N値」と「粘着力 Cu [kN/m²]」の列を追加しました。**
  粘着力は粘性土以外では 0 なので、その場合は「-」と書きます。
- **「計算条件・仮定」章の文面を、計算書の文体に改めました。**
  「既定は」「代替を選択できる」「基本設定画面」といった操作の説明をやめ、
  この計算に用いた前提だけを述べるようにしています。
  参照している文献名も、本文の他の箇所と同じ表記に揃えました。

### 修正

- **計算書のモデル図の文字が、印刷すると読めないほど小さかった**のを修正。
  この図は倍密度（1134×756 ピクセル）で描いて 150mm 幅に貼っており、
  線の太さや節点の大きさはその倍率に追従していたのに、
  **文字の大きさだけが追従していません**でした。
  10 ピクセルで描いた文字が、紙面では実効 3.7pt になっていました。
  図中の書体を游ゴシック 8pt に揃え、倍率に追従させています。
- **N値柱状図の「N=0 N=10 …」の見出しが重なって読めなかった**のを修正。
  目盛の間隔（約 28 ピクセル）よりラベルの幅（約 26 ピクセル）が大きいのに、
  7 本すべてにラベルを付けていました。重なるものは間引きます。
- **土質点ごとの N 値が全点に印字されて縦に密集していた**のを修正。
  層の代表 N 値は左の土層帯に移し、柱状図では重なる分を間引きます。
- **土層の背景色が、杭頭と地表面が一致しない場合にずれていた**のを修正。
  1 層目の上端を地表面標高ではなく杭頭標高としていたためです。
- **地盤変位の大きさが図から読めなかった**のを修正。
  最大変位を常に一定の幅に正規化して描くだけで、実寸を示していませんでした。
  凡例に δmax を出します。
- **計算書の図の生成が、まれに応答しなくなる**可能性があったのを修正。
  終了済みスレッドの描画キューに処理を投げていました。
- **杭体の曲げ検定で、安全限界の耐力がまれに小さく評価されることがありました。**
  断面の N-M 曲線は初回アクセス時に計算して覚える作りですが、検定は杭要素ごとに
  並列で回るため、同じ断面の曲線が同時に計算されることがありました。
  曲線の算出は途中でその断面の軸力の適用範囲を書き換えてから、
  その値で曲線を切り取ります。同時に走ると互いの途中の値を掴んでしまい、
  切り取りが狂った曲線ができていました。
  実測では、安全限界 M が 710.3 kN·m と出るべきところ 664.8 kN·m になる例が
  19,200 回中 17 回発生しました。**解析結果そのものは正しく、耐力側だけがずれる**ため
  気づきにくい不具合でした。断面ごとに計算を直列化して修正しています。
  （副次的に、同じ曲線を何度も計算しなくなるため検定は速くなります）
- **表の 1 行がページ境界で上下に割れていた**のを修正。
  Word の既定では行の途中でも改ページできるため、
  ページ末尾に来た行が割れて、前のページに空のセルだけが残っていました。
  すべての表の行に「行の途中で改ページしない」を設定しています。
  （1 行だけで 1 ページに収まらない場合は、これまでどおり分割されます）
- **群杭の沈下を検討していないのに、計算書に群杭の算定方針と式が載っていた**のを修正。
  「杭の沈下検討」の節が、群杭沈下解析の有無を見ずに単杭・群杭の両方を書いていました。
  群杭を出力しないときは、方針文から群杭の記述を外し、
  スタインブレナーの式（等価荷重面法）も載せません。

## [1.0.26-beta] — 2026-08-28

### 変更

- **群杭沈下のコンタが、保存ファイルに二重に入らなくなりました。**
  結果はケースごとの記録が持ちます。入力側にあった同じ内容の控えには、
  もう書き込みません（ファイルが小さくなります）。
  - **旧いバージョンで開くと、沈下コンタが表示されません**（杭ごとの沈下量は残ります）。
    このバージョンで開き直せば元どおり表示されます。
  - これまでに保存したファイルは、これまでどおり開けます。

### 修正

- **「基礎のねじれを拘束」と剛床連結を併用すると、解析が例外で落ちていた**のを修正。
  代表節点の Rz を拘束すると、その節点を master に持つ接合節点の荷重を
  行列へ積む先が無くなる。荷重ベクトルの組立だけこの判定が抜けており、
  負の添字でベクトルを引いて `ArgumentOutOfRangeException` になっていた。
  拘束された自由度に対応する式は無く、荷重は支点反力になるので捨てる
  （剛性行列の組立は元からこの判定を持っていた）。
  - 踏むのは「ねじれ拘束 ON」「剛床連結」「杭軸力を接合節点へ外力として与える
    （入力値＋応力解析結果 / P-S 非線形ばね）」が揃ったときだけ。
    剛体連結や杭軸力が「入力値」のままでは起きません。
- **解析結果を含むファイルを開くと、編集していなくても「解析のあとに入力が編集されています」
  と言われた**のを修正。編集の有無を「解析時の入力の控えが、現在の入力と別物か」で
  判定していましたが、控えは解析のたびに複製して作るため常に別物になります。
  編集したかどうかをファイルに記録するようにしました。
  - 読み込みの仕上げで Undo の初期状態を積む際に、そこでも編集扱いになっていました。
    記録した値へ戻すようにしています。
  - これまでに保存したファイルには記録が無いため、開くと従来どおり確認が出ます。
    一度このバージョンで保存し直すと出なくなります。
- **計算書の沈下コンタ図が縞状になっていた**のを修正。
  1 区画を 4×4 の単色の小片で塗っていたため、格子の粗い方向に段差が出ていました。
  画素ごとに補間して描くようにし、連続した分布になります。
- **計算書の沈下コンタ図で、カラーバーの文字が小さすぎて読めなかった**のを修正。
  図は 2 倍の解像度で描くのに、余白・カラーバー・文字だけ 1 倍の寸法で書かれており、
  紙のサイズに縮めるとそこだけ半分になっていました。
  カラーバーが右上に小さく寄っていたのも同じ原因です。
- **計算書の沈下コンタ図が、画面と違うケースの図になることがあった**のを修正。
  画面は表示中のケースから描くのに、計算書だけが入力モデル内の複製を読んでいました。
  ケースを切り替えても計算書は切り替わりません。
- **N-M 図・Q-N 図の 1 点が、別々の要素の M（Q）と軸力からできていた**のを修正
  （画面・計算書とも）。区間に複数の要素があるとき、M は区間内の最大値を取るのに、
  軸力は最大と無関係に「最後に見た要素の値」で上書きしていました。
  **最大を出した要素の軸力**と組にします。
  区間が 1 要素のモデル、および「解析軸力を使う」が OFF のモデルでは図は変わりません。
- **旧ファイルを開くと、杭ごとの群杭沈下量が 0 になっていた**のを修正。
  読込時に旧形式から結果を建て直す処理で、コンタは移していたのに
  杭ごとの沈下量を移していませんでした。
- **杭要素分割ウィンドウ（F4）の保存で、何も知らせずに閉じることがあった**のを修正。
  確認を「消えるものがあるとき」だけに出していたため、**水平解析だけを行ったモデルでは
  無言で閉じて**いた。水平解析の結果は消えない（解析時の入力ごと切り離してある）が、
  分割が変われば再解析が必要になる。**消えなくても影響があるなら知らせる**ようにした。
  - 分割の中身が実際に変わったかは見ていない。見落として黙るより、
    保存を押したときに毎回知らせるほうが安全側と判断した。
- **回転角（θX / θY / θZ / θH）が全節点でゼロだと、画面が白紙になっていた**のを修正。
  ゼロの回転には楕円が描けないため何も描かずに抜けており、
  「すべてゼロ」なのか「描けていない」のか画面から区別できなかった。
  「θZ はすべて 0 です」と表示する。
  基礎のねじれを拘束すると θZ は全節点で厳密にゼロになるので、これは正常な結果。

## [1.0.25-beta] — 2026-08-28

### 追加

- **「基礎のねじれを拘束」した解析を選べるようにした**（水平解析ウィンドウ「■ 設計条件」）。
  慣性力作用点（基礎の代表節点）の鉛直軸まわりの回転を拘束します。
  基礎がねじれなくなるため、**杭頭の水平変位が全杭で揃います**。
  - 既定は OFF（従来どおり）。ON/OFF で結果が変わるため、切り替えたら再解析が必要です
    （追加実行では引き継げません）。
  - 杭配置が 1 列の場合は元から拘束されるため、ON/OFF で結果は変わりません。
  - ON のときは計算書の仮定一覧にも記載されます。

### 修正

- **杭を一部だけ表示すると、水平解析の結果が出ないことがあった**のを修正。
  結果は「表示中の杭に属する要素」で絞り込むが、その杭と解析結果の対応付けが
  復元されていないと絞り込みが空になり、**表示中の杭のぶんまで含めて丸ごと消えて**いた。
  - 対応付けが取れないときは絞り込みをやめて全体を描き、ログに残すようにした。
    杭をすべて非表示にしたときは従来どおり消える。
  - 杭Zばね（P-S 非線形ばね）の対応付けが、解析直後の複製にもファイル保存にも
    入っていなかったのを追加した。
  - 結果の描画が「編集中の入力」と「解析時の入力」を混ぜて見ていたのを、
    解析時の入力に統一した。
- **NMINT / QNINT グラフで「杭体」を選び直しても描き変わらなかった**のを修正。
  杭体を選び直すたびに杭区間が「すべて」へ戻されており、
  NMINT / QNINT は「すべて」だと描く対象が決まらず軸だけ描いて終わっていた。
  1 回目で空になり、2 回目以降は既に「すべて」なので何度変えても空のまま
  ＝「再描画されない」ように見えていた。
  これらのグラフでは区間を保つようにした（同じ番号があれば維持、無ければ先頭）。
  「すべて」を扱えるグラフ（p-y・M-φ・EI-φ）は従来どおり。
  - あわせて、**NMINT / QNINT の「杭区間番号」から「すべて」を外した**。
    これらは 1 区間ぶんの断面から限界曲線を作るため「すべて」では描くものが決まらず、
    選ぶと軸だけの空グラフになっていた。選べない形にした。
- **杭要素分割ウィンドウ（F4）を開くだけで「解析結果が削除されます」と聞かれていた**のを修正。
  このウィンドウは保存せずに閉じれば分割前の状態へ戻す作りなので、
  見るだけなら何も失われない。中身を確認したいだけでも判断を迫られていた。
  確認は**保存ボタンを押したとき**に出すようにした（断れば閉じずに編集を続けられます）。
  保存せずに閉じた場合は、編集済みの印も Undo の記録も残りません。
- **杭要素分割ウィンドウ（F4）が、開くたびに分割を作り直していた**のを修正。
  分割済みで開いても最小分割で上書きされるため、**解析に使った各要素の水平ばねなどを
  確認できません**でした（全ランプ消灯・保存ボタンも灰色）。
  分割済みのときは作り直さず、保存されている分割をそのまま開くようにした。
  ジオメトリを変える編集では分割が無効になるため、開いた時点で分割済みなら
  その分割は今の形状に対して有効です。
  - 保存済みの分割を開いた場合は、全セットを確認済み（ランプ点灯）として扱います。
  - 土圧合力ばねだけ入力モデルと同じ実体を編集しており、キャンセルで閉じても
    戻らなかったのを修正。他の項目と同じく複製を編集します。
  - **「変更可否」が読み込み時に全行 false へ潰されていた**のを修正。
    自動分割で足した節点まで「動かせない」扱いになり、削除も Z の編集もできませんでした。
- **計算書の N-M 図・Q-N 図に、見出しと違う杭体の結果が載っていた**のを修正。
  図の見出しは「杭体符号 | 杭区間番号」で 1 つの断面を指しているのに、
  地震時の点だけ杭体で絞っておらず、**区間番号さえ一致すれば別の杭体の杭の点も
  同じ図に乗って**いました（区間番号 1 はどの杭体にもあるため、杭体が複数あるモデルでは
  必ず混ざります）。常時（VL）の点は元から絞られており、画面のグラフも正しく絞っています。
  杭体が 1 種類のモデルでは図は変わりません。
- **計算書の N-M 図が、基本設定の「解析軸力を使う」を見ていなかった**のを修正。
  画面の NMINT は解析で出た軸力を足して描くのに、計算書は入力軸力のままだったため、
  このオプションが ON のモデルでは**同じ図の点が画面と計算書で横にずれて**いました。
  画面と同じ式に揃えました。OFF のモデルでは図は変わりません。
- **Q-N 図（画面・計算書とも）の軸力を N-M 図と同じ扱いにした**。
  Q-N 図だけ「解析軸力を使う」を反映しておらず、同じ杭・同じケースなのに
  図によって軸力が違い、2 つの図を並べて読めませんでした。
  このオプションが OFF のモデルでは図は変わりません。

- **解析結果を含むファイルを開き直すと、M-θ / M-φ 曲線が空になっていた**のを修正。
  曲線の点が保存の対象にならない形で持たれており、保存すると中身が消え、
  読み戻すと 1 点も復元されなかった。例外も警告も出ないので気付けなかった。
  これまでに保存したファイルの曲線は取り戻せないが、開くことはできる（再解析で復元されます）。
- **杭頭バネのウィンドウだけ、ヘルプを開くとその画面を操作できなくなっていた**のを修正。
  他の画面と同じく、ヘルプを読みながら元の画面を触れるようになります。
- **ショートカット一覧に載っていないキー割当があった**のを修正。
  計算書（Word）の出力 `Ctrl + Shift + W` とコマンドパレット `Ctrl + Shift + P` を追加した。
- **リボンのボタンが灰色のとき、押せない理由が読めなかった**のを修正。
  ToolTip は既定では無効なコントロールに出ない。理由を書いてあっても、
  まさに読みたいときに出ない状態だった。
  無効になりうるリボンボタン 37 個のうち **35 個**で指定が抜けていた。
- **画面ごとに書かれた重複スタイルの整理を進めた**（重複 25 → 7、未使用 21 → 7）。
  誰にも届かない定義 48 件を削除し、中身が同一のものは共有辞書に 1 つずつ置いた。
  見た目は変えていない。
  地盤ウィンドウのラベルは、共有辞書の同名スタイルを隠していた（あちらを直しても効かない状態）ため、
  別名に分けた。見た目は変わらない。
  残る重複 6 件は中身が食い違うため画面ごとの判断が要る。
  残る未使用 7 件は共有辞書側にあり、色やサイズの直書きを寄せるために<b>用意してある</b>もの。
- **同じスタイルが画面ごとに書かれ、中身が食い違っていた**のを整理した。
  表の見出し色 `YellowHeaderStyle` は 6 箇所にあり中身が 4 通りに分かれていた
  （うち 5 箇所は誰にも使われていなかった）。右寄せ系も同様。
  共有するものは `Styles.xaml` に 1 つずつ置き、各画面の定義を削除した。
  見た目は変えていない（名前が似ている右寄せ 2 系統は<b>別物</b>なので統合していない）。
  - 未解消の重複 25 件・未使用 21 件は一覧にして、**増えたら止まる**ようにした。
    直したら一覧から外す必要があり、外し忘れも検出する。
- **細工した入力ファイルで意図しない型が作られる余地があった**のを塞いだ。
  読込のフォールバック経路が JSON の `$type` に書かれた任意の型を復元しようとしていた。
  復元してよい型をこのアプリ自身の型に限る。
  保存は `$type` を書かないため、正規のファイルへの影響は無い。
- **ファイルを読み込んだあと、杭の軸力や座標を編集しても ΣVL・転倒モーメントが
  更新されないことがあった**のを修正。
  入力モデルごと差し替わると杭ごとの変更通知が張られないままになっていた。
  併せて、差し替え前のモデルの購読が外れておらず、捨てたはずのモデルの編集でも
  集計が走り続けていたのも直した。
- **式が誤ったまま残っていた死にコードを削除した**。
  旧 Steinbrenner 実装（現行の実装とは別物で、どこからも呼ばれていなかった）と、
  未使用の剛性クラス。検索でひっかかって生きているコードと取り違える恐れがあった。
  併せて空の置き去りファイル 2 件、控えのファイル 1 件、
  実在しないファイルを指す `Compile Remove` 3 行、
  常に不発だったグラフウィンドウの Ctrl+Z ハンドラも削除した。
- **保存が途中で落ちると、書きかけのファイルが残っていた**のを修正。
  一時ファイルに書き切ってから差し替えるようにした。
  保存先は「前の内容」か「新しい内容」のどちらかになる。
  自動保存では、書きかけのファイルが復元候補として拾われる恐れがあった。
- **ダミー梁の結果が 1 件でも入ると、保存ファイルが開けなくなる作りだった**のを修正。
  復元されないプロパティが保存グラフに入っており、参照の解決に失敗する。
  現状は空のまま運用されているため表面化していなかった。
- **FEM の材料・断面キャッシュの鍵が足りず、別物を取り違える余地があった**のを修正。
  - 材料をヤング係数だけで引いていた。杭は ν=0.2 固定、基礎梁は利用者が入力した値を使うため、
    同じヤング係数で ν が違う組合せ（鋼の基礎梁など）では先に作られた方が使い回され、
    せん断・ねじり剛性が黙って別の値になっていた。
  - 断面の鍵にせん断断面積が無かった。杭は「せん断断面積 = 断面積」、基礎梁は (5/6)bh と
    作り方が違うのに同じキャッシュを共有している。
  - 公表された計算例（いずれも ν=0.2）では結果は変わらない。予防的な修正。

## [1.0.24-beta] — 2026-08-28

### 追加

- **杭の鉛直支持力を検定に加えた**（押込み・引抜き）。
  解析結果テーブルに「検定結果（杭の鉛直支持力）」が出る。
  応答値は入力した杭軸力、限界値は `SoilPile.CalculateResistances` の結果で、
  荷重との対応は限界状態で取る（長期=使用限界 `Ru/3`、レベル1=損傷限界 `Ru/1.5`、
  レベル2=終局限界 `Ru`。耐震グレード S ではレベル2 も損傷限界）。
  引抜きは `Rt_SLS` / `Rt_DLS`(=Rty) / `Rt_ULS`(=Rtr)。
  - 水平解析の検定（低減前／低減後）とは**別の表**にした。支持力は低減の有無で変わらないため、
    2 枚に載せると同じ行が重複して支配ケースを読み違える。同じ理由で**水平解析なしでも**
    （杭要素分割まで済んでいれば）表示する。
  - 軸力の向きに応じて押込みか引抜きの**どちらか一方**だけを出す。
    限界値が 0 の杭（引抜き抵抗を求めていない）は行を出さない。
  - 地盤は**杭体 No** で引く。`PileLayoutDataItem.SoilPile` は
    (地盤No, 杭体No, 杭頭Z) を鍵とするキャッシュのため Z が合わず null になり、
    そのまま使うと黙って検定が 0 件になる（`PileBearingEvaluatorTests` で固定）。
  - 計算書（docx）の検定テキストは**変わらない**（golden 10 件で確認）。

### 変更

- **変位制御法の割線剛性に接線剛性が混ざっていた**のを修正（変位制御法を選んだときだけ結果が変わります）。
  周面ばねだけ接線剛性を足しており、先端のみ割線だった。
  内力の評価が「割線剛性 × 変位」なので、剛性側に接線が混ざると
  $K_{sec}\,x = R(x)$ が崩れ、**収束しても釣り合っていない解**に落ち着く。
  既定の荷重制御法は接線剛性で正しく組んでおり、影響を受けない。
- **杭断面のねじり剛性 GJ と自重 W の式を修正した**（結果が変わります）。
  - `GJ` が曲げの断面二次モーメント `π(D⁴−d⁴)/64` を使っていた。
    円形断面のねじり定数は断面二次極モーメント `/32` なので**2 倍過小**だった。
  - `W` / `WCorroded` が鋼材断面積を二重に控除していた
    (`Ac` の時点で主筋・テンドンを引いてあるのに、もう一度引いていた)。
    自重が **PHC で 0.5〜1.1%、PRC で 1.4〜5.5% 過小**。
    押込みでは軸力の過小評価（危険側）、引抜きでは抵抗の過小評価（安全側）だった。
  - 例題 3 種の検定テキスト（golden 10 件）は**変わらなかった**。表示桁より小さい差にとどまる。
    引抜き抵抗と自重荷重には効くので、既存モデルは再解析して値を確認すること。


### 修正

- **計算書 (docx) が画面と違う入力で作られていた**のを修正。
  解析完了時に入力ごと複製して切り離す仕組み (`AnalysisResultSet`) を入れたとき、
  画面・グラフ・結果テーブル・検定は `ResultInputModel` (解析時の入力) へ移したが、
  `Output/` 配下だけが `CurrentInputModel` (現在の入力) のままだった。
  解析後に入力を編集すると、諸元表は編集後・解析結果は解析時という混在した計算書になる。
  `ResultSnapshotConsumerTests` で入口と `Output/` の両方を機械的に検査する。
  - 併せて、解析後に入力を編集した状態で出力しようとしたときは、
    「解析を実行した時点の入力で作成する」ことを先に断るようにした。
- **反復解析（基礎梁考慮）を実行すると、入力した矩形荷重が収束後の荷重に書き換わっていた**のを修正。
  荷重表に自分が入れた値と違うものが出るうえ、元に戻すために内部で入力の控えを別に持つ、
  という仕組みが必要になっていた（控えは廃止）。
  収束後の荷重は 3D 表示に出る（表示中のケースの荷重を描く）。
- **各杭の群杭沈下量も「表示中のケース」から読むようにした**。
  3D コンタ・沈下グラフ・部材角ツールチップ・計算書の 18 箇所が
  杭に持たせた複製を読んでおり、ケースを切り替えても古い値が出る余地があった。
  併せて、解析が返した杭ごとの沈下量をそのままケースに記録するようにした
  （以前は杭側の複製から拾い直しており、結果の正が入力側にあった）。
- **矩形荷重を編集すると、保存済みの沈下ケースの中身まで書き換わっていた**のを修正。
  ケースの結果と画面表示が<strong>同じ荷重オブジェクトを共有</strong>していたため。
  グリッド点・矩形荷重とも値のみの複製を渡すようにした。
  - これは第 2 段（複製の撤去）の前提でもある。共有をやめたことで、
    今後保存されるファイルは複製を外しても開ける（`SettlementMirrorTests` で検査）。
- **沈下コンタが「表示中のケース」と食い違うことがあった**のを修正（第 1 段）。
  群杭沈下の結果は `CaseRecords`（ケースごと）が正で、
  `SettlementGridData` はそこから同期される複製という二重管理になっていた。
  同期を忘れた経路があると画面が古い結果を出す。
  表示（沈下コンタ・グラフ）と読込時の解析済み判定を、
  **表示中のケースを直接読む**形に変えた。
  - 複製そのものはまだ消せない。保存ファイルの中でケース側の要素が複製側を `$ref` で参照しており、
    複製を外すと**既存ファイルが開けなくなる**（`SettlementMirrorTests` に実証を残した）。
  - `CaseRecords` を持たない旧ファイルは、読込時に複製から 1 件復元する。
- **同じ解析に 3 つの呼び名が残っていた**のを修正。
  「単杭沈下解析（基礎梁考慮）」が、画面では「基礎梁考慮沈下解析」、
  **計算書（docx）の章題では「基礎梁考慮鉛直解析」**になっていた。
  F6 も一覧では「荷重沈下関係解析」のままだった。
  用語検査が XAML の表示属性と help.html しか見ておらず、
  `Tag` 属性・C# の文字列（ショートカット一覧・ダイアログ・計算書の見出し）が
  死角になっていたため。検査範囲を広げ、あわせて呼び名を揃えた。
- **エラーの詳細がログに残っていなかった**のを修正。
  例外の文言をダイアログに出すだけでログに記録していない箇所が 60 以上あり、
  不具合を知らされても手掛かりが残らない状態だった。
  `MessageService.ShowError` に集約し、Serilog へ記録したうえで
  画面には**ログの場所（ヘルプ タブ → バージョン情報 → ログフォルダを開く）**を添えるようにした。
  併せて、オーナーウィンドウの付かない生の `MessageBox.Show` も解消した
  （別ウィンドウの背面に隠れることがある）。
- **群杭沈下解析だけ「押してから叱る」ままだった**のを修正。
  実行できない理由 7 種類を実行後のダイアログで出していた（F5〜F7 は押す前に灰色になる）。
  判定を 1 か所にまとめ、**ボタンが灰色になり、その理由がツールチップに出る**ようにした。
  - F7（基礎梁考慮沈下解析）がキー操作では CanExecute を迂回していたのも直した。
    F8 は解析の実行ではなくタブの切替なので従来どおり。
- **杭・節点の削除がメイン画面の Ctrl+Z で戻せず、別ウィンドウの Ctrl+Z で戻っていた**のを修正。
  プロセス全体で 1 本の静的な Undo スタック (`UndoService.Instance`) があり、
  メイン画面の DataGrid 編集・杭削除・節点削除がそこへ積まれる一方、
  消費するのは杭頭バネのウィンドウだけだった。
  - メイン画面の Undo は ViewModel のスナップショット履歴に一本化した。
    DataGrid のセル編集は元々そちらにも記録されていたので、積んでいた分は捨てて済む。
    杭・節点の削除は記録が無かったため `SaveUndoState` を呼ぶようにした（**新たに戻せるようになる**）。
  - 杭頭バネのウィンドウは自分の Undo スタックを持つ形にし、静的インスタンスは廃止した。
  - `UndoOwnershipTests` が、共有の静的スタックの復活とメイン画面からの誤った push を検出する。
- **解析結果を捨てる処理を 1 か所にまとめた** (`ClearAllAnalysisState`)。
  捨てるものが「VM のフラグ + CurrentModel」「解析結果セット」
  「入力モデルの中に格納された沈下の結果」の 3 か所に分かれており、
  経路ごとに部分集合しか消していなかった。
  - **新規作成・計算例の読み込みで結果セットが残っていた**。
    前のモデルの結果がグラフ・結果テーブル・ステータスバーに出る。
  - **破棄した沈下結果が入力モデル内に残り、保存 → 再読込で「解析済み」が復活していた**。
    読込時の解析済み判定は `SettlementGridData` 等の有無から推定するため。
  - `AnalysisStateClearingTests` が、フラグを直接落としている箇所を機械的に検出する。
- **沈下だけ再実行すると、陳腐化した水平解析結果の警告が消えていた**のを修正。
  `CaptureAnalysisResultSet` が入力スナップショットを無条件に取り直すため、
  「編集後の入力」と「解析時の水平解析結果」が 1 組に組み直されていた
  (この仕組みが防ぐはずの混在そのもの)。
  水平解析の結果が前回のスナップショットのままなら取り直さないようにした。
- **表示 OFF の一般節点が解析モデルから消えていた**のを修正 (`AnalysisModelling.AddInputNodes`)。
  `IsVisible` は表示専用のフラグで、解析モデルの中身を変えてはいけない。
  `DisplayOnlyFlagTests` で FEM 配下の参照を禁じた。
- **地盤ウィンドウで `y` を打つと入力が消えて Redo が走っていた**のを修正。
  Ctrl+Z / Ctrl+Y の実装が 2 箇所にあり、片方の Y だけ修飾キーの判定が抜けていた。
  `KeyHandlerTests` で英字キーの修飾キー判定を検査する。
- **新規作成で「はい」と答えると空のモデルで上書き保存され得た**のを修正。
  保存を await せずに `Reset()` していた。アプリ終了時は正しく await しており、
  この経路だけが食い違っていた。
- **ヘルプが廃止済みの「検定ウィンドウ」を説明したままだった**のを修正。
  1.0.23-beta でリボンの「検定」ボタンとウィンドウを廃止したが、
  ヘルプの該当節・リボン表・サブウィンドウの説明が旧のままだった。
  検定節を解析結果テーブル基準に書き直し、支持力の判定基準表を追加した。

## [1.0.23-beta] — 2026-08-24

### 修正

- **単一ファイル発行 (publish) が通らなくなっていたのを修正**。
  バージョン情報ダイアログのビルド日取得で `Assembly.Location` を使っており、
  単一ファイル発行では常に空文字を返すためアナライザ (IL3000) がエラーにしていた。
  `AppContext.BaseDirectory` に変更。
  この解析は publish のときだけ走るので、通常のビルドとテストでは検出できなかった。

### 変更

- **NuGet パッケージを更新した**（AvalonDock 4.74.1 → 5.0.0 ほか 6 件）。
  AvalonDock 5.0.0 で `AvalonDock.Layout.Serialization.XmlLayoutSerializer` が
  削除されており、そのままではビルドが通らなくなっていた。
- **ドッキングレイアウトの保存/復元を削除した**。
  復元は呼び出し側でコメントアウトされていて到達不能な状態が続いており、
  保存だけが誰も読まないファイル（`layout.xml`）を書いていた。
  AvalonDock 5.0.0 の新しい方式（DTO を自前で XML 化する）へ移植しても
  検証できないコードが増えるだけなので、この機に整理した。
  必要になったら `DockingManager` の `DtoMapper` で作り直せる。
  DataGrid 列設定とウィンドウ位置の保存は従来どおり残している。


### 追加

- **検定結果を数値で持つようにし、検定比と支配ケースを表示するようにした**。
  検定は応答値と限界値を両方計算しながら比を作らず、`[OK]` / `[NG]` の行を
  テキストに積むだけだったため、「NG が 3 件」としか言えなかった。
  OK でも「あと 3%」なのか「半分」なのかが分からず、
  どの杭・どのケースが効いているかも読み取れなかった。
  - 応答値・限界値の桁数は<strong>量ごと</strong>に変える。
    モーメント・せん断力は小数 1 桁、杭頭回転角は 3 桁、
    基礎梁の傾斜角は 5 桁（限界 1/300 = 0.00333 なので 3 桁だと
    応答と限界が同じ 0.003 に潰れる）
  - リボンの「検定」ボタンと検定ウィンドウを<strong>廃止</strong>した。
    検定は解析結果テーブルから見るようにしたため
  - **解析結果テーブルの一覧にも「検定結果」を追加**した。
    結果を確認しに来た人が最初に開くのは解析結果テーブルなので、
    検定ウィンドウを開いてボタンを押さなくても検定比が見られるようにした。
    荷重ケース・組合せ・液状化を<strong>またぐ 1 枚もの</strong>で、
    検定比の降順に並ぶ（支配ケースを探すには全条件を通した並びが要るため）。
    CSV 出力もできる。
    条件のフィルタ（荷重ケース／荷重組合せ／液状化）は<strong>行に対して</strong>掛かり、
    合う行が無ければ表ごと出ない。
    解析していない条件で絞ったときに表が残ると、
    その条件の結果があるように見えてしまうため。
    表の左端に出る荷重条件のメタ列も、またぐ表では出さない
    （表そのものが条件を持たないため既定値の「液状化 = 無」が並んでしまう）
  - 計算書の内容は<strong>変わらない</strong>
    （テキストは同一であることを golden テストで確認している）

### 内部

- 検定を `EvaluationItem` / `EvaluationResult` として構造化し、
  テキストはそこから組み立てる形にした。
  判定は比から導かず算出元と同じ比較を保っている
  （曲げ・回転角は「超えたら NG」、傾斜角は「限界未満なら OK」で
  ちょうど等しいときの扱いが違うため）。
  案の比較や自動探索の前提にもなる。


### 改善

- **確認ダイアログを、失うものがあるときだけ出すようにした**。
  起動直後に計算例を読み込むだけでも「現状の入力内容は削除されます」が出て、
  何もせずにアプリを閉じるだけでも「現在のデータを保存しますか？」が出ていた。
  失うものが無いのに出す確認は、内容を確かめずに押すだけのものになり、
  本当に失うものがあるときにも読まれなくなる。
  - 対象は 3 箇所: 計算例の読み込み / 新規作成 / アプリ終了
  - 判定は「保存していない作業があるか」(`HasUnsavedWork`) に一本化。
    true になるのは<strong>入力の編集</strong>と<strong>解析の完了</strong>の 2 つで、
    保存・読み込み・新規作成・計算例ロードの直後に false へ戻る
  - 編集の検出には `SaveUndoState`（DataGrid のセル確定も含む全編集の集約点）、
    解析の検出には `SetLatestAnalysisCompleted` を使う。
    解析には時間がかかるので、入力を編集していなくても結果があれば確認する
  - 入力ウィンドウ（基本設定・荷重・地盤・杭体・基礎梁・杭要素分割）は自前の Undo を持ち
    共有の入力モデルを直接書き換えるため、`SaveUndoState` を通らない。
    変更の有無を確実に知る手立てが無いので、**開いたら編集されたものとして扱う**。
    キャンセルで閉じても確認が出るが、
    「編集したのに確認が出ない」でデータを失うよりはよい

### 修正

- **群杭沈下解析の完了でステータスバーの「最終解析」時刻が更新されていなかったのを修正**。
  他の 3 つの解析は完了通知を呼んでいたが、群杭沈下だけ抜けていた。

### 追加

- **バージョンと更新履歴を利用者が確認できるようにした**。
  バージョンは以前から 5 箇所に出ていたが、更新履歴は本ファイルにしか無く
  配布物に含まれていなかったため、利用者からは一切見えなかった。
  - リボンの「ヘルプ」タブに<strong>「バージョン情報」</strong>を追加。
    版・ビルド日・実行環境・ログと自動保存の保存先を表示し、
    「更新履歴を見る」「ログの場所を開く」ボタンを置いた
    （パスは選択してコピーできるようにしてある）
  - ヘルプに<strong>「プログラム更新履歴概要」章</strong>を追加。
    バージョン番号の読み方、直近 5 版の詳細、それ以前の一覧。
    計算結果が変わる版には「結果が変わります」の印を付けた
  - 本ファイル (CHANGELOG.md) は開発者向けのまま残し、
    ヘルプの更新履歴は利用者向けの言葉で別に書く。
    版の抜けはテストで検出する


### 改善

- **ヘルプ第1部の章立てを作業順に組み直した**。UI の構造順（リボンの並び順）で書かれていて
  作業順に読めず、「リボンメニュー」章だけで 4,022 行（第1部の 57%）あった。
  本文は移動のみで、新しい解説は書いていない。

  - 章の並びを「はじめに → クイックスタート → 杭モデル → 操作方法 →
    リボンメニュー概要 → 入力 → データタブ → 解析の実行 → 結果 →
    その他のメニュー → 便利な機能 → FAQ → 設計実務上の扱い」に変更。
    F1 は文書の先頭に着地するため、以前は初めての人が
    告示解釈 181 行を読まされてからクイックスタートに辿り着く並びだった
  - 4,022 行の「リボンメニュー」章を 5 章に分割。
    埋もれていた 14 の入力画面の説明（h4）を節（h3）に昇格した。
    1 章の最大は 4,022 行 → 1,337 行
  - 章の分割にともない、見出しの階層を 1 段ずつ繰り上げた。
    その結果、ヘルプ検索（ヘルプチャット）が索引する見出しが 375 → 451 に増えた
    （h5/h6 に埋もれて検索に出てこなかった 76 節が拾われるようになった）
  - 移動で合わなくなった相互参照と、手書きの章番号「5.1〜5.7」を修正
  - 昇格した見出し 16 個に固定 id を付与


## [1.0.22-beta] — 2026-08-22

### 改善

- **画面とヘルプから開発者向けの表示を減らした**。読んでも次の操作が決まらない内部表記は
  出さず、必要な情報はログに残す方針に統一した。
  - 解析完了ダイアログに出していた「フォールバック（計算失敗→既定値での代替）N 件」の
    警告を取りやめ、解析ログとログファイルにのみ記録するようにした
    （β 版の現段階で、対処のしようがない警告を毎回出す必要はないため）
  - 解析結果テーブルの杭頭ばね「状態」列に、内部のセットアップ経路
    （`CombinedXY(12pts, Mcr=715, axialN=2465kN)` など）がそのまま出ていたのを、
    「剛」「M-θ曲線」「線形ばね」の表記に改めた
  - ダイアログの「InputModel が見つかりません」等を「入力データ〜」に改めた
  - ヘルプの見出しから内部のバージョン記号を削除した

- **わかりやすさの棚卸し (Phase 1〜6)**。「使う人にとって」「読む人にとって」
  「計算内容の説明」の 3 方向で見直した。

  - **明らかな誤りの修正**
    - 「全土層削除」のツールチップが「土層を追加します」だった (直上ボタンからのコピペ)
    - 変形係数 Es の単位 (kN/m³) → (kN/m²)、地表面加速度 (m/s) → (m/s²)、
      質点質量 (ton/m2) → (t/m²)
    - Ctrl+D / F4 / F5 / F6 が code-behind から VM を直接呼び、ボタン側の有効/無効判定を
      迂回していた。判定をコマンドの CanExecute に移し KeyBinding に一本化
      (元の KeyBinding は MVVM Toolkit が生成しない名前を指しており無言で失敗していた)

  - **入門への導線**
    - 起動時の案内ダイアログ (WelcomeDialog) が実装済みなのに一度も表示されていなかった。
      App の起動処理から表示するようにし、DataContext 未設定も修正
    - 未バインドだった「計算例を開く」を接続し、「使い方をみる」を追加
    - リボンのヘルプタブに「クイック スタート」を追加
    - 主要 6 窓 (地盤 / 荷重 / 杭体 / 杭断面 / 杭要素分割 / 水平解析) に
      「このウィンドウのヘルプ」を追加

  - **用語と記号**
    - 沈下系 4 解析の呼び名を「単杭 / 群杭」体系に統一
      (F6 単杭沈下解析 / F7 単杭沈下解析（基礎梁考慮） /
       F8 群杭沈下解析（一般） / 群杭沈下解析（基礎梁考慮・反復）)。
      ウィンドウ名とヘルプ見出しが一致していなかったため、
      タイトルバーの名前でヘルプ内を検索しても当たらなかった
    - 解析結果テーブル 110 列すべてに説明のツールチップを追加
    - リボンの結果選択 (Mh / UH / RX など) に意味のツールチップを追加
    - グラフのホバーだけ α / β₁ / β₂ で、荷重条件・計算書の αL / βU / βL と
      食い違っていた (β₁ / β₂ は杭体応力の低減係数として別の意味で使用) のを統一
    - 「絞り込みなし」がグラフ "All" / テーブル "ALL" と分かれていたのを「すべて」に統一
    - 入力範囲 (Min / Max) をツールチップに表示

  - **メッセージと前提の可視化**
    - 水平解析の実行ボタンが灰色のとき、理由をツールチップで読めるようにした
      (以前は理由を組み立てながらログにしか出していなかった)
    - コマンドパレットが CanExecute を見ておらず、リボンでは灰色のコマンドが
      有効に見えていたのを修正
    - 「杭が無い」だけで 4 通りあった文言を集約し、内部用語の露出を解消
    - 例外オブジェクトをスタックトレース込みで出していた箇所を要約 + ログの場所に変更
    - 計算書出力後の「Word で開き目次を F9 で更新」がトーストで数秒で消えていたのを
      ステータスバーにも残すようにした (3dm / DXF / MGT の出力先も同様)
    - ステータスバーに未完了の段も灰色で並べ、次に何をすべきか分かるようにした

  - **手順の説明**
    - リボン・クイックヒント・ヘルプで 3 通りあった手順を、
      ヘルプの Step 1〜8 に統一

  - **ヘルプ**
    - h1〜h3 の見出し 98 個に固定 id を付与
      (無いと見出しを 1 つ足すだけで以降の URL がずれる)
    - 手書きの章番号がずれていた相互参照を修正
    - 開発者向けの記述 (クラス名・版数マーカー・デバッグ実測値) を除去

  - **開発者向け文書**
    - `README.md` / `CLAUDE.md` を新設。構成・暗黙の前提 5 件・
      メッセージの規約・用語表・ビルド手順を集約
    - 一過性の実装メモを `Docs/archive/` へ移動

### 内部

- 「ビルドは通るが実行時に静かに壊れる」種類の不具合を機械的に検出するテストを追加
  (コマンドバインドの実在 / ヘルプアンカーの双方向 / 結果列の説明の有無 /
   引退した用語の復活 / 内部用語の露出)。テストは 1274 件 → 1296 件。

## [1.0.21-beta] — 2026-08-22

### 追加

- **解析結果の保存／復元が「解析時の入力」も持つようにした**。
  編集途中で保存したファイルを開き直したとき、結果が現在の入力を基準に描かれて
  混在表示に戻ってしまうのを防ぐ。
  - `ProjectData.ResultInputSnapshot` / `ResultCapturedAt` を追加。省略可能なプロパティなので
    **`FormatVersion` は上げていない**（旧ファイルは欠落プロパティが既定値 null になるだけで、
    従来どおり現在の入力を解析時の入力として扱う）
  - `AnaModel` が同じ入力を参照しているため、`ReferenceHandler.Preserve` のもとでは
    実体は 1 つで `$ref` になり、増えるのは参照 1 個分（テストで確認）
  - 読込時に結果セットを復元し、保存時点で入力が編集済みだった場合は
    「入力が変更されています」の状態も引き継ぐ
  - 解析結果を保存しない設定のときはスナップショットも保存しない

### 修正

- **沈下解析の結果は従来どおり入力変更で破棄するようにした**。
  水平解析の結果は解析時の入力ごと切り離して保持するが、沈下の結果は
  `PileGroupSettlement.CaseRecords` / `SettlementGridData` / 各杭の `GroupPileSettlement` のように
  **入力モデルの中に**格納されており切り離せない。残すと杭配置グリッドなど入力系の表示に
  古い値がそのまま出るうえ、傾斜角検定の可否判定にも使われてしまう。
  破棄対象（沈下解析結果 / 杭要素分割）は 1 回のダイアログでまとめて確認し、
  対象が無ければダイアログは出ない（沈下解析を使っていなければ従来どおり無確認で編集できる）
- **MGT エクスポートが現在の入力からモデルを組み直すようにした**。
  `CurrentModel` は解析を実行した時点のスナップショットなので、そのまま出力すると
  入力を編集したあとに画面と違う形状が出ていた。前提条件も「水平解析が実行済み」から
  「杭要素分割が実行済み」に改めた（分割が無いと FEM モデルを組めないため）

- **「解析結果を削除」しても解析結果セットが残っていたのを修正**。
  結果セット（解析時の入力スナップショット）が残ると `ResultInputModel` が解析時の入力を
  返し続け、消したはずの結果の痕跡（ステータスバーの実行時刻、グラフの「断面の基準」切替）が
  残っていた。結果を消すすべての経路で結果セットも破棄する
- **入力を変更したあとに水平解析ウィンドウを開くと、古い結果が新しい解析モデルへ転写されていたのを修正**。
  `SeedEditModelFromMainCurrentModel` は「入力が変わっていれば結果は破棄されているはず」という
  前提で節点・要素の件数だけを見ていた。結果を保持するようになりこの前提が崩れたため、
  節点数が変わらない編集（断面変更・軸力変更・杭の移動など）では古い結果がそのまま転写され、
  「済」列が解析済みと主張し、追加実行で新旧の結果が混ざっていた。
  解析後に入力が編集されていれば転写しないようにした

- **保存ファイルが「Reference 'nnnn' was not found」で開けなくなっていたのを修正**。
  `AnaModel.InputModel` が getter のみで、書き出されはするが読込時に読み飛ばされていた。
  `ReferenceHandler.Preserve` では実体は「最初の出現」に `$id` 付きで書かれるため、
  解析時の入力スナップショットの実体がここに載ると読込時に `$id` が登録されず、
  後続の `$ref` が解決できなくなる（読み飛ばされた部分木の中の `$id` もすべて巻き添え）。
  `[JsonInclude]` + private setter にして復元させるようにした。
  この形で保存済みのファイルもそのまま開けるようになる

- **保存したファイルを開き直すと杭・根入れ部が「分割後」の色（青）で描かれる問題を修正**。
  メイン画面の杭の色は杭要素分割の状態だけで決まる（黄＝分割前 / 青＝分割後）が、
  この状態はファイルに保存されておらず、読込時に「AnaModel に節点があるか」で推定していた。
  解析結果を保持したまま杭要素分割だけを取り消せるようになったため、この推定は成り立たない
  （分割を取り消しても結果の AnaModel は残るので、開き直すと分割済みに戻ってしまう）。
  `ProjectData.IsElementSplit` として状態そのものを保存するようにした。
  省略可能なプロパティなので旧ファイルは従来どおり推定する

- **「解析時の入力」基準のグラフが何も表示されない問題を修正**（M-φ グラフで報告）。
  杭 (`PileLayoutDataItem`) の `Beams` / `PileNodes` / `SoilNodes` / `HorizontalSoilSprings` /
  `PileTopRotationalSpring` はいずれも解析ランタイム状態として `[JsonIgnore]` で、
  解析結果スナップショットの JSON 往復で失われていた。結果表示はここを辿って
  断面力や M-φ を引くため、杭ごとに結果を引く表示が軒並み空になっていた。
  スナップショット作成時にインデックス対応で張り直すようにした
  - 併せて、**保存ファイルを開き直したときにも同じ関連が失われる問題**を解消した。
    これらは FEM モデルを組むとき (`AnalysisModelling`) にしか設定されないため、
    解析結果を含むファイルを読み込んでも杭から要素を辿れなかった（解析結果セット導入前からの穴）。
    `ProjectData.PileFemLinks` に対応表（インデックスのみ）を保存して読込時に張り直す。
    省略可能なプロパティなので旧ファイルは従来どおり

- **ダミー梁を含むモデルで、解析結果を含む JSON の読込が失敗していたのを修正**。
  `DummyBeam` が get のみのプロパティ + 引数付きコンストラクタだったため、
  System.Text.Json がコンストラクタ経由で復元しようとし、`ReferenceHandler.Preserve` の
  `$ref` をコンストラクタ引数へ渡せずに
  「Reference metadata is not supported when deserializing constructor parameters」で
  例外になっていた。節点は他要素と共有される（＝ 2 個目以降は必ず `$ref` になる）ため、
  ダミー梁があるモデルでは必ず起きる。`Beam` と同じく既定コンストラクタ + setter の形に揃えた。
  この不具合は解析結果セット導入前から保存ファイルの読込に存在していたが、
  解析完了時のスナップショット作成でも同じ経路を通るため顕在化した

- **逆シリアライズ後の `AnaModel` が入力への参照を失っていた**のを修正。
  `AnaModel.InputModel` は getter のみで、JSON へ書き出されはするが読込時に捨てられ、
  ロード直後は null だった。`RebindInputModel` で張り直せるようにし、
  結果セットの復元時に解析時の入力と結び付けるようにした

- **断面性状グラフに「断面の基準」の切替を追加**（N-M 相関 / 定着部 N-M / Q-N / 杭体 M-φ / EI-φ）。
  解析結果を保持したまま、編集中の断面で曲線を見られるようにした。
  - 既定は「解析時の入力」。曲線と、重ねて描く解析結果の点（レベル1/レベル2 地震時）が
    同じ断面で整合する
  - 「現在の入力」に切り替えると編集中の断面で曲線を描く。曲線と断面が食い違うため
    **解析結果の点は表示しない**（「現在の入力の断面（解析結果は非表示）」と注記を出す）
  - **解析結果は破棄されない**。基準を戻せばすぐ結果付きの表示に戻る。
    これまでは現在の断面で曲線を見るには解析結果を全部捨てるしかなかった
  - 杭頭 M-θ 系は対象外。曲線が「解析時に杭頭ばねへ設定された構成」そのもので、
    現在の入力から作り直す経路が無いため
- **グラフウィンドウの XAML スモークテストを追加**。StaticResource のキー誤りは
  ビルドを通ってしまい、ウィンドウを開いた瞬間に例外になるため

- **入力を変更しても解析結果が消えなくなった**。実務では解析結果を横目に見ながら入力を変えていくため、
  少しでも触ると結果が破棄される従来の運用は成り立たなかった。
  - 解析完了時に「解析を実行した時点の入力」と結果を 1 組に複製して切り離す
    （`AnalysisResultSet`）。複製は保存と同じ JSON 往復（`ReferenceHandler.Preserve`）で行うため、
    入力と結果が相互に参照し合ったまま丸ごと切り離される
  - 結果表示系（グラフ・結果キャンバス・断面照査）は現在の入力ではなく
    スナップショット（`MainWindowViewModel.ResultInputModel`）を見る。
    現在の入力を混ぜると「変位は解析時・断面は編集後」という読み手が区別できない図になるため
  - 入力を変更するたびに出ていた「解析結果が削除されます」の確認ダイアログを廃止。
    荷重条件の変更ではダイアログが一切出なくなる
  - ステータスバーに「解析結果: 実行時刻」を常時表示。入力を変更すると
    「表示中の解析結果は … 実行時の入力によるものです（入力が変更されています。再解析が必要です）」に変わる
  - 解析結果は明示的な破棄コマンド（`DiscardAnalysisResults`）でのみ消える
  - 杭要素分割は解析結果ではなく入力側の状態なので、従来どおりジオメトリ変更時に確認のうえ無効化する

### 修正

- **コピーした入力の要素が「現在の入力」を見てしまう問題を修正**。
  `PileLayoutDataItem.InputModel` が `MainWindowViewModel.CurrentInputModel` を返していたため、
  現在編集中でない InputModel に属する杭でも現在の入力の断面・地盤を拾っていた。
  親モデルへの明示的な参照を持たせ、自分が属するモデルを返すようにした
  （解析結果のスナップショットが現在の入力に化けるのを防ぐ）


### 修正

- **杭頭 M-θ が現ステップの軸力に追随するようにした**（杭体 M-φ と揃えた）。
  杭軸力は解析中に動く（`SetVectorDF` が設定した `AxialForceIncrement = (N_seis − VL)/nStep` を
  `UpdateF` が毎ステップ加算する、VL から入力地震時軸力までの荷重ステップ比例のランプ）。
  杭体の M-φ は元からこれに追随していたが、杭頭 M-θ はケース開始時の 1 回きりで
  満載時の地震時軸力に固定されており、序盤のステップで杭体と杭頭が別の軸力の断面として
  振る舞っていた（Example10 の杭頭で step 1 の Mcr を 864 → 715 と 17% 過小に見ていた）。
  ステップ毎に作り直す `UpdateMThetaByCurrentAxialForLoadCase` を追加し、
  クラック履歴（`HasCrackedXY` / クラック方向 / `ThetaProjMax`）は保持するようにした
  （ここでリセットするとヒステリシスが壊れる）。
  ケース開始時のセットアップは従来どおり入力地震時軸力で構築する
  （直後のスナップショットが画面・計算書の表示用に使われるため）。
  M-θ の曲線生成は場所打ち RC で重いので、(杭体No, 軸力 1kN 丸め) でキャッシュする。
  結果への影響は小さく、計算例では最大 0.2%（ExampleK8 のみ変化、他 3 例題は変化なし）。
  収束は概ね改善（ExampleK8 L1 82 → 70 回、L2 130 → 117 回）。
  解析時間は計算例 4 件で 9s → 14s と増える

- **1.0.20-beta で入れた「M-φ に常に地震時軸力を使う」変更を revert した**（誤診に基づく変更だったため）。
  ステップ毎の M-φ 再解決が使う `model.GetAxialForce` は「常時軸力 VL 固定」ではなく、
  `SetVectorDF` が設定した `AxialForceIncrement = (N_seis − VL)/nStep` を `UpdateF` が
  毎ステップ加算した**荷重ステップ比例のランプ**で、最終ステップでちょうど入力の地震時軸力に一致する
  （実測 Example10 L2 / 16 step: 3461.6 → … → 2465.0、VL=3528・N_seis=2465）。
  入力した地震時軸力は元から反映されており、しかも荷重レベルと整合していた。
  常に N_seis を使うと荷重レベルと軸力が不整合になるため元に戻した。
  収束後の結果への影響は元々ほぼ無く（≤0.12%）、変わるのは反復数のみ。
  同じ読み違いを繰り返さないよう、ランプであることをコードコメントとテストで明示した
  （`MPhiAxialForceSourceTests.AxialForce_RampsFromGravityToSeismicOverLoadSteps`）。
  なお 1.0.20-beta で併せて直した「例題ビルダーの軸力欠落」はテスト側の修正なので維持する
  （水平変位が 0〜30% 変わったのはこちらの効果）

- **限界状態のステップラインが常に軸力 0 の耐力で描かれていた**のを修正（画面の杭応力分布グラフと
  計算書の両方）。`(Fxi + Fxj) / 2` で要素中央の軸力を求めていたが、梁要素の端力は軸方向で
  逆対称 (`Fxj = -Fxi`) なので恒等的に 0 になっていた。断面照査と同じ設計軸力
  （ケース別の入力地震時軸力、未入力なら常時軸力）を使うようにし、
  `PileLayoutDataItem.GetDesignAxialForce` に集約した。
  実測影響: 場所打ちRC 1200φ で安全 M が 1738 → 2859（**39% 過小**、保守側）、
  鋼管の充填鋼管部 400×12.7 では N=3000kN のとき損傷 M が 408 → 254（**61% 過大＝危険側**）。
  グラフ／計算書の限界線と照査画面の判定が食い違う問題も解消する
- **「入力値＋応力解析結果」モードで、解析軸力が毎ステップ累積されていた**のを修正。
  `CumulativeForce.Fxi` は増分ではなく「そのステップまでの累積軸力」なのに、毎ステップ
  引き続けていたため、解析軸力の寄与が約 (nStep+1)/2 倍に膨らんでいた
  (Example10 L1・4 ステップで 17.0 → 33.9 と 2 倍を実測。nStep=16 なら約 8 倍)。
  前ステップで適用済みの分を打ち消してから今ステップの値を適用するようにした。
  対象は P-S ばね無効時の通常分岐（案 Z 分岐は元から代入で正しい）
- **`GetSeismicAxialForce` の境界チェックが誤っていた**のを修正。レベル 2 でも
  `AxialForceLevel1s.Count` で判定して `AxialForceLevel2s` を索引していたため、
  2 つのコレクションの長さが違うと有効な値まで弾かれていた（呼び出し側は例外を握りつぶして
  重力ベースへ黙って落ちるため気付けない）。下限も `<= 0` で弾くようにした
- **軸力の受け渡しの回帰テストを追加** (`AxialForceSourceAuditTests`)。上記 3 件はいずれも
  修正を外すとテストが落ちることを確認済み

## [1.0.20-beta] — 2026-08-21

### 修正

- **「入力値」モードで、入力した地震時軸力が M-φ に反映されていなかったのを修正**。
  ステップ毎の M-φ 再解決 (`SetupMPhiByCurrentAxialForMiddleBeam`) が常に
  `model.GetAxialForce`（= 常時軸力 VL）を見ており、しかもステップループの先頭で
  初期セットアップの曲線を上書きしていたため、`AxialForceLevel{1,2}s` に入力した
  地震時軸力は一度も使われていなかった。M-θ（杭頭回転ばね）は地震時軸力で作られており、
  同じケースの中で杭頭ばねと杭体が違う軸力を見ている状態だった。
  軸力の優先順位を `ResolveMPhiAxialForce` に集約し、M-θ と揃えた:
  「入力値」モードは地震時軸力優先（未入力なら重力ベース）、
  「入力値＋応力解析結果」モードは従来どおり解析 Fxi 加算後の現在軸力（重ねると二重計上になるため）。
  **既存の非線形水平解析の結果が変わる**（1200φ・Fc27・16-D29 で、変動下限の杭は Mcr を 22% 過大、
  上限の杭は 17% 過小に評価していた）。要再解析

- **同じ入力でも水平非線形解析の結果が実行ごとに変わる問題を修正**。全体剛性の組立
  (`AnaModel.MapOnKmat`) で、beam の要素剛性を `ConcurrentBag` に集めた thread-local の
  COO リストから加算していた。beam のスレッドへの割り当ても bag の列挙順も実行ごとに変わるため、
  重複する (row, col) の浮動小数加算順が変わり、K が ULP レベルで揺れていた。
  非線形 NR ではこの揺れが line search / bisection の分岐を変え、計算例10 / L2-1.C1.Liq では
  反復数が 175 ⇄ 353、代表変位が 3% 振れていた（低頻度で収束回帰テストが落ちる原因でもあった）。
  beam を固定長の連番チャンクに切り、チャンク番号順に連結することで加算順を beam の
  インデックス順に固定した。並列組立は維持したまま、逐次実行とビット一致し、コア数にも依存しない。
  なお発端として疑っていた M-φ キャッシュは無関係だった（キャッシュの温冷はスレッドの
  タイミングを変えるので相関して見えていただけ）

### 改善

- **SC杭ライブラリの重複製品 5 組を削除**（`SC-500-標準-105-14/16`、`SC-600-標準-105-14/16/19`）。
  No. だけ違う完全重複で、断面タイプの選択肢に同じ名前が 2 回並んでいた。193 → 188 行
- **使われていない `Models/ShapeDrawer.cs` を削除**（246 行）。断面図の描画は
  `PileSectionViewModel` 側だけが生きており、こちらはどこからも生成されていなかった。
  「双子の描画ロジックを両方直す」という負担が消える
- **収束回帰テストの静的状態を入口で既定へ戻すようにした**。`ConcreteModelOptions` は
  プロセス全体で共有され、他のテストが書き換えたまま残ると材料モデルが変わる
- **M-φ キャッシュキーに鋼材ヤング係数の出所を追加**。オプション違いの取り違えを防ぐ
- **M-φ キャッシュの決定性を損なう欠陥を 2 件修正**
  - キーは軸力を 1kN に丸めるのに曲線は生の軸力で計算しており、同じキーに
    「最初にそのキーを作った軸力」の曲線が入っていた。キーと計算で同じ量子化を使うようにした
  - キャッシュ上限 (10,000) に達すると全クリアしていた。いつクリアされるかが実行履歴で
    変わるため、以後は積まないだけに変更した
- **再実行でビット一致することを検証するテストを追加** (`RepeatedRunsAreBitIdentical`)。
  同一入力で 2 回解析し、反復数だけでなく変位・反力まで完全一致を要求する。
  許容差を置くと「わずかに揺れる」種類の欠陥を見逃すため
- **収束回帰テストで M-φ キャッシュを入口でクリアするようにした**。組立順を固定して
  結果が決定的になったため、クリアしても反復数は変わらなくなった
- **例題ビルダー (`BuildExampleInputModel`) が杭の軸力を写すようにした**。
  `AxialForceVL0` / `AxialForceLevel{1,2}s` を写していなかったため、解析系テストの模型は
  全杭 N=0 で、M-φ が軸力ゼロでしか作られていなかった（Example10 の 1200φ では
  Mcr が常時軸力時の 40%、My が 50% にしかならない別断面を試験していた）。
  併せて収束回帰スナップショットを取り直した（水平変位が 0〜30% 小さくなる = 杭が本来の剛性になった）
- **M-φ の軸力の出所を検証するテストを追加** (`MPhiAxialForceSourceTests`)


### バグ修正

- **杭先端の拡大根固め部の描画を 5 箇所で統一**。同じ形を 杭姿図 / 擬似3D / Viewport3D /
  DXF / 3dm で別々に組んでおり、表示が食い違っていた。位置の決定を `PileToeShape` に集約した
  - Viewport3D が**拡大根固め杭を拡底コーンとして描いていた**（杭姿図は円柱）
  - DXF / 3dm が**球根を杭先端から下向きに描いていた**（杭姿図・3D は上向き）
  - Viewport3D / DXF / 3dm が **Hybrid ニーディングを認識していなかった**
  - 拡底部の立上り・側面角度が Viewport3D / DXF / 3dm で `0.3m` / `12°` のハードコードになっており、
    入力値（場所打ち拡底杭先端立ち上がり・杭先端角度）が反映されていなかった


### 変更

- **曲げ剛性 EI に PC鋼材の換算断面二次モーメント項を追加**（既定を新式に変更）。
  EA には `Ep·Ap` が入っているのに EI には無く、カタログの換算断面二次モーメント Ie より
  小さい値になっていた。EI は梁要素の曲げ剛性になるため、水平解析の変位・断面力に直接効く
  - `Ie = I + (1/2)(np−1)·Ap·(Dp/2)² + (1/2)(nr−1)·Ag·(Dr/2)²`。**鋼材ごとに配置円も換算比も分ける**
    （JP-NPRC は 232 行すべてで PC鋼材と異形棒鋼の配置円が異なり、分離でカタログ Ie と最大 0.046% 一致。
    一括だと 1.41% ずれる）
  - EI の増加率（中央 / 最大）: JIS PHC 3.74 / 5.39%、JIS PRC 2.94 / 3.69%、MS-hi105 4.16 / 5.80%、
    DAM105 2.35 / 3.19%、JP-NPH 3.91 / 5.91%、**JP-NPRC 12.36 / 29.53%**
  - **SC杭と場所打ち系は影響なし**（PC鋼材が無いため）。既存モデルは再解析が必要

### 新機能

- **鋼材のヤング係数の出所を選べるようにした**: 基本設定に「製品カタログの値」（既定）と
  「基礎部材の強度と変形性能の値」の切り替えを追加
  - カタログ値はメーカーで食い違う（異形棒鋼 200,000 / 205,000、鋼管 200,000 / 205,000）
  - 指針側は既製杭が **ヤング係数比 n = 5 固定**、場所打ち系の鉄筋・鋼管と SC杭の鋼管が E = 205,000
  - 差し替えは製品を断面へ反映する時点で行うため、EA・EI だけでなく N-M 曲線・M-φ にも一貫して効く
  - ヘルプに出所の整理表と換算断面二次モーメントの式を追加


### 新機能

- **Hybrid ニーディング工法（三谷セキサン）の支持力に対応**: 杭工法に `埋込み杭（Hybridニーディング）` を追加
  （大臣認定 TACP-0586 / 0587 / 0588、引抜きは日本建築センター評定 BCJ評定-FD0421-03 / FD0422-03）
  - **適用は三谷セキサンの既製コンクリート杭に限る**。ジャパンパイルの製品（JP-NPH / JP-NPRC）が
    使われている場合は解析前の警告で通知する
  - 押込み `Ra = 1/3{α·N·Ap + (β·Ns·Ls + γ·qu·Lc)·ψ}`、引抜き `tRa = 2/3{κ·N·Ap + (λ·Ns·Ls + μ·qu·Lc)·ψ} + Ws`
  - 先端支持力係数 `α = 200e(e+0.2)`（砂質・礫質）/ `200e²`（粘土質）。カタログ p.3 の対比表 11 点をテストで固定
  - **引抜きに先端項 κ·N·Ap がある**（既存の引抜き抵抗は周面のみ）。設計拡径比 e が 1.3 以下、
    または軸部を拡大掘削する場合（es > 1.0）は κ = 0
  - **根固め部径 D3 は入力しない**。設計拡径比 e と節部径 D1 から `D3 = e·D1` として導出し、
    姿図・3D・CAD 出力が参照する杭先端径に書き戻す。杭体ウィンドウには導出元の
    **節部径 D1 を読み取り専用で表示**し、D3 の欄は読み取り専用（設計拡径比や断面を変えた時点で即座に追随する）
  - 周面は ストレート形状 / 節付き形状 × 標準型 / 摩擦強化型 の 4 通り。摩擦強化型では設計掘削径比 es が掛かる
  - 先端平均N値は先端支持力算定位置の下方 1·D1 〜 根固め部上端（e ≦ 1.6 で 2m、e ≧ 1.7 で 3m）。
    周面摩擦の算定範囲は先端支持力算定位置（杭先端より杭下長 Lu 上）まで
  - 沈下曲線の極限先端支持力に本工法の値をそのまま使う（Smart-MAGNUM と同じ扱い）
  - 根固め部と杭周固定部を杭姿図・3D 表示・計算書の姿図に描画。計算書に支持力算定根拠表を追加
  - 杭体ウィンドウの入力パネルとヘルプから工法カタログ (PDF) へのリンクを開ける

### 改善

- **メーカー判定を `PileMakers` に共通化**: 高支持力杭工法とメーカーの組み合わせ検査を一箇所に集約した
  （断面タイプと製品名の接頭辞で判定。メーカーを特定できない JIS 規格品はどの工法でも許容する）

### 改善（節杭の表示）

- **杭断面図に節部径の円と節部側面図を追加**: 節杭の断面（軸部の切り口）はストレート杭と見分けが
  つかないため、節部径を一点鎖線の円で重ね、断面図の脇に節 3 個分の側面図（形状・節部径・ピッチ）を並べた。
  画面と計算書の双子の描画ロジックから共通ヘルパー `NodularSectionDrawing` を呼ぶ形にしている
- **諸元表の「杭断面タイプ」にメーカーと製品系列を表示**: `BF.S先端軸部` のような製品名だけの
  断面タイプが PHC節杭 であることを備考で示す（例: 「三谷セキサン BF.S パイル (PHC節杭) の先端軸部。
  節はこちらに付く。断面耐力は PHC杭 と同一」）
- **杭姿図の塗りを統一**: 節杭の区間だけ白のままだったのを、ストレート杭の区間・根固め部・拡底部と
  同じ薄い青に揃えた（外形の描き方が Rectangle か Polygon かで見た目が変わらないよう色を一元化）
- **杭断面図を塗るようにした**: コンクリート断面は薄い青、鋼管断面は見分けられるよう一段濃い青。
  外径・内径の 2 円を EvenOdd で塗ることで中空断面の抜きがそのまま表現される
  （帯筋は配筋を示す線なので塗らない）。杭姿図・断面図・節部側面図の塗り色は
  `NikkenBrush.PileConcreteFill` / `PileSteelFill` に一元化し、図によって杭の見え方が変わらないようにした
- **メイン画面の 3D 表示で杭体を薄く塗るようにした**: 輪郭の色に合わせて要素分割前は薄い橙、分割後は薄い青。
  擬似 3D の円筒シルエット（上面の楕円の上半分 ─ 左右の母線 ─ 底面の楕円の下半分）に沿わせており、
  要素の継ぎ目では上端も楕円の下半分にすることで上下の帯が完全に一致し、重なりも隙間も生じない
  （重なると半透明の継ぎ目だけ色が濃くなる）。杭体・節杭の節・拡大根固め部・場所打ちの拡底部が対象で、
  回転貫入杭の羽根（螺旋）は対象外
- **メイン画面の 3D 表示で、拡大根固め部に埋まる節を破線で描くようにした**: 直接は見えない範囲だが、
  節が根固め部のどこに位置するかは支持力の前提（先端有効断面積が節部径基準）なので示す。
  根固め部内部の杭体と同じ破線に揃えている

### バグ修正

- **杭断面ウィンドウを開いただけで断面タイプが失われる問題** を修正。
  上の区間を削除して節杭の区間が最上段になると選択肢から節杭が外れ、ComboBox の SelectedItem が
  null になって TwoWay バインドがその null を書き戻し、断面タイプが場所打ち RC に化けていた
  （杭姿図とメイン画面から節が消え、選択肢も旧 3 種類だけになる）
  - 現在選ばれている断面タイプは、選択肢の条件から外れていても必ず選択肢に含めるようにした
  - null / 空文字の書き戻しでは断面タイプを変更しないようにした
  - 未知の断面タイプのフォールバック先を杭体タイプに合わせた
    （既製コンクリート杭なら PHC杭、鋼管杭なら鋼管部。従来は常に鉄筋コンクリート部だった）
  - **「最上段の区間では節杭を選べない」制限を撤廃**した。判定に使う `IsTopSegment` は区間更新が
    走るまで false のままで、同じ状態でも「初回は選べるが開き直すと消える」不安定な挙動だった。
    また Smart-MAGNUM / Hybrid ニーディングは先端が節杭である前提の工法で、1 区間だけで
    モデル化したい場合に節杭が選べなくなる。最上段に節杭が来ていることは入力チェックの警告で知らせる

- **杭姿図で節杭の立上りの横線が描かれないことがあった問題** を修正。
  縮小表示では 4 本すべてを省略していたが、節の範囲を示す立上りの 2 本は縮尺によらず描くようにした
  （節部平坦面の 2 本は潰れる縮尺では従来どおり省略する）
- **共有ヘルパー `DrawLine` が実線を描画していなかった問題** を修正。
  `StrokeDashArray = [0]` により破線パターン長が 0 になり線が消えていた（コード内に「描けない」というコメントが残っていた）。
  これにより杭姿図の土層境界線・Z 目盛の目盛線も描かれるようになる


## [1.0.19-beta] — 2026-08-19

### Changed

- **保存オプションを既定 ON にし、設定を永続化**: 「手動保存に解析結果も含める」の既定を ON に変更した。
  ファイルを開き直したときに再計算なしで前回の解析結果 (水平解析・基礎梁考慮鉛直解析) を確認できる。
  結果が不要でファイルを軽くしたい場合は従来どおり OFF にできる
  - チェックの状態を `LocalAppData` の `PileDesign\user_settings.json` に保存し、次回起動時に引き継ぐようにした
    (従来はセッション内の状態で、起動のたびに OFF に戻っていた)
  - 自動保存側は定期実行で数十 MB の書込が繰り返し発生するため、既定 OFF のまま
  - 単杭沈下・群杭沈下の結果は入力データの一部として、このチェックに関わらず従来どおり常に保存される

### ➕ 新機能

- **Smart-MAGNUM 工法（ジャパンパイル）の支持力に対応**: 杭工法に `埋込み杭（Smart-MAGNUM）` を追加
  （大臣認定 TACP-0625 / 0626 / 0627、引抜きは GBRC 性能証明第 20-21 号）
  - **適用はジャパンパイルの既製コンクリート杭に限る**。杭体タイプが既製コンクリート杭のときだけ
    工法の選択肢に現れる（カタログの適用杭種には鋼管杭も含まれるが本プログラムでは対象外）。
    他メーカー製品（三谷セキサン BF.S / MS-hi105 / Hi-SC105 / DAM105）が使われている場合は解析前の警告で通知。
    メーカーを問わない JIS 規格品（PHC- / CPRC- / SC-）は適用対象として扱う
  - 杭体ウィンドウの入力パネルとヘルプから**工法カタログ (PDF) へのリンク**を開ける
  - 押込み `Ra = 1/3{α·N·Ap + (β·Ns·Ls + γ·qu·Lc)·ψ}`、引抜き `tRu = (0.8·β·Ns·Ls + 0.9·γ·qu·Lc)·ψ` を実装。
    長期 Ru/3・短期 2Ru/3 が既存の使用限界 Ru/3・損傷限界 Ru/1.5 と一致するため限界状態の扱いは他工法と共通
  - 先端支持力係数 `α = 240ωp^1.5 + 45(2+LL')ωp`（砂質・礫質）/ `210ωp^1.25 + …`（粘土質）。
    カタログ p.10 の α 対比表 15 点と計算例（ωp=1.52, LL=1.0 → α=654）をテストで固定
  - **先端面積は節部径 Don 基準**（姿図・N値範囲に使う根固め部径 Den とは別物）。
    Don / Dos は杭区間の断面から自動導出するため、節杭ライブラリ（JP-NPH / JP-NPRC / BF.S）を選ぶだけで入力不要
  - **周長 ψ は節杭では節部径基準**。基礎指針'19 の一般式による他工法は従来どおり軸部径固定（安全側）
  - 杭先端平均N値は Nu（上方2m）と Nl（下方 LL+Den+Don）の重み付き平均。周面摩擦の算定範囲は杭先端の 0.4m 上まで
  - 標準型 / 周面強化型 × ストレート杭 / 節杭 の 4 通りの β・γ に対応。拡大比 ωs は拡翼掘削範囲内の節杭区間にのみ適用
  - 適用範囲（ωs・ωp 1.00〜2.00 / LL 0〜2m / 掘削径 ≤2.5m / N値 / Ns / qu / 杭径）を解析前の警告で通知。
    範囲外はクランプ後の値で計算を続行する
  - 一軸圧縮強度 qu は入力の粘着力 Cu から `qu = 2Cu` として換算（ヘルプに明記、テストで固定）
- **Smart-MAGNUM の沈下曲線への反映**: 沈下曲線の極限先端支持力に本工法の極限先端支持力そのものを使う
  - カタログに荷重-沈下関係の規定が無いため、曲線形状と α・n は基礎指針の値を据え置き
  - 変位スケール `0.1·Dp` の Dp には拡大根固め部径 Den を使用。`SettleRpu == Rpu` が厳密に成立することをテストで固定
  - 周面の τ-s ばねは τ2・τT・ψ を支持力と共有するため、0.4m 除外・節部径周長・引抜き係数が
    沈下解析と水平解析の鉛直ばねにも自動的に反映される
- **Smart-MAGNUM の掘削形状を描画**: 根固め部（杭先端の 2m 上 〜 LL 下）と拡翼掘削部を
  杭姿図・3D 表示・DXF / 3dm 出力・計算書の姿図に追加。Des > Den でも姿図に収まるよう横スケールを調整
- **計算書に Smart-MAGNUM 支持力算定根拠表を追加**: Don / Dsn / Den / ωp / LL / α / Nu / Nl / N / Ap を
  カタログ計算例と照合しやすい並びで出力（該当する杭がある場合のみ）

### 🔧 改善

- **杭工法名を定数化**: `PileConstructionTypeNames` を新設し、プレボーリングの表記揺れ 3 種
  （「プレボーリング」「プレボーリング杭」「プレポーリング」）の判定を `IsPreboring()` に集約。
  個別に `==` で比較していた箇所は表記揺れのファイルで分岐が静かに外れる危険があった

### 🐛 バグ修正

- **3D 表示の拡大根固め部が高さ径比を無視していた問題** を修正
  （`MainWindow.CanvasElements.cs` が高さを `根固め部径 × 2.0` とハードコードしており、
  杭体の描画下限は比を使っていたため、比が 2.0 以外だと球根の外形と杭体破線の切替位置がズレていた）

### ➕ 新機能

- **PHC節杭 を杭断面として選択可能に**: 既製コンクリート杭の断面タイプに `PHC節杭` を追加
  - 断面タイプは杭体区間ごとに選べるため、「下杭のみ節杭・上杭はストレート PHC」といった
    高支持力杭工法での使い分けをそのまま表現できる
  - 断面耐力（N-M / N-Q / M-φ / EA / EI）は軸部基準でストレート PHC 杭と完全に一致するため、
    断面計算クラスは新設せず既存の `PHCSection` をそのまま使う。同一性はテストで固定
  - 自重はカタログ標準質量から算定（節部体積を幾何積分してもカタログ質量を再現できないため）。
    このため基本設定のコンクリート単位体積重量 γc は節杭には適用されない
  - **周面抵抗は軸部径のまま**（節の効果は未考慮＝安全側）。工法ごとの評定式が必要なため未対応で、
    断面ウィンドウ・諸元表・ヘルプにその旨を明記
  - **杭頭タイプ（標準 / 拡頭中間径 / 拡頭）を直上の杭区間の径から自動判定**。継手で接合する以上
    杭頭径は直上区間と一致する必要があるため、カタログの拡頭形状一覧から Dt が一致する製品を選ぶ。
    一致が無い場合は標準タイプとして扱い入力チェックで警告。HS タイプは未対応
  - **最上段の杭区間では PHC節杭 を選べない**（上杭に継ぐ下杭として使うため）。
    断面タイプの選択肢から除外し、上の区間を削除して最上段になった場合は入力チェックで検出
  - 杭姿図・3D 表示に**節の形状**を描画。Do・D はカタログ値、テーパーは姿図実測で厳密に 45°
    （軸方向長 =(Do−D)/2）、位置は姿図の寸法記入値（区間上端 600mm・ピッチ 1000mm・区間下端 400mm）。
    節部の平坦長のみ姿図実測による推定値で、**図示専用**（断面耐力・自重・支持力には不使用）
- **既製杭 節杭の製品ライブラリを追加**: ジャパンパイル JP-NPH85/105/123 パイル
  (プレストレスト高強度コンクリート節杭) のカタログ全表を収録
  - `pile_library_NodularPile.csv` 292 行 (呼び名 17 種 × コンクリート強度 3 種 × 種類 A〜CH)
    — 節部径 Do・軸部径 D・肉厚・PC鋼棒・PCD・Ao・Ae・Io・Ie・Ze・σce・許容軸力・
    Mal/Mas/Mc/Mu・Qal/Qas/Qu・標準質量・設計諸定数
  - `pile_library_NodularPile_head.csv` 25 行 — 拡頭中間径/拡頭タイプの Do/D/Dt/Lt/質量増分
  - メーカー横断の汎用スキーマ (`Maker`/`Series`/`Shape` 列) で、他社の節杭も同じ CSV に追加可能
  - 断面性能はすべて**軸部**基準の中空円形断面であることを確認済み。既製 PHC 杭の
    断面計算ロジックがそのまま流用できる
  - 抽出は PDF 罫線ベースの自動化 (`tools/pile-catalog/`)。全 292 行を
    `Ao=π/4(D²−di²)` 等の理論式と突合するテストを追加 (最大相対差 0.09%)
  - 検算によりカタログ側の誤植を検出: φ700-600/φ800-600 の Fc123・AH 種の Ze が
    直上行と同じ値で印字されている。印字値は `Ze` に忠実に残し、正しい値を `ZeFromIe`
    列と `Note` 列で提供する
  - 節部の形状寸法 (節部長さ・テーパー長) はカタログに寸法記入が無いため、姿図の
    ベクタ実測 (テーパー 45°) による推定値を `Estimated*` プロパティとして分離

- **PRC節杭 を杭断面として選択可能に**: 既製コンクリート杭の断面タイプに
  `PRC節杭` と `PRC節杭(PHC部)` を追加 (ジャパンパイル JP-NPRC105 パイル)
  - 1 本の PRC節杭 は杭頭側の **PRC部**(PC鋼棒 + 異形棒鋼) と、それより下の
    **PHC部**(PC鋼棒のみ) の 2 断面から成るため、場所打ち鋼管コンクリート杭の
    「鋼管コンクリート部 / 鉄筋コンクリート部」と同じ思想で別々の断面タイプとして区間に割り当てる
  - 断面耐力は軸部基準でストレート杭と一致するため、断面計算クラスは新設せず
    PRC部 → `PRCSection` / PHC部 → `PHCSection` をそのまま使う。同一性はテストで固定
  - 自重・節形状・杭頭タイプ (拡頭) の扱いは PHC節杭 と同じ。拡頭形状一覧は NPRC 専用の
    CSV を参照する (NPH と呼び名が同じでも用意された拡頭径が異なるため)
  - `pile_library_NodularPrcPile.csv` 232 行 (呼び名 17 種 × 種類 Ⅰ〜Ⅷ)、
    `pile_library_NodularPrcPile_head.csv` 25 行
    — PRC部/PHC部 の Ae・Ie・Ze・σce・耐力、せん断補強筋 (標準型 490/785・高せん断型 785)、
    せん断スパン比 1.0/1.5/2.0 別の Qas/Qu、異形棒鋼の諸定数、標準質量
  - 全 232 行を理論式と突合するテストを追加 (換算比 np = Ep/Ec = 5.0、
    nr = Er/Ec = 205000/40000 = 5.125。最大相対差 0.13%)
  - 検算によりカタログ側の誤植を検出: φ700 (t=100) の全 18 行の断面 1 次モーメント `So` が
    `18 617×10³` と印字されているが `(D³−di³)/12 = 18 167×10³` が正しい (桁の入れ替わり)。
    印字値は `So` に忠実に残し、正しい値を `SoFromSection` 列と `Note` 列で提供する

- **BF.S パイル (頭部厚型節付き杭) を杭断面として選択可能に**: 既製コンクリート杭の断面タイプに
  `BF.S頭部軸部` と `BF.S先端軸部` を追加 (三谷セキサン BF.S105 / BF.S123 パイル)
  - この製品は 1 本の杭が**外径の違う 2 つの軸部**から成る (頭部軸部 Dt/Tt と先端軸部 D0/T)。
    内径は両軸部で完全に一致し、頭部だけが外側に厚い。PC 鋼棒は本数・断面積・配筋径とも共通
  - 断面耐力はどちらも PC 鋼棒のみの中空断面なので `PHCSection` をそのまま使う
  - `pile_library_BfsPile.csv` 54 行 (Fc 105/123 × 呼び名 9 種 × 種類 A2/B2/C2)
  - **カタログに書かれていない量を 2 つ補っている**
    - 配筋径 PCD: カタログに印字が無いため頭部軸部の Ie から逆算
      (同一呼び名なら Fc・種類によらず一致し 5mm 丸めに乗ることを確認)
    - 先端軸部の断面諸元と σce: **カタログの標準性能表は頭部軸部の値しか載せていない**ため、
      有効プレストレス力 σce·Ae が両軸部で共通という前提で算定。
      結果が JIS A 5373 の A/B/C 種の規定値 (4 / 8 / 10 N/mm²) に ±5% で乗ることを確認しており、
      これが PCD 逆算に対する独立した裏付けになっている
  - **先端軸部の耐力 (Mcr / Mu / Qas 等) はカタログに存在しない**ため、アプリの断面計算の値を
    そのまま設計値として用いる。その妥当性の根拠として、頭部軸部では同じ計算値がカタログの Mu と
    全 27 製品で +2〜7% に収まることをテストで固定
  - 節の寸法はカタログ標準構造図に寸法記入がある (テーパー 25(50) / 節部 75(100) / 節ピッチ 1000 /
    先端 500)。節杭 (JP-NPH / JP-NPRC) のような推定値ではないため、`CatalogNodeFlatLength` を
    新設してカタログ値と推定値を区別した。テーパーは 45° で、節の出寸法は軸部径で決まるので
    頭部軸部と先端軸部で節の見え方が変わる
  - **このカタログには標準質量表が無い**ので、自重は軸部体積による (節の分は未計上)。
    節杭 (カタログ標準質量を使用) とは扱いが異なる
  - 頭部軸部そのものが太いため「拡頭」の設定は無く、杭頭タイプは常に標準タイプ

- **メーカー製 Fc105 ストレート PHC 杭を製品ライブラリに追加**: 三谷セキサン MS-hi105 パイル
  (`pile_library_PHC_MSHI105.csv` 94 行 = 径 12 種 × 肉厚仕様 3 種 × 種類 A/B/B2/C/C2)
  - ストレート杭で断面の挙動は既存の `PHC杭` と完全に同じなので、**断面タイプは増やさず
    PHC杭 の製品一覧に連結**した (節杭のように形状・自重が違うものだけ断面タイプを分ける方針)
  - 既存の JIS 汎用ライブラリとは別物で、置き換えではなく純粋な追加
    - 肉厚仕様が 標準/特厚 の 2 段階 → **標準型/厚型/特厚型 の 3 段階**
    - 同径でも PCD・PC鋼棒断面積が異なる (φ400 で PCD 330 → 335 など)
    - σce は 標準型・特厚型 が JIS の種別規定値ちょうど、**厚型のみ**が下がる
      (標準型と同じ PC 鋼棒のまま肉厚が増えるため)
  - PCD はカタログに印字が無いため Ie から逆算。径ごとに一意で 5mm 刻みに乗ることを確認
  - カタログの破壊モーメント Mu と、アプリの断面計算による安全限界モーメントが
    全 94 製品で同じ幅に収まることをテストで固定
  - 抽出は `tools/pile-catalog/build_pmc.py`。**この表だけ行の区切りが線ではなく背景の
    塗り矩形の縁**で描かれており罫線ベース (`extract.py`) が使えないため、行を等ピッチの
    グリッドとして再構成し、縦結合は「文字が結合範囲の中央に置かれる」ことから span を厳密に解いている

- **メーカー製 Fc105 SC杭を製品ライブラリに追加**: 三谷セキサン Hi-SC105 パイル
  (`pile_library_SC_HISC105.csv` 579 行 = 径 13 種 × 肉厚仕様 3 種 × 鋼管厚 6〜25mm を 1mm 刻み)
  - MS-hi105 と同じく**断面タイプは増やさず SC杭 の製品一覧に連結**した
  - 既存の JIS 汎用ライブラリとは別物で、置き換えではなく追加
    - 鋼管厚が代表値 (4.5/6/9/12/…) → **1mm 刻み**
    - 杭径が φ1200 まで → **φ1500 まで**
    - 鋼管ヤング係数 200,000 → **205,000** (SKK490 / STK490、降伏点 325)
  - **腐食代 1mm の表を腐食モデルの外部検証データとして取り込んだ**。カタログは腐食代 0mm と
    1mm の両方を載せており、製品としては 0mm 側を使い、1mm 側は CSV の `Corr1*` 列に併記して
    「本プログラムが `CorrosionDepth=1` で出す腐食後の鋼管断面積・曲げ剛性」と突合している。
    これまでのカタログには無かった検証手段
  - 検算によりカタログ側の誤植を検出: 腐食代1mm 表の φ1200 特厚型 の `Ao` が `Ac+As` と
    80cm² 食い違う (縦結合のため 17 行)。`Ac+As` へ置き換え `Corr1Note` に注記
  - カタログの `Ie` は鋼管が厚いほど厳密式より小さい (最大 0.22%、薄肉近似と見られる) ことも
    テストで固定。`Ao`/`Ac`/`As`/`Ae` は印字の丸め以内で一致する

### 🎨 UI・UX

- **諸元表に鉄筋規格の不一致を知らせる注記を追加**: 鋼管規格と同じく、製品ライブラリの選択では
  鉄筋規格は切り替わらない (本数・径・配筋径だけが転記される)。製品の主筋降伏点と選択中の規格の
  降伏点が食い違う場合に諸元表で知らせる (DAM105 は SD345、既定は SD390)
- **諸元表に鋼管規格の不一致を知らせる注記を追加**: 製品ライブラリの選択では鋼管規格は
  切り替わらない (杭径・鋼管厚だけが転記される) ため、製品の鋼管降伏点に対して選択中の
  規格の基準強度が明らかに低い場合 (SKK490 の製品に SKK400 を選んだ等) に諸元表で知らせる。
  基準強度と降伏点は元々別の量なのでわずかな差では出さない

- **メーカー製 Fc105 PRC杭を製品ライブラリに追加**: 三谷セキサン DAM105 パイル
  (`pile_library_PRC_DAM105.csv` 218 行 = 径 15 種 × 肉厚仕様 3 種 × 種類 A-D13〜A-D35)
  - MS-hi105 / Hi-SC105 と同じく**断面タイプは増やさず PRC杭 の製品一覧に連結**した
  - 既存の JIS 汎用ライブラリとは別物で、置き換えではなく追加
    - 杭径 φ300〜φ1000 → **φ1500 まで**、主筋 D13〜D29 → **D35 まで**
    - 肉厚仕様 (標準型/厚型/特厚型) を持つ
  - PC鋼棒・異形鉄筋とも E = 200,000 なので**換算比は n = 5.0 の 1 本**
    (JP-NPRC は異形棒鋼が 205,000 で 2 本立てだった)
  - **配筋径は 2 円モデルで逆算**。カタログに印字が無く、PC鋼棒と異形鉄筋の 2 群があるため
    Ie 1 本では決まらないので、同じ断面の種類 (主筋径) 違いを使い
    「PC鋼棒の配筋径」と「主筋のかぶり」を最小二乗で解いた。残差 RMS は全 35 断面で 0.11% 以内
  - **せん断耐力はカタログ自身が「シアスパン比 a=1.0 の参考値」と注記**しているため、
    設計値としては取り込まず参照列に置いた
  - 検算によりカタログ側の誤植を検出: φ400 厚型 A-D16 の主筋断面積が `13.39cm²` と
    印字されているが 7-D16 = `13.90cm²` が正しい。正しい値へ置き換え `AsNote` に注記

- **地盤 (p-y) 非線形性を 3 段階から選択可能に**: 荷重ケースごとに
  「線形 (kh0 固定)」/「kh 低減のみ」/「kh 低減 + py 頭打ち」を選択
  - 従来の `IsSoilNonLinear` (bool) は載荷ステップ数の決定にしか使われておらず、
    チェックを外しても地盤ばねは常に完全非線形 (kh 低減 + py 頭打ち) で解かれていた。
    今回 `SoilNonlinearityMode` (enum) を剛性組立 (`PrepareKmat`) まで配線し、選択が実際に効くようにした
  - 「線形」の kh は kh0 の定義 (基準変位 y0 = 1cm における値) をそのまま用いる
  - py = 0 となる深さ (砂質土で σz' = 0 等) は従来どおり全モードで反力ゼロ
  - 荷重条件ウィンドウはチェックボックス → 3 択 ComboBox に変更。計算書の入力条件表にも段階名と凡例を出力
  - **既存ファイルの互換**: 旧 `IsSoilNonLinear` = true → 「kh 低減 + py 頭打ち」、
    false → 「線形」に移行する。false だったケースは従来 (実質は非線形で計算されていた) と
    結果が変わるため、該当ファイルは再解析のうえ確認が必要

### 🐛 修正

- **沈下解析が周面抵抗の考慮チェックを無視していた問題** を修正
  - 沈下ウィンドウの「押込み/引抜き周面抵抗考慮」チェックボックスは杭区間
    (`PileCircumVertical`) 側のフラグを編集するが、沈下解析だけが土層 (`GroundLayer`) 側を
    読んでいた。同じチェックボックス操作で支持力表 (Rfu/Rtu/Rtr/Rty) は反応するのに
    沈下解析が無反応、という食い違いが生じていた
  - 参照元を杭区間側に統一（支持力表・水平解析の鉛直ばねと同じ出所）。
    チェックを操作していたモデルでは沈下量・引抜き検討の結果が変わる
- **杭先端沈下曲線パラメータ α・n が常に最後の杭体の値だった問題** を修正
  - `InputModel.PileBodies[^1]` と最終要素固定で参照していたため、杭体が複数あると
    別の杭体の α・n で Rp-Sp 曲線が計算されていた（同じ式に渡す先端径 Dp は
    正しい杭体から取れていたため、両者が食い違っていた）
  - 解析対象の `SoilPile.PileBodyInput` から取るよう統一（沈下解析・水平解析の鉛直ばね 7 箇所）。
    杭体が 2 つ以上あり α・n が杭体ごとに異なるモデルでは沈下量が変わる
- **降伏後の py 頭打ち精度を 10 倍改善** (post-yield 接線 2% → 0.2%)
  - 降伏境界の接線不連続を smoothstep (|y|/yy ∈ [1, 1.5]) で解消したうえで勾配を縮小したため、
    収束性は悪化せず改善。回帰スナップショットで Example10 の L2 反復数が 344 → 181 に半減
  - p の py 超過量は |y| = 10·yy でも 0.9% 以内
- **p-y 曲線の表示と解析の不一致を解消**: 表示用 `GetP` の `min(kh·y, py)` ハードクランプを廃止し、
  FEM 本体と同一式に統一。グラフ・MGT 出力・杭要素分割ダイアログと解析結果が厳密に一致する

- **2 回目の docx 出力ボタンで StackOverflowException**（プロセス即死）を根本修正 (`62bead3`)
  - RadioButton ペアの「正値 TwoWay + 反転 TwoWay」2 ライター構成が、GroupName の static
    管理と共有 VM + ウィンドウ再生成の組合せで相互解除の無限ループを起こしていた
  - BoolEqualsConverter 新設（チェック時のみ書き戻し、アンチェックは Binding.DoNothing）に統一。
    基本設定 9 ペア・解析実行ウィンドウ 3 ペアの同型潜在危険も予防修正
- **docx M-θ 図のマーカーが曲線に乗らない問題** を修正 (`5ae9b4b`)
  - 方向ロック + ヒステリシス下ではベクトルノルム (|Δθ|, |M|) は単調載荷曲線に乗らない。
    アプリ内グラフと同じ「ピーク投影値 (ThetaProjMax, curve(ThetaProjMax))」方式に統一
- **Q-N 図の低減後曲線が β2 切替閾値 (σ0=(1/3)ξFc) で斜めに落ちる問題** を修正 (`5ae9b4b`)
  - 閾値 N に切替前後の複製 2 点を挿入し垂直な段差として描画（アプリ画面・docx 両方に反映）

## [1.0.18-beta] — 2026-08-11

### ➕ 新機能

- **2025年版 構造関係技術基準解説書 付録1-3 準拠オプション**: 告示1113(第8) の許容圧縮・
  許容せん断への切替、安全限界曲げ/せん断を耐震設計指針(案)5.4.1 準拠に（場所打ちRC=e関数法、
  鋼管巻き=鋼管トリリニア+指針せん断式）。マスターチェック・競合グレーアウト・
  限界状態呼称の長期/短期許容切替を含む (`a779aa4`, `7bab9c1`, `d768aaf`, `89de396`)
- **材料モデル化オプション基盤 (ConcreteModelOptions)**: Ec 算定 ξ=1.0、圧縮 0.85Fc、
  鉄筋/鋼管 1.1F バイリニア等を基本設定から選択。kh0 の土層単位手入力オーバーライド、
  諸元表の腐食考慮/非考慮併記、EA への鋼管項 (Es·As) 追加も同梱 (`a779aa4`)
- **ファイバーモデル M-φ** を 3 段階で導入: 比較表示 → 解析用オプション → 全コンクリート系杭
  （場所打ちRC/場所打ち鋼管コンクリート/PHC/PRC/SC/充填鋼管部）へ拡大 (`4437cb2`, `309a5f9`, `abac988`)
- **docx 計算書の説明充実 Phase1〜4**: 「計算条件・仮定」章の新設（単位系・符号規約・
  材料オプション選択状態表・解析時オプションとの不一致警告、計算書レベル非依存で常時出力）、
  「基礎部材の強度と変形性能」節の記載誤り是正、見出し構造・重複出力の整理、
  導入文・記号定義・β1β2 一覧表の追加 (`c3c4177`, `29a942f`, `0c35b73`, `86e1b6d`)
- **サイレント計算フォールバックの可視化 (CalcFallbackTracker)**: 例外→既定値 0 や線形 M-φ
  代替を発生源別にカウントし、水平解析完了ダイアログに警告表示 (`c202690`)

### 🎨 UI・UX

- 基本設定を「基礎部材の強度と変形性能 vs 代替」の既定/代替ラジオペア（全 9 項目）に再設計。
  具体式併記・項目別ヘルプリンク付き。MaterialOptionRadioPair コントロールに
  テンプレート化 (`5ba658b`, `e1461cc`)
- M-φ/M-θ グラフの凡例爆発を解消（曲線凡例廃止、軸力範囲はキャプションへ）(`4059cba`)
- 計算書出力ウィンドウに全選択/全解除ボタンと出力前検証を追加 (`25004d3`)
- 基本設定ウィンドウの縦サイズをノートPCに適合、既製杭断面タイプ切替時に同径断面を
  既定選択（旧タイプ諸元のまま計算が続く問題の修正）(`8efe2bd`)

### 🐛 修正

- 計算層の正確性バグ修正: 割線反復の無限ループ防止、場所打ち鋼管コンクリート杭 安全限界
  せん断の面積式誤り（非安全側）、せん断オプション変更が反映されない N-Q キャッシュ破棄漏れ、
  Tendons の Fpu 誤代入ほか (`0c96c31`)
- UI オートメーション（ナレーター等）とタイトル変更の競合による Win32Exception クラッシュを
  修正（WPF 既知バグ dotnet/wpf#4786 対策）(`8698e60`)
- docx 出力の全面点検: 数式・単位・記号の表記ゆれ、杭頭径、材料オプションの式への連動、
  計算書レベル仕分け誤分類、杭配置図バインディング (`fc6cd79`, `63dac58`, `0b5914e`, `243d32d`)

### 🏗️ 内部構造

- God class の partial 物理分割（挙動不変）: PileSection2 / HorizontalCalculationViewModel
  (6,843→3,014行+4) / MainWindowViewModel (7,641→4,090行+4) / WordDocument (5,908→1,674行+4) /
  MainWindow.CanvasResults / GraphViewModel / DocxOutputViewModel (`17966dd`, `ca68a1e`, `c900a58`, `243d32d`)
- 全体コードレビュー対応: 杭種マジック文字列の定数化、MaterialLaw enum 化、収束許容値・
  指針定数の named constant 化、M-φ 単位変換の一元化、計算コアの nullable 警告解消、
  ログ統一 (`13270a4`, `d5e400c`, `e7c5a15`, `7797455`, `4403b89`)
- PHC/PRC の M-φ Newton 反復を共通ドライバに統合、MphiCurveResolver 新設、
  M-φ キャッシュ上限追加 (`e1461cc`, `17966dd`)
- TargetFramework を net8.0-windows10.0.19041.0 に変更、NuGet 更新
  (SkiaSharp 3→4 ほか) (`6de1f60`, `eb4e710`)

### 🧪 テスト

- 文献検証テスト第1弾（基礎指針'19 計算例1/2）、耐力算定回帰テスト、グラフ描画・
  docx 2回連続生成スモークテスト (`68b58f5`, `3aa388f`, `a7ff3a9`, `54c6c85`)

### 📚 ドキュメント

- Help「設計実務上の扱いについて」節新設（告示1113(第8) との差異注記）、
  β1/β2・e関数法記述の整合修正 (`6b97436`, `3abb6be`)

## [1.0.17-beta] — 2026-07-06

### ➕ 新機能

- コンクリートのヤング係数 Ec 算定で ξ(=Gsi)=1.0 とするオプションを追加
  （強度側は実 Gsi のまま、Ec の算定式のみ）(`eb98306`)

### 🔧 解析・FEM

- 圧縮側折れ点の低減オプションを 0.85·Gsi·Fc → 0.85·Fc に変更（Gsi は乗じない）(`eb98306`)

## [1.0.16-beta] — 2026-07-03

### 🐛 修正

- 鉄筋 1.1F 完全バイリニア型オプションを安全限界のみに限定（損傷限界・使用限界は
  基準 σy を維持。例: SD490 損傷限界 490N/mm² のまま、安全限界のみ 539N/mm²）(`e3720d4`)

## [1.0.15-beta] — 2026-07-03

### ➕ 新機能

- 基本設定に材料モデル化オプション追加（安全限界 NM と M-φ→解析の両方に適用）:
  バイリニアコンクリートの引張 0 / 圧縮低減、鉄筋・鋼管の 1.1F 完全バイリニア (`dabd9e1`)
- 杭要素分割: 基準水平地盤反力係数 kh0 の土層単位手入力オーバーライド
  （右クリックで自動計算に戻す）(`dabd9e1`)

### 🔧 解析・FEM

- 場所打ち鋼管コンクリート杭 圧縮側軸力制限 σ0e=0.4ξFc の換算断面積を文献整合の
  Ae = Ac + (n−1)Ag + nAs に統一、鋼管板厚の負許容差 1mm を廃止し腐食のみに (`dabd9e1`)

### 🎨 UI・UX

- 杭断面諸元: 鋼管系 As/A0/W/EA/EI を腐食考慮・非考慮の 2 行表示、腐食代行を追加。
  EA に鋼管項 Es·As を追加（鉛直解析に影響）。計算書杭径を公称外径表示に修正 (`dabd9e1`)

## [1.0.14-beta] — 2026-06-12

### 🎨 UI・UX

- ヘルプウィンドウの Topmost を撤去（他アプリ操作を阻害しない）(`d374f9a`)
- Ctrl+A（すべて選択）と Ctrl+Shift+A（すべてアクティブ）をスワップ (`d374f9a`)

### 🛡️ 安定化

- グローバル未捕捉例外ハンドラ強化: UI スレッド例外で続行/終了を選択可能に、
  良性例外フィルタ拡充（IME/WebView2/キャンセル）(`d374f9a`)
- 解析前プリフライトに入力警告セクション追加（ΔZc≤0、Es=0/Cu=0/N=0 等を黄色枠表示）(`d374f9a`)

## [1.0.13-beta] — 2026-06-12

### 🎨 UI・UX

- 地盤ウィンドウ N 値分布グラフに表示系列切替（土層平均 N 値の階段グラフ / 土質点 N 値の折れ線）(`eac76bd`)
- リボン「土層」に「平均N値」3D 表示オプション追加 (`eac76bd`)

### 🐛 修正

- 変形係数 Esk 列ヘッダーの単位誤り（kN/m³ → kN/m²）を修正 (`eac76bd`)

## [1.0.12-beta] — 2026-06-02

### 🎨 UI・UX

- クリップボード操作の安全化: リトライ付き ClipboardHelper に全 SetText/SetDataObject を統一
  （CLIPBRD_E_CANT_OPEN 対策）(`747e244`)
- 杭姿図の右クリック「画像コピー」追加（PileBodyWindow / SettlementWindow）(`747e244`)
- 解析後の表示改善: 沈下解析完了時に VL ケースへ自動切替、水平解析 OK 時に解析済み先頭
  ケースを選択、マップ選択時にトップビューへ自動切替 (`747e244`)

### 🐛 修正

- 接合点 M/Q マップ: 基礎梁未設定時に杭頭 i 端力を接合点標高へ剛オフセット移送
  （Mx_C = Mx_H + ΔZc·Fy 等、力学整合）(`747e244`)
- SoilPile.SettleRpu を Dp 基準に変更（支持力検討 Rpu は D 基準のまま）(`747e244`)

## [1.0.11-beta] — 2026-05-29

2026-05-17〜19 セッションの成果を含む（詳細は旧 CHANGELOG 記載分を集約）。

### 🔧 解析・FEM

- VL 単独ケースで X 反力が出る問題を修正（強制変位の VL 適用を早期 return）
- P-S 非線形ばねを沈下解析と同じ物理関数で評価する PileVerticalSoilSpringModel を導入、
  杭体自重の Fz 注入を含む
- 計算例9 を X 軸対称化、キャプリング 2 例題に地盤変位「考慮しない」を反映

### 🎨 UI・UX

- キーボード操作の全 18 ウィンドウ統一（Enter/Esc/Alt+X/Tab/Space、共通ボタンスタイル化）
- ファイルメニューに Ctrl+N/O/S/Shift+S、ステータスバーに「⌨ ショートカット」ボタン
- EnhancedDataGrid: 未編集セルの 1 打目入力、ペースト後フォーカス復帰、一括ペースト高速化
  （IsBulkEditing）、Delete クリア、一括 Undo (`6ef2fdf`)
- 解析結果 RZ ダイアグラムのバブル表示、配置 DataGrid の ComboBox 値消失/全杭非表示時の
  反力表示問題を修正

### ➕ 新機能

- コマンドライン引数によるファイル起動（`PileDesign.exe project.json` / `--open`）
- エクスプローラからの .json ドラッグ&ドロップ読込
- 保存オプション: 手動保存・自動保存それぞれで解析結果を含めるか独立選択 (`6ef2fdf`)

### 🧪 テスト

- 収束リグレッションテスト基盤（4 例題スナップショット、退化判定付き）、並列実行決定性テスト
- GitHub Actions CI（push/PR で build + 全テスト自動実行）

### 🗑️ 削除

- PileDesignCore / PileDesign.Cli / PileDesign.Mcp プロトタイプ削除（.sln 未登録の古いフォーク、
  計 111 ファイル）

## [1.0.8-beta] — 2026-05-13

### ➕ 新機能

- **キャプリングパイル工法**（既製コンクリート杭/鋼管杭の杭頭半剛接合）を実装:
  PC リング・引張定着筋ライブラリ、バイリニア M-θ (θu=0.03rad) (`47838d2`, `b44775c`)
- **鋼管杭 + 鉄筋定着工法**を実装: コンクリート充填鋼管部/鋼管部サブ分類、2 段 RC 断面
  NM 評価、接合部弾性回転剛性 Kθ (`e6cb4f5`, `88a3efd`)
- 結果ダッシュボード + コマンドパレット (Ctrl+Shift+P) (`5728626`)
- 編集履歴パネル + 解析実行前提条件チェック + DataGrid セル編集の Undo 記録 (`d70ef4f`, `26e63df`)
- ヘルプチャット（help.html の bigram 検索）(`47838d2`)

### ⚡ 性能

- 水平解析 76% 高速化: 周期的 K 再構築（MNR プラトー検知）でベンチマーク 2448s→588s (`7b8bd2f`)
- docx 出力 5× 高速化（13s→2.6s）: 結果検索の Dictionary キャッシュ + PNG 並列生成 (`7b8bd2f`)
- Undo DeepCopy 8-9× 高速化（220ms→25ms）: デバウンス + QuickClone (`12cacee`)
- 発行構成を x86→x64 に変更（大規模モデルの OutOfMemoryException 対策）(`7b8bd2f`)

### 🎨 UI・UX

- **UI 全面リニューアル**: Fluent.Ribbon 移行、Backstage ファイルメニュー（計算例 11 例題）、
  AvalonDock テーマ統一、アイコン 158 点を SVG から再生成 (`83f07f1`, `bfc3540`)
- 軸力タブに「絶対 ⇔ 変動」モード切替 + L1/L2±VL 一括変換 (`12cacee`, `7b8bd2f`)
- 杭姿図に qu (=2Cu) 階段グラフ・土層 N 値階段図 (`83f07f1`, `dd95217`)

### 🐛 修正

- M-φ 軸力の単位バグ（1/1000 過小評価）+ 単位回帰テスト、M-θ をケース別スナップショット化し
  軸力源を地震時軸力に統一 (`c738eb4`, `41357b6`)
- 引張軸力時の杭頭軸剛性解放を case-local model に適用 (`12cacee`)

### 🧪 テスト

- キャプリング単体テスト 30 件ほか (`a41c074`)

### 📚 ドキュメント

- 計算例の軸力データを文献値に整備、help.html にキャプリング・F.T.Pile・鉄筋定着の各章 (`83f07f1`, `47838d2`)

## [1.0.5-beta] — 2026-05-01

### 🔧 解析・FEM

- 非線形収束整合性修正 v22〜v24: 土圧合力の共有ばね K 上書きバグ、降伏境界接線の連続化、
  杭地盤ばね 2D Jacobian、合成 M-φ 対角ブレンド、2 ステップ予測器 (`f92e9a2`)
- 収束改善 v25〜v28: 勾配情報付き line search、適応的初期 nStep、杭頭 M-θ
  方向ロック + ヒステリシスで 25 反復周期リミットサイクルを解決 (`3d42089`)
- 水平解析の堅牢性: 杭頭 kh=0 の三段階フォールバック、状態変化検知による K 強制再構築 (`1e38da1`)
- 地盤水平変位の第 3 算定法「**応答スペクトル法**」追加（MDOF 固有値解析 +
  Hardin-Drnevich 等価線形化、周期依存 Gs(T)）(`3807165`)
- 杭セクション損傷限界の L1/L2 分離 + 性能グレード A/S 切替 (`0146a2b`)
- Z 座標と絶対標高の分離設計（入力・解析は Z、docx は標高表示）(`78a5b15`)
- 群杭沈下拡張: 仮想最上層処理、Gs0/Es0/νs 列、PL 値自動計算 (`c2ca6ba`)

### ⚡ 性能

- 水平解析の**荷重ケース並列化** (E1〜E3c): AnaModel DeepCopy + case-local 隔離 +
  結果マージ、MDOP 既定 16 (`eedf2e7`, `2376fbe`)
- K 組立の並列化、ソルバーの Cholesky→LDL→LU→QR 段階フォールバック、
  Modified NR + Cholesky 因子キャッシュ (`7d74274`, `b8faa83`)

### 🎨 UI・UX

- 水平解析ウィンドウ刷新: 並列モニタ、ケース別ログタブ、解析サマリーレポート (`80a1c47`, `34a1963`)
- ログウィンドウの仮想化 + 部分コピー、GraphWindow 強化（土層背景色・純理論値モード等）(`f0e74b7`, `fcab338`)
- EnhancedDataGrid 改善: コピー TSV の行番号除去、塗り潰しペースト、
  12 インチ画面対応 (`26dc50d`, `9b2158f`, `db59463`)

### 🐛 修正

- 未捕捉例外ハンドラ + 緊急 AutoSave + Serilog 構造化ログ導入 (`1c421ec`, `9e31a32`)
- MainWindow の PropertyChanged 購読解除漏れ（メモリリーク）修正 (`e9aa05a`)

### 🧪 テスト

- FsCheck プロパティベーステスト導入、v22〜v26 収束修正の最小再現テスト、
  機能テスト計 300+ 件 (`22b66dd`, `2711581`, `ae0267c`)

## [1.0.4-beta] — 2026-04-21

### 🎨 UI・UX

- 解析結果キャンバス描画の拡充（値表示・カテゴリ別コンバータ・梁 IJ 入替）(`120d64d`)

### 🐛 修正

- 「杭頭」値表示フィルタのバグ修正（IsPileTop と IsPileHeadElement の分離）(`001650a`)

### 🧪 テスト

- ViewModel を [ObservableProperty] source generator に段階移行（Phase A〜F、18 ファイル）(`58a083d`)

### 📚 ドキュメント

- 文献検証ページ verification.html を大幅拡充（基礎指針 2019 図表画像を多数追加）(`120d64d`)

## [1.0.3-beta] — 2026-04-18

### 🐛 修正

- ScottPlot グラフコピーの BitmapMetadata 例外、DataGrid 仮想化対応コピー、
  変形後形状の剛体放射線描画を修正 (`6073c80`)

### 🏗️ 内部構造

- 大規模リファクタ: WordDocument（7,755→5,504 行）・MgtExporter の分割、DeepCopyUtil 新設、
  Dof 列挙子による型安全化、保存プロトコルの ApplyPostLoadProtocol 集約、
  テスト 96 件追加（160→275）(`7df47d7`)

## [1.0.2-beta] — 2026-04-13

### ➕ 新機能

- 解析結果を含むファイル保存/復元に対応（読込時 ComboBox 空白バグも修正）(`41d97e4`)
- 沈下量の値表示（ON/OFF・小数桁）、沈下曲線の凡例切替、解析結果タブのテキストサイズ調整 (`41d97e4`)

### 📚 ドキュメント

- docx に基礎梁考慮鉛直解析結果テーブルを追加 (`41d97e4`)

## [1.0.1-beta] — 2026-04-09

### 🎨 UI・UX

- UI スレッドブロッキング解消: 解析の async/await 化、MessageService 新設、
  成功系メッセージの Toast 化 (`a16c70a`)
- WelcomeDialog のスプラッシュ化、節点/杭の整列・ソートボタン、
  解析結果キャンバスの Hermite 曲線描画 (`939a402`, `4c864fd`, `9c881b6`)

### ➕ 新機能

- MGT エクスポートを MIDAS iGen v9.4.5 準拠に (`314b276`)
- 2D 伏図 DXF / 3D DXF・3dm エクスポート追加 (`9c7a31a`, `399687e`, `066fcfd`)

### 🐛 修正

- 要素分割済み状態の保護（編集時の確認ダイアログ）(`723be01`)

### 📚 ドキュメント

- Word 計算書出力を大幅拡充、help.html 拡充 (`c27aaad`)

## [1.0.0-beta] — 2026-04-06

Beta 公開準備。

### 🔧 解析・FEM

- 杭断面計算の広範改善: MN 曲線・M-φ・既製杭定着部・軸力制限、SKK490 基準強度修正
  (325→315)、数値積分分割数 100→200 (`2dda0aa`)
- 場所打ち鋼管コンクリート杭の水平解析・M-φ 単位変換・要素分割のバグ修正 (`4454209`)
- NR 収束性改善（長時間反復時の基準自動緩和）、杭頭モーメント不連続の根本修正
  （ペナルティ結合化）(`ffb34a9`, `0d12157`)
- EffectiveDepth を 0.8D → 基礎指針準拠の 0.9D に修正 (`5269504`)

### 🎨 UI・UX

- p-y グラフ・土圧合力ばねグラフを新規追加、解析結果テーブルに地盤反力・M-φ・M-θ (`60f2920`, `de116d0`)

### 🛡️ 安定化

- データ保護強化: 読込後 Undo クリア、JSON エラーの日本語表示、FormatVersion による
  上位バージョン拒否、入力バリデーション (`61e341f`)

## [1.0.0] — 2026-03-18

初期開発期（2025-11-08 の初回コミットから約 4 か月・60 commits）の総括。

### ➕ この時点までに実装された機能

- 杭基礎検討 WPF アプリの基盤: 杭配置・杭断面・地盤・荷重ケース入力、2D/3D キャンバス表示
- 鉛直・水平 FEM 解析エンジンと解析結果の図化・テーブル表示
- 基礎梁モデル化（Phase 1〜4）: データモデル → 偏心接合含むモデル化 → 入力 UI →
  3D/2D 可視化、基礎梁節点の自動割当 (`4546b4f`〜`8bd41aa`)
- Undo 機構の統一化 (`d3947c6`)、ズーム操作の軽量描画化 (`23e9284`)
