--- c:\Users\keisu\source\repos\PileDesign_0\Graphics_r1\ViewModels\MainWindowViewModel.cs.bak	2026-02-11 08:52:34.797143600 +0900
+++ c:\Users\keisu\source\repos\PileDesign_0\Graphics_r1\ViewModels\MainWindowViewModel.cs	2026-02-11 22:05:26.991236400 +0900
@@ -6,3 +6,2 @@
 using PileDesign.FEM;
-using PileDesign.Models;
 using PileDesign.Models.InputData;
@@ -14,3 +13,2 @@
 using System.Collections.ObjectModel;
-using System.IO;
 using System.Linq;
@@ -30,4 +28,2 @@
 using ToolkitRelayCommand = CommunityToolkit.Mvvm.Input.RelayCommand;
-using Graphics_r1.Constants;
-using System.Threading;
 
@@ -35,6 +31,28 @@
 {
+    /// <summary>
+    /// MainWindowViewModel (メインファイル)
+    ///
+    /// 責任範囲:
+    /// - ファイル操作（新規作成、開く、保存、エクスポート）
+    /// - コレクション管理（杭配置、通り心、荷重面、土層の追加・削除）
+    /// - ウィンドウ表示制御（各種ダイアログウィンドウの開閉）
+    /// - DataGrid編集イベント処理
+    /// - 解析実行制御（要素分割、解析実行前チェック）
+    /// - Undo/Redo機能
+    /// - UI更新制御（デバウンス処理を含む）
+    ///
+    /// その他のpartialクラス:
+    /// - MainWindowViewModel.Constructor.cs : プロパティ定義とコンストラクタ
+    /// - MainWindowViewModel.Examples.cs : 設計例集データ生成
+    /// - MainWindowViewModel.Improvements.cs : パフォーマンス最適化機能
+    /// - MainWindowViewModel.TreeView.cs : TreeView制御
+    /// - MainWindowViewModel.SettlementGridCache.cs : 沈下グリッドキャッシュ
+    /// - MainWindowViewModel.ConfirmDeleteAnalysisModel.cs : 解析モデル削除確認
+    /// </summary>
     public partial class MainWindowViewModel : ObservableObject
     {
-        // 追加: フィールド宣言の変更
         private readonly UndoManager _undoManager = new();
+        private readonly FileOperationService _fileOperationService;
+        private readonly PileLayoutService _pileLayoutService;
+        private readonly SettlementAnalysisService _settlementAnalysisService;
 
@@ -43,3 +61,3 @@
 
-        private void Debounce(ref System.Windows.Threading.DispatcherTimer? timer, int milliseconds, Action action)
+        private static void Debounce(ref System.Windows.Threading.DispatcherTimer? timer, int milliseconds, Action action)
         {
@@ -59,6 +77,5 @@
 
-
         /// <summary>
-        /// SoilPiles の生成をデバウンスでリクエストします。
-        /// ・ｽZ情報ﾔに包ｿｽ根入部ﾄばゑｿｽﾄゑｿｽ・ｽA・ｽﾅ鯉ｿｽﾌ呼び出根入部情報闔橸ｿｽﾔ鯉ｿｽ・ｽ1・ｽｾゑｿｽ情報s根入部ﾜゑｿｽ・ｽB
+        /// SoilPiles の生成をデバウンス付きでリクエストします。
+        /// 短時間に複数回呼ばれても、最後の呼び出しから一定時間後に1回だけ実行されます。
         /// </summary>
@@ -68,3 +85,3 @@
             _soilPilesGenerationPending = true;
-            Debounce(ref _generateSoilPilesDebounceTimer, TimingConstants.SOIL_PILES_DEBOUNCE_MS, () =>
+            Debounce(ref _generateSoilPilesDebounceTimer, 50, () =>
             {
@@ -77,6 +94,5 @@
         }
-
         /// <summary>
-        /// SoilPiles ・ｽﾌ撰ｿｽ情報ｦ搾ｿｽ・ｽﾉ趣ｿｽ・ｽs情報ﾜゑｿｽ・ｽi・ｽf・ｽo・ｽE情報X情報X・ｽL・ｽb・ｽv・ｽj・ｽB
-        /// 即時に実行する必要がある場合に使用します。
+        /// SoilPiles の生成を即座に実行します（デバウンスをスキップ）。
+        /// 明示的に即時実行が必要な場合に使用します。
         /// </summary>
@@ -84,3 +100,3 @@
         {
-            // 保留中のデバウンスタイマーをキャンセル
+            // 保留中のデバウンスをキャンセル
             _generateSoilPilesDebounceTimer?.Stop();
@@ -96,8 +112,8 @@
         private bool _updateWindowPending = false;
-
         /// <summary>
-        /// ウィンドウ更新をデバウンスでリクエストします。
-        /// ・ｽZ情報ﾔに包ｿｽ根入部ﾄばゑｿｽﾄゑｿｽ・ｽA・ｽﾅ鯉ｿｽﾌ呼び出根入部情報闔橸ｿｽﾔ鯉ｿｽ・ｽ1・ｽｾゑｿｽ情報s根入部ﾜゑｿｽ・ｽB
+        /// ウィンドウ更新をデバウンス付きでリクエストします。
+        /// 短時間に複数回呼ばれても、最後の呼び出しから一定時間後に1回だけ実行されます。
         /// </summary>
-       
+
+
         public void RequestUpdateWindow()
@@ -105,3 +121,3 @@
             _updateWindowPending = true;
-            Debounce(ref _updateWindowDebounceTimer, TimingConstants.WINDOW_UPDATE_DEBOUNCE_MS, () =>
+            Debounce(ref _updateWindowDebounceTimer, 30, () =>
             {
@@ -116,4 +132,4 @@
         /// <summary>
-        /// ・ｽE・ｽB情報h・ｽE・ｽX・ｽV・ｽｦ搾ｿｽ・ｽﾉ趣ｿｽ・ｽs情報ﾜゑｿｽ・ｽi・ｽf・ｽo・ｽE情報X情報X・ｽL・ｽb・ｽv・ｽj・ｽB
-        /// ダイアログを閉じた時など、即座更新が必要な場合に使用します。
+        /// ウィンドウ更新を即座に実行します（デバウンスをスキップ）。
+        /// ダイアログを閉じた後など、即時更新が必要な場合に使用します。
         /// </summary>
@@ -121,3 +137,3 @@
         {
-            // 保留中のデバウンスタイマーをキャンセル
+            // 保留中のデバウンスをキャンセル
             _updateWindowDebounceTimer?.Stop();
@@ -129,2 +145,155 @@
 
+        /// <summary>
+        /// UI更新を一元的に通知します。
+        /// ウィンドウ更新とTreeView更新を統一的に処理します。
+        /// </summary>
+        /// <param name="updateTree">TreeViewも更新するか（デフォルト: true）</param>
+        /// <param name="immediate">即座に実行するか（デフォルト: false、デバウンス付き）</param>
+        private void NotifyUIChanged(bool updateTree = true, bool immediate = false)
+        {
+            if (immediate)
+                UpdateWindowImmediate();
+            else
+                RequestUpdateWindow();
+
+            if (updateTree)
+                UpdateTreeView();
+        }
+
+        /// <summary>
+        /// DataGridセルエディット完了時の共通処理
+        /// バインディング更新とUI更新を一元的に処理します。
+        /// </summary>
+        /// <param name="e">DataGridセルエディットイベント引数</param>
+        /// <param name="customAction">追加のカスタム処理（オプション）</param>
+        /// <param name="updateTree">TreeViewも更新するか（デフォルト: true）</param>
+        /// <returns>Commitアクションの場合true、それ以外false</returns>
+        private bool HandleDataGridCellEditEnding(DataGridCellEditEndingEventArgs e, Action customAction = null, bool updateTree = true)
+        {
+            if (e.EditAction == DataGridEditAction.Commit)
+            {
+                // バインディングソースの更新
+                var binding = e.EditingElement.GetBindingExpression(TextBox.TextProperty);
+                binding?.UpdateSource();
+
+                // カスタム処理の実行
+                customAction?.Invoke();
+
+                // UI更新
+                NotifyUIChanged(updateTree);
+
+                return true;
+            }
+            return false;
+        }
+
+        /// <summary>
+        /// コレクションからアイテムを削除する共通処理
+        /// </summary>
+        /// <typeparam name="T">アイテムの型</typeparam>
+        /// <param name="sender">削除対象のアイテム</param>
+        /// <param name="collection">削除元のコレクション</param>
+        /// <param name="postDeleteAction">削除後のカスタム処理（オプション）</param>
+        /// <param name="saveUndo">Undo保存するか（デフォルト: false）</param>
+        /// <param name="updateTree">TreeViewも更新するか（デフォルト: false）</param>
+        /// <param name="immediate">即座に実行するか（デフォルト: false）</param>
+        /// <returns>削除に成功した場合true</returns>
+        private bool DeleteCollectionItem<T>(
+            object sender,
+            ObservableCollection<T> collection,
+            Action postDeleteAction = null,
+            bool saveUndo = false,
+            bool updateTree = false,
+            bool immediate = false)
+        {
+            if (sender is not T itemToDelete)
+                return false;
+
+            if (saveUndo)
+                TrySaveUndoSnapshotSafely();
+
+            collection.Remove(itemToDelete);
+
+            postDeleteAction?.Invoke();
+
+            NotifyUIChanged(updateTree, immediate);
+
+            return true;
+        }
+
+        /// <summary>
+        /// ダイアログウィンドウを開く共通処理（Undo保存付き）
+        /// </summary>
+        /// <typeparam name="TViewModel">ViewModelの型</typeparam>
+        /// <typeparam name="TWindow">Windowの型</typeparam>
+        /// <param name="postDialogAction">ダイアログ終了後のカスタム処理（オプション）</param>
+        private void OpenDialogWindowWithUndo<TViewModel, TWindow>(Action postDialogAction = null)
+            where TViewModel : ObservableObject
+            where TWindow : Window, new()
+        {
+            // Undoポイントを追加（読込前の状態を保存）
+            _undoManager.SaveState(CurrentInputModel.DeepCopy());
+
+            // ダイアログを開く
+            OpenDialogWindow<TViewModel, TWindow>(this);
+
+            // 追加処理の実行
+            postDialogAction?.Invoke();
+        }
+
+        /// <summary>
+        /// IsFrontPileフラグを選択されたアイテムに適用
+        /// </summary>
+        /// <param name="selectedItems">選択されたアイテムのリスト</param>
+        /// <param name="isApplicable">各レベルの適用可否（4要素の配列）</param>
+        /// <param name="values">各レベルの値（4要素の配列）</param>
+        private static void ApplyIsFrontPileFlags(
+            IEnumerable<PileLayoutDataItem> selectedItems,
+            bool[] isApplicable,
+            bool[] values)
+        {
+            for (int i = 0; i < 4; i++)
+            {
+                if (isApplicable[i])
+                {
+                    foreach (var item in selectedItems)
+                    {
+                        item.IsFrontPiles[i] = values[i];
+                    }
+                }
+            }
+        }
+
+        /// <summary>
+        /// 粒度区分からポアソン比を決定
+        /// </summary>
+        /// <param name="granularityClass">粒度区分</param>
+        /// <returns>ポアソン比</returns>
+        private static double DeterminePoissonsRatio(string granularityClass)
+        {
+            return granularityClass switch
+            {
+                "粘性土" => 0.4,
+                "砂質土" or "礫質土" => 0.3,
+                _ => 0.33
+            };
+        }
+
+        /// <summary>
+        /// SettlementSoilLayerのThickness（層厚）を計算
+        /// </summary>
+        /// <param name="layers">沈下土層のリスト</param>
+        /// <param name="loadingPlaneAltitude">載荷面標高</param>
+        private static void CalculateLayerThicknesses(
+            ObservableCollection<SettlementSoilLayer> layers,
+            double loadingPlaneAltitude)
+        {
+            for (int i = 0; i < layers.Count; i++)
+            {
+                layers[i].Thickness = i == 0
+                    ? loadingPlaneAltitude - layers[i].BottomAltitude
+                    : layers[i - 1].BottomAltitude - layers[i].BottomAltitude;
+            }
+        }
+
         // JsonSerializerOptions をキャッシュ
@@ -150,8 +319,8 @@
 
-        // 追加: コマンド更新最適化用
+        // 追加: コマンド更新一括ヘルパ
         private void RaiseAllCommandsCanExecute()
         {
-            // 情報t情報N・ｽV根入部・ｽ・ｽ "Command" ・ｽﾅ終・ｽ・ｽ驍ｷ・ｽﾗてのコ・ｽ}情報h・ｽv情報p・ｽe・ｽB・ｽ・ｽ唐・ｽ・ｽA
+            // リフレクションで "Command" で終わるすべてのコマンドプロパティを列挙し、
             // CommunityToolkit の IRelayCommand は NotifyCanExecuteChanged() を呼び、
-            // 旧来 RelayCommand には RaiseCanExecuteChanged() を呼び出す。
+            // 自前 RelayCommand 等は RaiseCanExecuteChanged() を呼び出す。
             var props = this.GetType()
@@ -165,6 +334,5 @@
                 {
-                    var cmdObj = p.GetValue(this) as ICommand;
-                    if (cmdObj == null) continue;
+                    if (p.GetValue(this) is not ICommand cmdObj) continue;
 
-                    // CommunityToolkit の IRelayCommand を優先して試す
+                    // CommunityToolkit の IRelayCommand を優先して扱う
                     if (cmdObj is CommunityToolkit.Mvvm.Input.IRelayCommand toolkitCmd)
@@ -175,3 +343,3 @@
 
-                    // 旧来 RelayCommand の RaiseCanExecuteChanged() を探して呼び出す
+                    // 自前 RelayCommand の RaiseCanExecuteChanged() を探して呼び出す
                     var raiseMethod = cmdObj.GetType().GetMethod("RaiseCanExecuteChanged", System.Reflection.BindingFlags.Instance | System.Reflection.BindingFlags.Public | System.Reflection.BindingFlags.NonPublic);
@@ -183,3 +351,3 @@
 
-                    // 念のため他の NotifyCanExecuteChanged メソッドがあれば試す（稀なケース）
+                    // 互換性のため NotifyCanExecuteChanged メソッドも試す（まれなケース）
                     var notifyMethod = cmdObj.GetType().GetMethod("NotifyCanExecuteChanged", System.Reflection.BindingFlags.Instance | System.Reflection.BindingFlags.Public | System.Reflection.BindingFlags.NonPublic);
@@ -189,3 +357,3 @@
                 {
-                    // 個別コマンドの状態更新で例外が起きても継続する
+                    // 個別コマンドの状態更新で例外が起きても他は続行する
                 }
@@ -193,3 +361,3 @@
         }
-        
+
         private InputModel? _currentInputModel;
@@ -200,13 +368,11 @@
             {
-                // SetProperty は ObservableObject のユーティリティ(CommunityToolkit)
+                // SetProperty は ObservableObject のユーティリティ（CommunityToolkit）
                 if (SetProperty(ref _currentInputModel, value))
                 {
-                    // VM でアタッチなどはここで一度だけ行う
+                    // VM 再アタッチなどはここで一度だけ行う
                     _currentInputModel?.AttachViewModel(this);
 
-                    // 初期化後のUI更新・コマンド状態などをまとめて行う
                     UpdateWindowImmediate();
-                    RaiseAllCommandsCanExecuteOptimized();
+                    RaiseAllCommandsCanExecute();
 
-                    // 必要な個別プロパティ通知
                     OnPropertyChanged(nameof(CurrentInputModel));
@@ -216,3 +382,3 @@
 
-        // 以前: CurrentFilePath
+        // 修正例: CurrentFilePath
         private string? _currentFilePath;
@@ -226,4 +392,3 @@
                 {
-                    // ファイルパス変更に伴う状態変更（必要なら）
-                    RaiseAllCommandsCanExecuteOptimized();
+                    RaiseAllCommandsCanExecute();
                 }
@@ -232,3 +397,3 @@
 
-        // 以前: Canvas3DLayout
+        // 修正例: Canvas3DLayout
         private Canvas? _canvas3DLayout;
@@ -241,5 +406,8 @@
 
+        // エクスポート用キャプチャ中フラグ（SetCtの自動上書きをスキップする）
+        public bool IsCapturingForExport { get; set; }
+
         private Action? _updateWindowAction;
 
-        // 以前: アクション型プロパティ（必要なら）
+        // 修正例: アクションをプロパティ化（必要なら）
         public Action? UpdateWindowAction
@@ -260,3 +428,3 @@
 
-        // ・ｽC・ｽx情報g・ｽｭ火ゑｿｽ・ｽ驛・ｿｽ\・ｽb・ｽh
+        // イベントを発火するメソッド
         public virtual void OnDataGridSettlementSoilLayersCellEditEnding(DataGridCellEditEndingEventArgs e)
@@ -266,4 +434,5 @@
 
-        // コマンドの実装
         private ICommand _dataGridSettlementSoilLayersCellEditEndingCommand;
+        private Action zoomFitAction;
+
         public ICommand DataGridSettlementSoilLayersCellEditEndingCommand
@@ -277,4 +446,3 @@
 
-        // 追加: ビュー操作用デリゲート（コードビハインド側でセット）
-        public Action? ZoomFitAction { get; set; }
+        public Action? ZoomFitAction { get => zoomFitAction; set => zoomFitAction = value; }
         public Action<double, double>? AnimateViewAnglesAction { get; set; }
@@ -292,3 +460,4 @@
         {
-            // θ=-90, φ=90
+            // θ=-90, φ=90、透視投影を無効化
+            CanvasThreeDView.IsPerspective = false;
             if (AnimateViewAnglesAction != null) AnimateViewAnglesAction(-90, 90);
@@ -306,2 +475,4 @@
         {
+            // 透視投影を無効化
+            CanvasThreeDView.IsPerspective = false;
             if (AnimateViewAnglesAction != null) AnimateViewAnglesAction(0, 0);
@@ -315,3 +486,2 @@
 
-        // XZ平面
         [RelayCommand]
@@ -319,2 +489,4 @@
         {
+            // 透視投影を無効化
+            CanvasThreeDView.IsPerspective = false;
             if (AnimateViewAnglesAction != null) AnimateViewAnglesAction(-90, 0);
@@ -328,3 +500,2 @@
 
-        // アイソメ
         [RelayCommand]
@@ -341,3 +512,2 @@
 
-        // イベントハンドラの実装
         private void HandleDataGridSettlementSoilLayersCellEditEnding(object sender, DataGridCellEditEndingEventArgs e)
@@ -380,8 +550,4 @@
         {
-            if (e.EditAction == DataGridEditAction.Commit)
+            HandleDataGridCellEditEnding(e, () =>
             {
-                // バインディングソースの更新
-                var binding = e.EditingElement.GetBindingExpression(TextBox.TextProperty);
-                binding?.UpdateSource();
-
                 IsElementSplit = false;
@@ -389,6 +555,2 @@
 
-                // 変更: デバウンスで更新
-                RequestUpdateWindow();
-                UpdateTreeView();
-
                 // コレクション自体の変更通知
@@ -398,3 +560,3 @@
                 OnPropertyChanged(nameof(GroupPileSettlementYMax));
-            }
+            });
         }
@@ -405,12 +567,3 @@
         {
-            if (e.EditAction == DataGridEditAction.Commit)
-            {
-                // バインディングソースの更新
-                var binding = e.EditingElement.GetBindingExpression(TextBox.TextProperty);
-                binding?.UpdateSource();
-
-                // 変更済（追加の箇所で適用）
-                RequestUpdateWindow();
-                UpdateTreeView();
-            }
+            HandleDataGridCellEditEnding(e);
         }
@@ -421,12 +574,3 @@
         {
-            if (e.EditAction == DataGridEditAction.Commit)
-            {
-                // バインディングソースの更新
-                var binding = e.EditingElement.GetBindingExpression(TextBox.TextProperty);
-                binding?.UpdateSource();
-
-                // 変更済（追加の箇所で適用）
-                RequestUpdateWindow();
-                UpdateTreeView();
-            }
+            HandleDataGridCellEditEnding(e);
         }
@@ -447,5 +591,5 @@
                 MessageBoxResult result = MessageBox.Show(
-                    $"{text}・ｽ・ｽﾒ集・ｽA・ｽm・ｽ閧ｷ・ｽ・ｽﾉは、情報ﾍ済みの要・ｽf根入部根入部ﾑ、" +
-                    $"\n・ｽ・ｽﾍ鯉ｿｽ・ｽﾊゑｿｽ情報ﾝゑｿｽ・ｽ・ｽ鼾・ｿｽﾍ会ｿｽﾍ鯉ｿｽ・ｽﾊゑｿｽ・ｽ尞懶ｿｽ情報K・ｽv根入部・ｽ・ｽﾜゑｿｽ・ｽB" +
-                    $"\n・ｽ・ｽ・ｵ情報ﾅゑｿｽ情報B",
+                    $"{text}を編集、確定するには、入力済みの要素分割および、" +
+                    $"\n解析結果が存在する場合は解析結果を削除する必要があります。" +
+                    $"\nよろしいですか。",
                     "確認",
@@ -461,3 +605,3 @@
                     IsHorizontalAnalysisDone = false;
-                    // 変更済（追加の箇所で適用）
+                    // 変更後（以下の箇所で適用）
                     RequestUpdateWindow();
@@ -475,3 +619,2 @@
                 // マウス位置で ContextMenu を表示
-                //startPoint = e.GetPosition(null);
             }
@@ -488,3 +631,3 @@
                 {
-                    // Visibility ・ｽｧ御す・ｽ・ｽo・ｽC情報f・ｽB情報O・ｽ・ｽﾝ抵ｿｽ
+                    // Visibility を制御するバインディングを設定
                     var isElastic = IsElastic ? Visibility.Visible : Visibility.Collapsed;
@@ -497,3 +640,3 @@
         {
-            if (!CheckAndResetElementSplit("根入部・ｽ・ｽ"))
+            if (!CheckAndResetElementSplit("根入部"))
                 e.Handled = true;
@@ -503,3 +646,3 @@
         {
-            if (!CheckAndResetElementSplit("根入部・ｽ・ｽ"))
+            if (!CheckAndResetElementSplit("根入部"))
                 e.Handled = true;
@@ -509,3 +652,3 @@
         {
-            if (!CheckAndResetElementSplit("根入部・ｽ・ｽ"))
+            if (!CheckAndResetElementSplit("根入部"))
                 e.Handled = true;
@@ -515,3 +658,3 @@
         {
-            if (!CheckAndResetElementSplit("根入部・ｽ・ｽ"))
+            if (!CheckAndResetElementSplit("根入部"))
                 e.Cancel = true;
@@ -521,6 +664,2 @@
         {
-            //if (!CheckAndResetElementSplit("・ｽn・ｽ・ｽ"))
-            //{
-            //    e.Handled = true;
-            //}
         }
@@ -529,6 +668,2 @@
         {
-            //if (!CheckAndResetElementSplit("・ｽY・ｽ・ｽ"))
-            //{
-            //    e.Handled = true;
-            //}
         }
@@ -557,5 +692,4 @@
                 UpdateEmbedment();
-                // 変更済（追加の箇所で適用）
-                RequestUpdateWindow();
-                UpdateTreeView();
+                // 変更後（以下の箇所で適用）
+                NotifyUIChanged();
             }
@@ -566,3 +700,3 @@
             UpdateEmbedment();
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -581,4 +715,2 @@
                     LayerThickness = lastItem.LayerThickness,
-                    //TopAltitude = lastItem.TopAltitude,
-                    //BottomAltitude = lastItem.BottomAltitude,
                     X1 = lastItem.X1,
@@ -595,4 +727,2 @@
                     LayerThickness = 5.0,
-                    //TopAltitude = 0.0,
-                    //BottomAltitude = 0.0,
                     X1 = 0.0,
@@ -608,11 +738,3 @@
         {
-            if (e.EditAction == DataGridEditAction.Commit)
-            {
-                // バインディングソースの更新
-                var binding = e.EditingElement.GetBindingExpression(TextBox.TextProperty);
-                binding?.UpdateSource();
-
-                UpdateEmbedment();
-                RequestUpdateWindow();
-            }
+            HandleDataGridCellEditEnding(e, () => UpdateEmbedment(), updateTree: false);
         }
@@ -621,16 +743,9 @@
         {
-            if (e.EditAction == DataGridEditAction.Commit)
-            {
-                // バインディングソースの更新
-                var binding = e.EditingElement.GetBindingExpression(TextBox.TextProperty);
-                binding?.UpdateSource();
-
-                RequestUpdateWindow();
-            }
+            HandleDataGridCellEditEnding(e, updateTree: false);
         }
 
-        // 根入部情報f・ｽ[・ｽ^・ｽO情報b・ｽh・ｽX・ｽV情報\・ｽb・ｽh
+        // 根入部データグリッド更新メソッド
         public void UpdateEmbedment()
         {
-            // EmbedmentCollection ・ｽﾌ更・ｽV
+            // EmbedmentCollection の更新
             for (int i = CurrentInputModel.EmbedmentInput.EmbedmentLayers.Count - 1; i >= 0; i--)
@@ -648,12 +763,3 @@
         {
-            if (e.EditAction == DataGridEditAction.Commit)
-            {
-                // バインディングソースの更新
-                var binding = e.EditingElement.GetBindingExpression(TextBox.TextProperty);
-                binding?.UpdateSource();
-
-                IsGroupPileSettlementAnalysisDone = false;
-
-                RequestUpdateWindow();
-            }
+            HandleDataGridCellEditEnding(e, () => IsGroupPileSettlementAnalysisDone = false, updateTree: false);
         }
@@ -688,3 +794,3 @@
                 }
-                // 変更済（追加の箇所で適用）
+                // 変更後（以下の箇所で適用）
                 RequestUpdateWindow();
@@ -693,3 +799,3 @@
 
-        // GridX・ｽﾇ会ｿｽ情報\・ｽb・ｽh
+        // GridX追加メソッド
         [RelayCommand]
@@ -697,7 +803,7 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi1・ｽ・ｽﾌ追会ｿｽ・ｽ・ｽ1・ｽX・ｽe・ｽb・ｽv・ｽﾅ戻ゑｿｽ・ｽ・ｽ謔､・ｽﾉゑｿｽ・ｽ・ｽj
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undoポイントを追加（1回の追加を1ステップで戻せるようにする）
+            TrySaveUndoSnapshotSafely();
 
-            // ・ｽh・ｽg・ｽ・ｽ: null ・ｽﾌ場合・ｽﾍゑｿｽ情報ﾅ撰ｿｽ・ｽ・ｽ
-            CurrentInputModel.GridXItems ??= new ObservableCollection<GridDataItem>();
+            // 防波堤: null の場合はここで生成
+            CurrentInputModel.GridXItems ??= [];
             AddGrid(CurrentInputModel.GridXItems, "X1", 7.2);
@@ -706,3 +812,3 @@
 
-        // GridY・ｽﾇ会ｿｽ情報\・ｽb・ｽh
+        // GridY追加メソッド
         [RelayCommand]
@@ -710,4 +816,4 @@
         {
-            TrySaveUndoSnapshotSafelyOptimized();
-            CurrentInputModel.GridYItems ??= new ObservableCollection<GridDataItem>();
+            TrySaveUndoSnapshotSafely();
+            CurrentInputModel.GridYItems ??= [];
             AddGrid(CurrentInputModel.GridYItems, "Y1", 7.2);
@@ -716,3 +822,3 @@
 
-        // Grid・ｽﾇ会ｿｽ情報\・ｽb・ｽh
+        // Grid追加メソッド
         private void AddGrid(ObservableCollection<GridDataItem> collection, string name, double spacing)
@@ -722,3 +828,3 @@
                 collection[^1].Name = name;
-            // 根入部・ｽﾌア・ｽC・ｽe根入部根入部鼾・ｿｽA・ｽO・ｽﾌア・ｽC・ｽe情報ﾌ設抵ｿｽ・ｽ・ｽR・ｽs・ｽ[
+            // 複数のアイテムがある場合、前のアイテムの設定をコピー
             else if (collection.Count == 2)
@@ -734,3 +840,3 @@
             RecalculateGrid(collection);
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -755,3 +861,3 @@
             }
-            // 変更: デバウンスで更新
+            // 変更: デバウンス付きで更新
             RequestUpdateWindow();
@@ -759,3 +865,3 @@
 
-        // ・ｽ・ｽ`・ｽﾗ重・ｽﾇ会ｿｽ情報\・ｽb・ｽh
+        // 矩形荷重追加メソッド
         [RelayCommand]
@@ -766,4 +872,4 @@
 
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undoポイントを追加
+            TrySaveUndoSnapshotSafely();
 
@@ -775,3 +881,3 @@
 
-        // ・ｽ・ｽﾍ後処根入部・ｽ[・ｽh・ｽﾌ場合・ｽﾌ確・ｽF
+        // 解析後処理モードの場合の確認
         private bool CheckAndResetPostAnalysisMode()
@@ -780,11 +886,11 @@
             {
-                var result = MessageBox.Show("・ｽ解析前処理モードにしますか？", "確認", MessageBoxButton.YesNo, MessageBoxImage.Question);
+                var result = MessageBox.Show("解析前処理モードにしますか？", "確認", MessageBoxButton.YesNo, MessageBoxImage.Question);
                 if (result == MessageBoxResult.No)
-                    return false; // 根入部・ｽ・ｽL根入部・ｽZ・ｽ・ｽ
-                IsPostAnalysisMode = false; // ・ｽ・ｽﾍ前根入部情報[・ｽh・ｽﾉ変更
+                    return false; // 操作をキャンセル
+                IsPostAnalysisMode = false; // 解析前処理モードに変更
             }
-            return true; // 根入部ｱ行
+            return true; // 操作を続行
         }
 
-        // 群杭根入部根入部・ｽp根入部・ｽp・ｽy・ｽw・ｽﾇ会ｿｽ情報\・ｽb・ｽh
+        // 群杭沈下検討用検討用土層追加メソッド
         [RelayCommand]
@@ -795,3 +901,3 @@
 
-            TrySaveUndoSnapshotSafelyOptimized();
+            TrySaveUndoSnapshotSafely();
 
@@ -825,5 +931,5 @@
 
-            UpdateSettlementSoilLayer(); // ・ｽX・ｽV
+            UpdateSettlementSoilLayer(); // 更新
 
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -831,3 +937,3 @@
 
-        // ・ｽS・ｽy・ｽw・ｽ尞懶ｿｽ・ｽ・ｽ\・ｽb・ｽh
+        // 全土層削除メソッド
         [RelayCommand]
@@ -839,8 +945,8 @@
 
-            TrySaveUndoSnapshotSafelyOptimized();
+            TrySaveUndoSnapshotSafely();
 
-            // ・ｽy・ｽw・ｽR情報N・ｽV根入部情報N情報A
+            // 土層コレクションをクリア
             settlement.SettlementSoilLayers?.Clear();
 
-            // ・ｽ・ｽﾍに用根入部O情報b・ｽh・ｽf・ｽ[・ｽ^情報N情報A
+            // 解析に用いるグリッドデータをクリア
             try
@@ -853,6 +959,6 @@
             {
-                // ・ｽO・ｽﾌゑｿｽ・ｽﾟ暦ｿｽO・ｽﾍ厄ｿｽ情報i・ｽR情報N・ｽV根入部・ｽ・ｽ null ・ｽﾌ可能情報ﾈど）
+                // 念のため例外は無視（コレクションが null の可能性など）
             }
 
-            // 情報f根入部・ｽﾌグ情報b・ｽh・ｽf・ｽ[・ｽ^・ｽ尞懶ｿｽp情報\・ｽb・ｽh根入部・ｽ・ｽﾎ呼ゑｿｽ
+            // モデル側のグリッドデータ削除用メソッドがあれば呼ぶ
             try
@@ -863,6 +969,6 @@
             {
-                // 根入部情報ﾈゑｿｽ・ｽ鼾・ｿｽ・ｽ・ｽO・ｽﾍ厄ｿｽ・ｽ・ｽ
+                // 実装がない場合や例外は無視
             }
 
-            // ・ｽ・ｽﾍフ情報O・ｽﾆ表情報t情報O根入部・ｽZ・ｽb・ｽg
+            // 解析フラグと表示フラグをリセット
             IsGroupPileSettlementAnalysisDone = false;
@@ -872,6 +978,6 @@
 
-            // ・ｽK・ｽv・ｽﾈゑｿｽv情報p・ｽe・ｽB・ｽX・ｽV・ｽﾊ知
+            // 必要ならプロパティ更新通知
             OnPropertyChanged(nameof(CurrentInputModel));
 
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -880,3 +986,3 @@
 
-        // 群杭根入部根入部・ｽp根入部・ｽp・ｽy・ｽw・ｽ尞懶ｿｽ・ｽ・ｽ\・ｽb・ｽh
+        // 群杭沈下検討用検討用土層削除メソッド
         [RelayCommand]
@@ -884,19 +990,12 @@
         {
-            // sender ・ｽ・ｽ GridDataItem ・ｽﾅゑｿｽ・ｽ驍ｱ・ｽﾆゑｿｽ確認
-            if (sender is not SettlementSoilLayer itemToDelete)
-                return;
-
-            // ・ｽR情報N・ｽV根入部根入部尞・
-            CurrentInputModel.PileGroupSettlement.SettlementSoilLayers.Remove(itemToDelete);
-
-            UpdateSettlementSoilLayer(); // ・ｽX・ｽV
-
-            // 変更済（追加の箇所で適用）
-            RequestUpdateWindow();
+            DeleteCollectionItem(
+                sender,
+                CurrentInputModel.PileGroupSettlement.SettlementSoilLayers,
+                () => UpdateSettlementSoilLayer());
         }
 
-        // 群杭根入部根入部・ｽp根入部・ｽp・ｽy・ｽw・ｽf・ｽ[・ｽ^・ｽO情報b・ｽh・ｽX・ｽV情報\・ｽb・ｽh
+        // 群杭沈下検討用検討用土層データグリッド更新メソッド
         private void UpdateSettlementSoilLayer()
         {
-            // SettlementCollection ・ｽﾌ更・ｽV
+            // SettlementCollection の更新
             double loadingPlaneAltitude = CurrentInputModel.PileGroupSettlement.LoadingPlaneAltitude;
@@ -915,3 +1014,3 @@
             RecalculateGrid(CurrentInputModel.GridXItems);
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -922,3 +1021,3 @@
             RecalculateGrid(CurrentInputModel.GridYItems);
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -942,4 +1041,4 @@
         {
-            // Undo・ｽ|・ｽC情報g
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undoポイント
+            TrySaveUndoSnapshotSafely();
 
@@ -947,3 +1046,3 @@
             RecalculateGrid(CurrentInputModel.GridXItems);
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -953,4 +1052,4 @@
         {
-            // Undo・ｽ|・ｽC情報g
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undoポイント
+            TrySaveUndoSnapshotSafely();
 
@@ -958,3 +1057,3 @@
             RecalculateGrid(CurrentInputModel.GridYItems);
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -964,10 +1063,3 @@
         {
-            // sender ・ｽ・ｽ GridDataItem ・ｽﾅゑｿｽ・ｽ驍ｱ・ｽﾆゑｿｽ確認
-            if (sender is not Element itemToDelete) return;
-
-            // ・ｽR情報N・ｽV根入部根入部尞・
-            CurrentInputModel.Elements.Remove(itemToDelete);
-
-            // 変更済（追加の箇所で適用）
-            RequestUpdateWindow();
+            DeleteCollectionItem(sender, CurrentInputModel.Elements);
         }
@@ -976,6 +1068,6 @@
         {
-            // sender ・ｽ・ｽ GridDataItem ・ｽﾅゑｿｽ・ｽ驍ｱ・ｽﾆゑｿｽ確認
+            // sender が GridDataItem であることを確認
             if (sender is not GridDataItem itemToDelete) return;
 
-            // ・ｽR情報N・ｽV根入部根入部尞・
+            // コレクションから削除
             collection.Remove(itemToDelete);
@@ -986,10 +1078,3 @@
         {
-            // sender ・ｽ・ｽ GridDataItem ・ｽﾅゑｿｽ・ｽ驍ｱ・ｽﾆゑｿｽ確認
-            if (sender is not RectLoad itemToDelete) return;
-
-            // ・ｽR情報N・ｽV根入部根入部尞・
-            CurrentInputModel.PileGroupSettlement.RectLoads.Remove(itemToDelete);
-
-            // 変更済（追加の箇所で適用）
-            UpdateWindowImmediate();
+            DeleteCollectionItem(sender, CurrentInputModel.PileGroupSettlement.RectLoads, immediate: true);
         }
@@ -999,3 +1084,3 @@
         {
-            UpdateWindowImmediate(); // ・ｽf情報Q・ｽ[・ｽg・ｽ・ｽﾊゑｿｽ・ｽﾄコ・ｽ[・ｽh・ｽr・ｽn・ｽC情報h・ｽﾌ・ｿｽ・ｽ\・ｽb・ｽh情報ﾄび出・ｽ・ｽ
+            UpdateWindowImmediate(); // デリゲートを通じてコードビハインドのメソッドを呼び出す
         }
@@ -1005,6 +1090,6 @@
         {
-            UpdateCanvas3DAction?.Invoke(); // ・ｽf情報Q・ｽ[・ｽg・ｽ・ｽﾊゑｿｽ・ｽﾄコ・ｽ[・ｽh・ｽr・ｽn・ｽC情報h・ｽﾌ・ｿｽ・ｽ\・ｽb・ｽh情報ﾄび出・ｽ・ｽ
+            UpdateCanvas3DAction?.Invoke(); // デリゲートを通じてコードビハインドのメソッドを呼び出す
         }
 
-        // 杭配置・ｽﾇ会ｿｽ・ｽR・ｽ}情報h・ｽﾌ趣ｿｽ・ｽs情報\・ｽb・ｽh
+        // 杭配置追加コマンドの実行メソッド
         [RelayCommand]
@@ -1015,10 +1100,4 @@
 
-            // 根入部・ｽﾉ追会ｿｽ
-            //var deepCopy = CurrentInputModel.DeepCopy();
-            //Debug.WriteLine("CurrentInputModel.PileLayoutItems Hash: " + CurrentInputModel.PileLayoutItems.GetHashCode());
-            //Debug.WriteLine("DeepCopy.PileLayoutItems Hash: " + deepCopy.PileLayoutItems.GetHashCode());
-            //_undoManager.SaveState(deepCopy);
-
-            // ・ｽX・ｽi・ｽb・ｽv・ｽV情報b・ｽg・ｽ保存
-            TrySaveUndoSnapshotSafelyOptimized();
+            // スナップショットを保存
+            TrySaveUndoSnapshotSafely();
 
@@ -1027,3 +1106,3 @@
             {
-                // 情報O・ｽﾌ杭根入部 X 根入部・ｽ・ｽ 7.2m ・ｽI・ｽt・ｽZ・ｽb・ｽg
+                // 直前の杭から X 方向に 7.2m オフセット
                 nextPoint3D = CurrentInputModel.PileLayoutItems.Last().Point3D + new Vector3D() { X = 7.2 };
@@ -1031,3 +1110,3 @@
 
-            // UI ・ｽX情報b・ｽh・ｽﾅコ情報N・ｽV根入部・ｽﾖ追会ｿｽ
+            // UI スレッドでコレクションへ追加
             Application.Current.Dispatcher.Invoke(() =>
@@ -1036,3 +1115,3 @@
                 CurrentInputModel.PileLayoutItems[^1].SetMainWindowViewModel(this);
-                // ・ｽv・ｽf根入部情報ﾌ場合・ｽﾍ趣ｿｽ根入部 SoiPile 情報ﾄ撰ｿｽ・ｽ・ｽ
+                // 要素未分割の場合は自動で SoiPile を再生成
                 if (!IsElementSplit)
@@ -1040,3 +1119,3 @@
 
-                // 変更済（追加の箇所で適用）
+                // 変更後（以下の箇所で適用）
                 RequestUpdateWindow();
@@ -1063,3 +1142,3 @@
 
-        // ・ｽd情報v・ｽf・ｽﾌ削除
+        // 重複要素の削除
         [RelayCommand]
@@ -1067,4 +1146,4 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undoポイントを追加
+            TrySaveUndoSnapshotSafely();
 
@@ -1073,3 +1152,3 @@
 
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -1077,3 +1156,3 @@
 
-        // ・ｽv・ｽf・ｽﾌ節点・ｽﾊ置・ｽﾅの包ｿｽ・ｽ・ｽ
+        // 要素の節点位置での分割
         [RelayCommand]
@@ -1081,4 +1160,4 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undoポイントを追加
+            TrySaveUndoSnapshotSafely();
 
@@ -1099,3 +1178,3 @@
             CurrentInputModel.Elements = newElements;
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -1103,3 +1182,3 @@
 
-        // ・ｽv・ｽf・ｽｪ奇ｿｽ情報驛・ｿｽ\・ｽb・ｽh
+        // 要素を分割するメソッド
         public ObservableCollection<Element> SplitTwoNodeElementByNodes(Element element, double threshold = 0.005)
@@ -1150,3 +1229,3 @@
 
-        // 杭配置・ｽﾔ搾ｿｽ・ｽﾌ更・ｽV
+        // 杭配置番号の更新
         public void UpdatePileLayoutNo()
@@ -1157,3 +1236,3 @@
 
-        // ・ｽﾗ重・ｽﾊの趣ｿｽ根入部・ｽ・ｽ
+        // 荷重面の自動生成
         [RelayCommand]
@@ -1161,24 +1240,15 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undoポイントを追加
+            TrySaveUndoSnapshotSafely();
+
+            // BoundingBoxCalculator を使用して境界を計算
+            var boundingBox = BoundingBoxCalculator.Calculate(
+                CurrentInputModel.PileLayoutItems,
+                RectLoadPileDistance
+            );
 
-            double maxX = double.MinValue;
-            double minX = double.MaxValue;
-            double maxY = double.MinValue;
-            double minY = double.MaxValue;
-
-            foreach (var pileLayoutDataItem in CurrentInputModel.PileLayoutItems)
-            {
-                double x = pileLayoutDataItem.Point3D.X;
-                double y = pileLayoutDataItem.Point3D.Y;
-                if (x > maxX) maxX = x;
-                if (x < minX) minX = x;
-                if (y > maxY) maxY = y;
-                if (y < minY) minY = y;
-            }
-
-            double adjustedMinX = minX - RectLoadPileDistance;
-            double adjustedMaxX = maxX + RectLoadPileDistance;
-            double adjustedMinY = minY - RectLoadPileDistance;
-            double adjustedMaxY = maxY + RectLoadPileDistance;
+            double adjustedMinX = boundingBox.MinX;
+            double adjustedMaxX = boundingBox.MaxX;
+            double adjustedMinY = boundingBox.MinY;
+            double adjustedMaxY = boundingBox.MaxY;
 
@@ -1196,3 +1266,3 @@
 
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             UpdateWindowImmediate();
@@ -1204,3 +1274,3 @@
 
-        // 根入部根入部・ｽﾊの趣ｿｽ根入部・ｽ・ｽ
+        // 根入部平面の自動調整
         [RelayCommand]
@@ -1211,21 +1281,12 @@
 
-            double maxX = double.MinValue;
-            double minX = double.MaxValue;
-            double maxY = double.MinValue;
-            double minY = double.MaxValue;
-
-            foreach (var pileLayoutDataItem in CurrentInputModel.PileLayoutItems)
-            {
-                double x = pileLayoutDataItem.Point3D.X;
-                double y = pileLayoutDataItem.Point3D.Y;
-                if (x > maxX) maxX = x;
-                if (x < minX) minX = x;
-                if (y > maxY) maxY = y;
-                if (y < minY) minY = y;
-            }
-
-            double adjustedMinX = minX - EmbedmentPileDistance;
-            double adjustedMaxX = maxX + EmbedmentPileDistance;
-            double adjustedMinY = minY - EmbedmentPileDistance;
-            double adjustedMaxY = maxY + EmbedmentPileDistance;
+            // BoundingBoxCalculator を使用して境界を計算
+            var boundingBox = BoundingBoxCalculator.Calculate(
+                CurrentInputModel.PileLayoutItems,
+                EmbedmentPileDistance
+            );
+
+            double adjustedMinX = boundingBox.MinX;
+            double adjustedMaxX = boundingBox.MaxX;
+            double adjustedMinY = boundingBox.MinY;
+            double adjustedMaxY = boundingBox.MaxY;
 
@@ -1239,3 +1300,3 @@
 
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -1244,3 +1305,3 @@
 
-        // 根入部・ｽﾍ搾ｿｽp・ｽ_・ｽ・ｽ杭配置・ｽﾌ図・ｽS・ｽﾉ移難ｿｽ情報驛・ｿｽ\・ｽb・ｽh
+        // 慣性力作用点を杭配置の図心に移動するメソッド
         [RelayCommand]
@@ -1254,18 +1315,12 @@
 
-            TrySaveUndoSnapshotSafelyOptimized();
-
-            var xs = new List<double>();
-            var ys = new List<double>();
+            TrySaveUndoSnapshotSafely();
 
-            foreach (PileLayoutDataItem pileLayoutInput in CurrentInputModel.PileLayoutItems)
-            {
-                xs.Add(pileLayoutInput.Point3D.X);
-                ys.Add(pileLayoutInput.Point3D.Y);
-            }
+            // BoundingBoxCalculator を使用して平均中心を計算
+            var (centerX, centerY) = BoundingBoxCalculator.CalculateAverageCenter(CurrentInputModel.PileLayoutItems);
 
-            CurrentInputModel.LoadCasesInput.LoadCaseLevel1Common.ForceActionPointX = xs.Average();
-            CurrentInputModel.LoadCasesInput.LoadCaseLevel1Common.ForceActionPointY = ys.Average();
+            CurrentInputModel.LoadCasesInput.LoadCaseLevel1Common.ForceActionPointX = centerX;
+            CurrentInputModel.LoadCasesInput.LoadCaseLevel1Common.ForceActionPointY = centerY;
 
-            CurrentInputModel.LoadCasesInput.LoadCaseLevel2Common.ForceActionPointX = xs.Average();
-            CurrentInputModel.LoadCasesInput.LoadCaseLevel2Common.ForceActionPointY = ys.Average();
+            CurrentInputModel.LoadCasesInput.LoadCaseLevel2Common.ForceActionPointX = centerX;
+            CurrentInputModel.LoadCasesInput.LoadCaseLevel2Common.ForceActionPointY = centerY;
 
@@ -1273,4 +1328,4 @@
             {
-                loadCase.ForceActionPointX = xs.Average();
-                loadCase.ForceActionPointY = ys.Average();
+                loadCase.ForceActionPointX = centerX;
+                loadCase.ForceActionPointY = centerY;
             }
@@ -1279,7 +1334,7 @@
             {
-                loadCase.ForceActionPointX = xs.Average();
-                loadCase.ForceActionPointY = ys.Average();
+                loadCase.ForceActionPointX = centerX;
+                loadCase.ForceActionPointY = centerY;
             }
 
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             RequestUpdateWindow();
@@ -1290,3 +1345,3 @@
         {
-            TrySaveUndoSnapshotSafelyOptimized();
+            TrySaveUndoSnapshotSafely();
 
@@ -1301,3 +1356,3 @@
 
-        //群杭・ｽW情報E・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        //群杭係数ウィンドウを開くメソッド
         [RelayCommand]
@@ -1305,8 +1360,8 @@
         {
-            // Window情報C情報X・ｽ^情報X根入部・ｽﾄ表・ｽ・ｽ
+            // Windowをインスタンス化して表示
             GroupPileFactorWindow groupPileFactorWindow = new(this);
 
-            groupPileFactorWindow.ShowDialog(); // 情報[・ｽ_情報_・ｽC・ｽA情報O・ｽﾆゑｿｽ・ｽﾄ表・ｽ・ｽ
+            groupPileFactorWindow.ShowDialog(); // モーダルダイアログとして表示
 
-            // ・ｽﾏ更: ・ｽ_・ｽC・ｽA情報O・ｽ・ｽﾍ托ｿｽ根入部・ｽs
+            // 変更: ダイアログ後は即時実行
             UpdateWindowImmediate();
@@ -1316,83 +1371,3 @@
 
-        // 群杭根入部・ｽ・ｽﾍの趣ｿｽ・ｽs情報\・ｽb・ｽh
-        //[RelayCommand]
-        //private void PileGroupSettlementAnalysis()
-        //{
-        //    // ・ｽy・ｽw・ｽ・ｽ0・ｽﾌ場合・ｽﾍ警根入部・ｽo情報ﾄ擾ｿｽ情報・f
-        //    if (CurrentInputModel.PileGroupSettlement.SettlementSoilLayers == null ||
-        //        CurrentInputModel.PileGroupSettlement.SettlementSoilLayers.Count == 0)
-        //    {
-        //        MessageBox.Show("群杭沈下解析用の土層が1層以上必要です。", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
-        //        return;
-        //    }
-
-        //    ObservableCollection<RectLoad> rectLoads = [];
-
-        //    if (CurrentInputModel.PileGroupSettlement.LoadingType == "一括入力")
-        //    {
-        //        rectLoads = CurrentInputModel.PileGroupSettlement.RectLoads;
-        //    }
-        //    else if (CurrentInputModel.PileGroupSettlement.LoadingType == "個別入力")
-        //    {
-        //        foreach (PileLayoutDataItem pileLayoutDataItem in CurrentInputModel.PileLayoutItems)
-        //        {
-        //            SoilPile soilPile = CurrentInputModel.ElementDivision.SoilPiles[pileLayoutDataItem.SoilPileAltNo - 1];
-        //            double radius = soilPile.GroupPileLoadDia * 0.5;
-        //            Point point = new() { X = pileLayoutDataItem.Point3D.X, Y = pileLayoutDataItem.Point3D.Y };
-        //            double qa = pileLayoutDataItem.AxialForceVL0 + pileLayoutDataItem.AxialForceVLAdditional;
-
-        //            ObservableCollection<RectLoad> eachRectLoads
-        //                = PileGroupSettlement.GetCrossRectLoads(point, radius, qa);
-
-        //            foreach (var rectLoad in eachRectLoads)
-        //                rectLoads.Add(rectLoad);
-        //        }
-        //    }
-
-        //    foreach (PileLayoutDataItem pileLayoutDataItem in CurrentInputModel.PileLayoutItems)
-        //    {
-        //        Point point = new() { X = pileLayoutDataItem.Point3D.X, Y = pileLayoutDataItem.Point3D.Y };
-        //        pileLayoutDataItem.GroupPileSettlement = Steinnbrener.CalcSettlement(
-        //            point, rectLoads, CurrentInputModel.PileGroupSettlement.SettlementSoilLayers) * 1000;
-        //    }
-
-        //    double xMin = GroupPileSettlementXMin;
-        //    double xMax = GroupPileSettlementXMax;
-        //    double yMin = GroupPileSettlementYMin;
-        //    double yMax = GroupPileSettlementYMax;
-        //    double xOffset = GroupPileSettlementXOffset;
-        //    double yOffset = GroupPileSettlementYOffset;
-        //    double xSpacing = GroupPileSettlementXSpacing;
-        //    double ySpacing = GroupPileSettlementYSpacing;
-
-        //    CurrentInputModel.PileGroupSettlement.SetGridX(xMin, xMax, xOffset, xSpacing, CurrentInputModel.GridXItems);
-        //    CurrentInputModel.PileGroupSettlement.SetGridY(yMin, yMax, yOffset, ySpacing, CurrentInputModel.GridYItems);
-
-        //    ObservableCollection<double> xs = CurrentInputModel.PileGroupSettlement.SettlementGridX;
-        //    ObservableCollection<double> ys = CurrentInputModel.PileGroupSettlement.SettlementGridY;
-
-        //    CurrentInputModel.PileGroupSettlement.SettlementGridData = [];
-        //    foreach (var x in xs)
-        //    {
-        //        foreach (var y in ys)
-        //        {
-        //            Point point = new() { X = x, Y = y };
-        //            var settlement = Steinnbrener.CalcSettlement(
-        //                point, rectLoads, CurrentInputModel.PileGroupSettlement.SettlementSoilLayers) * 1000;
-        //            CurrentInputModel.PileGroupSettlement.SettlementGridData.Add(new());
-        //            CurrentInputModel.PileGroupSettlement.SettlementGridData[^1].X = x;
-        //            CurrentInputModel.PileGroupSettlement.SettlementGridData[^1].Y = y;
-        //            CurrentInputModel.PileGroupSettlement.SettlementGridData[^1].Settlement = settlement;
-        //        }
-        //    }
-
-        //    MessageBox.Show("スタインブレナーの近似式による解析が終了しました。");
-
-        //    IsGroupPileGridDeformationVisible = true;
-        //    IsGroupPileSettlementAnalysisDone = true;
-        //    //IsAnalysisResultVisible = true;
-        //    IsBubbleVisible = true;
-        //    IsArrowVisible = true;
-
-        //}
+        // 群杭沈下解析の実行メソッド
         [RelayCommand]
@@ -1400,9 +1375,20 @@
         {
-            var ct = GetLongOperationCancellationToken();
+            var result = _settlementAnalysisService.PerformSettlementAnalysis(
+                CurrentInputModel.PileGroupSettlement,
+                CurrentInputModel.PileLayoutItems,
+                CurrentInputModel.ElementDivision.SoilPiles,
+                CurrentInputModel.GridXItems,
+                CurrentInputModel.GridYItems,
+                GroupPileSettlementXMin,
+                GroupPileSettlementXMax,
+                GroupPileSettlementYMin,
+                GroupPileSettlementYMax,
+                GroupPileSettlementXOffset,
+                GroupPileSettlementYOffset,
+                GroupPileSettlementXSpacing,
+                GroupPileSettlementYSpacing);
 
-            // ・ｽy・ｽw・ｽ・ｽ0・ｽﾌ場合・ｽﾍ警根入部・ｽo情報ﾄ擾ｿｽ情報・f
-            if (CurrentInputModel.PileGroupSettlement.SettlementSoilLayers == null ||
-                CurrentInputModel.PileGroupSettlement.SettlementSoilLayers.Count == 0)
+            if (!result.Success)
             {
-                MessageBox.Show("群杭沈下解析用の土層が1層以上必要です。", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show(result.ErrorMessage, "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
                 return;
@@ -1410,106 +1396,14 @@
 
-            try
-            {
-                ObservableCollection<RectLoad> rectLoads = new ObservableCollection<RectLoad>();
-
-                if (CurrentInputModel.PileGroupSettlement.LoadingType == "一括入力")
-                {
-                    rectLoads = CurrentInputModel.PileGroupSettlement.RectLoads;
-                }
-                else if (CurrentInputModel.PileGroupSettlement.LoadingType == "個別入力")
-                {
-                    foreach (PileLayoutDataItem pileLayoutDataItem in CurrentInputModel.PileLayoutItems)
-                    {
-                        ct.ThrowIfCancellationRequested();
-
-                        SoilPile soilPile = CurrentInputModel.ElementDivision.SoilPiles[pileLayoutDataItem.SoilPileAltNo - 1];
-                        double radius = soilPile.GroupPileLoadDia * 0.5;
-                        Point point = new() { X = pileLayoutDataItem.Point3D.X, Y = pileLayoutDataItem.Point3D.Y };
-                        double qa = pileLayoutDataItem.AxialForceVL0 + pileLayoutDataItem.AxialForceVLAdditional;
-
-                        var eachRectLoads = PileGroupSettlement.GetCrossRectLoads(point, radius, qa);
-
-                        foreach (var rectLoad in eachRectLoads)
-                            rectLoads.Add(rectLoad);
-                    }
-                }
-
-                foreach (PileLayoutDataItem pileLayoutDataItem in CurrentInputModel.PileLayoutItems)
-                {
-                    ct.ThrowIfCancellationRequested();
-
-                    Point point = new() { X = pileLayoutDataItem.Point3D.X, Y = pileLayoutDataItem.Point3D.Y };
-                    pileLayoutDataItem.GroupPileSettlement = Steinnbrener.CalcSettlement(
-                        point, rectLoads, CurrentInputModel.PileGroupSettlement.SettlementSoilLayers) * 1000;
-                }
-
-                double xMin = GroupPileSettlementXMin;
-                double xMax = GroupPileSettlementXMax;
-                double yMin = GroupPileSettlementYMin;
-                double yMax = GroupPileSettlementYMax;
-                double xOffset = GroupPileSettlementXOffset;
-                double yOffset = GroupPileSettlementYOffset;
-                double xSpacing = GroupPileSettlementXSpacing;
-                double ySpacing = GroupPileSettlementYSpacing;
-
-                CurrentInputModel.PileGroupSettlement.SetGridX(xMin, xMax, xOffset, xSpacing, CurrentInputModel.GridXItems);
-                CurrentInputModel.PileGroupSettlement.SetGridY(yMin, yMax, yOffset, ySpacing, CurrentInputModel.GridYItems);
-
-                var xs = CurrentInputModel.PileGroupSettlement.SettlementGridX;
-                var ys = CurrentInputModel.PileGroupSettlement.SettlementGridY;
-
-                // 根入部・ｽ・ｽ SettlementGridDataItem ・ｽﾉ変更・ｽi・ｽ^・ｽ・ｽ Models.InputData ・ｽﾉ抵ｿｽ`・ｽﾏみ）
-                CurrentInputModel.PileGroupSettlement.SettlementGridData = new ObservableCollection<SettlementGridDataItem>();
-
-                foreach (var x in xs)
-                {
-                    ct.ThrowIfCancellationRequested();
-                    foreach (var y in ys)
-                    {
-                        ct.ThrowIfCancellationRequested();
-
-                        Point point = new() { X = x, Y = y };
-                        var settlement = Steinnbrener.CalcSettlement(
-                            point, rectLoads, CurrentInputModel.PileGroupSettlement.SettlementSoilLayers) * 1000;
-
-                        var gridPoint = new SettlementGridDataItem { X = x, Y = y, Settlement = settlement };
-                        CurrentInputModel.PileGroupSettlement.SettlementGridData.Add(gridPoint);
-                    }
-                }
-
-                MessageBox.Show("スタインブレナーの近似式による解析が終了しました。");
+            CurrentInputModel.PileGroupSettlement.SettlementGridData = result.SettlementGridData;
 
-                IsGroupPileGridDeformationVisible = true;
-                IsGroupPileSettlementAnalysisDone = true;
-                IsBubbleVisible = true;
-                IsArrowVisible = true;
-            }
-            catch (OperationCanceledException)
-            {
-                StatusMessage = "群杭根入部・ｽ・ｽﾍゑｿｽ・ｽL根入部・ｽZ根入部・ｽ・ｽﾜゑｿｽ情報B";
-                // ・ｽK・ｽv・ｽﾈゑｿｽr根入部・ｽﾊの破・ｽ・ｽ/・ｽﾄ擾ｿｽ根入部根入部情報ﾅ行・ｽ・ｽ
-            }
-            catch (Exception ex)
-            {
-                MessageBox.Show($"群杭根入部・ｽ・ｽﾍでエ情報[根入部根入部・ｽﾜゑｿｽ情報B\n{ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
-            }
-        }
+            MessageBox.Show("スタインブレナーの近似式による解析が終了しました。");
 
-        [RelayCommand]
-        private void CancelLongOperation()
-        {
-            // Improvements partial ・ｽ・ｽ CancelLongRunningOperations() 情報ﾄゑｿｽ
-            try
-            {
-                CancelLongRunningOperations();
-                StatusMessage = "根入部情報L根入部・ｽZ根入部・ｽﾄゑｿｽ・ｽﾜゑｿｽ...";
-            }
-            catch (Exception ex)
-            {
-                System.Diagnostics.Debug.WriteLine($"CancelLongRunningOperations failed: {ex}");
-                // 繧ｭ繝｣繝ｳ繧ｻ繝ｫ螟ｱ謨励・閾ｴ蜻ｽ逧・〒縺ｪ縺・◆繧√√Ο繧ｰ縺縺第ｮ九＠縺ｦ邯夊｡・
-            }
+            IsGroupPileGridDeformationVisible = true;
+            IsGroupPileSettlementAnalysisDone = true;
+            //IsAnalysisResultVisible = true;
+            IsBubbleVisible = true;
+            IsArrowVisible = true;
         }
 
-        // 根入部・ｽO情報Y・ｽﾝ抵ｿｽﾌ擾ｿｽ根入部・ｽ\・ｽb・ｽh
+        // 自動前方杭設定の処理メソッド
         private void AutoIsFrontPilesWindow_AutoIsFrontPileCompleted(object sender, AutoIsFrontEventArgs e)
@@ -1522,43 +1416,47 @@
                 {
+                    LoadCase loadCase = CurrentInputModel.LoadCasesInput.LoadCasesLevel1[i];
+
                     foreach (PileLayoutDataItem pileLayout0 in CurrentInputModel.PileLayoutItems)
                     {
-                        pileLayout0.IsFrontPiles[i] = true; // ・ｽS・ｽﾄの杭・ｽ・ｽO情報Y・ｽﾆゑｿｽ・ｽﾄ擾ｿｽ根入部
-
-                        foreach (PileLayoutDataItem pileLayout1 in CurrentInputModel.PileLayoutItems)
-                        {
-                            if (pileLayout0 == pileLayout1) { continue; }
-
-                            Point positionVector1 = new(pileLayout1.Point3D.X, pileLayout1.Point3D.Y);
-                            Point positionVector0 = new(pileLayout0.Point3D.X, pileLayout0.Point3D.Y);
-                            Vector directionVector = positionVector1 - positionVector0;
+                        // 前方杭かどうかを判定
+                        pileLayout0.IsFrontPiles[i] = IsFrontPile(pileLayout0, loadCase, cosAlpha);
+                    }
+                }
+            }
+        }
 
-                            LoadCase loadCase = CurrentInputModel.LoadCasesInput.LoadCasesLevel1[i];
+        /// <summary>
+        /// 指定された杭が前方杭かどうかを判定
+        /// </summary>
+        private bool IsFrontPile(PileLayoutDataItem targetPile, LoadCase loadCase, double cosAlpha)
+        {
+            Point targetPosition = new(targetPile.Point3D.X, targetPile.Point3D.Y);
+            Vector loadDirectionVector = PileDesign.Converters.VectorConverter.ConvertAngleToUnitVector(loadCase.LoadAngle);
 
-                            // ・ｽﾗ重根入部・ｽx・ｽN・ｽg根入部・ｽv・ｽZ
-                            Vector loadDirectionVector = PileDesign.Converters.VectorConverter.ConvertAngleToUnitVector(loadCase.LoadAngle);
+            foreach (PileLayoutDataItem otherPile in CurrentInputModel.PileLayoutItems)
+            {
+                if (targetPile == otherPile)
+                    continue;
 
-                            // 情報ﾏゑｿｽ・ｽv・ｽZ
-                            double dotProduct = Vector.Multiply(directionVector, loadDirectionVector);
+                Point otherPosition = new(otherPile.Point3D.X, otherPile.Point3D.Y);
+                Vector directionVector = otherPosition - targetPosition;
 
-                            // ・ｽx・ｽN・ｽg情報ﾌ大き根入部・ｽv・ｽZ
-                            double magnitudeDirection = directionVector.Length;
-                            double magnitudeLoadDirection = loadDirectionVector.Length;
+                // 内積を計算
+                double dotProduct = Vector.Multiply(directionVector, loadDirectionVector);
 
-                            // ・ｽ]根入部・ｽv・ｽZ
-                            double cosTheta = dotProduct / (magnitudeDirection * magnitudeLoadDirection);
+                // 余弦を計算
+                double cosTheta = dotProduct / (directionVector.Length * loadDirectionVector.Length);
 
-                            // ・ｽ]根入部・ｽw・ｽ・ｽp・ｽx情報蛯ｫ情報鼾・
-                            if (cosAlpha < cosTheta)
-                            {
-                                pileLayout0.IsFrontPiles[i] = false;
-                                goto NextPileLayout0;
-                            }
-                        }
-                    NextPileLayout0:;
-                    }
+                // 余弦が指定角度より大きい場合、前方杭ではない
+                if (cosAlpha < cosTheta)
+                {
+                    return false;
                 }
             }
+
+            // すべての杭に対してチェックを通過したら前方杭
+            return true;
         }
 
-        // 情報O情報ﾂゑｿｽ・ｽﾄ保托ｿｽ
+        // 名前をつけて保存
         [RelayCommand]
@@ -1577,15 +1475,3 @@
                 {
-                    var projectData = new ProjectData
-                    {
-                        InputModel = this.CurrentInputModel,
-                        AnaModel = this.CurrentModel // MainWindowViewModel・ｽ・ｽAnaModel根入部・ｽ・ｽ鼾・
-                    };
-                    //var options = new JsonSerializerOptions
-                    //{
-                    //    WriteIndented = true,
-                    //    ReferenceHandler = System.Text.Json.Serialization.ReferenceHandler.Preserve
-                    //};
-                    //string json = JsonSerializer.Serialize(projectData, options);
-                    string json = JsonSerializer.Serialize(projectData, _jsonOptions);
-                    File.WriteAllText(CurrentFilePath, json);
+                    _fileOperationService.SaveProjectData(CurrentFilePath, CurrentInputModel, CurrentModel);
                     MessageBox.Show("保存が完了しました。", "情報", MessageBoxButton.OK, MessageBoxImage.Information);
@@ -1608,10 +1494,3 @@
                 {
-                    var projectData = new ProjectData
-                    {
-                        InputModel = this.CurrentInputModel,
-                        AnaModel = this.CurrentModel
-                    };
-
-                    string json = JsonSerializer.Serialize(projectData, _jsonOptions);
-                    File.WriteAllText(CurrentFilePath, json);
+                    _fileOperationService.SaveProjectData(CurrentFilePath, CurrentInputModel, CurrentModel);
                     MessageBox.Show("保存が完了しました。", "情報", MessageBoxButton.OK, MessageBoxImage.Information);
@@ -1629,3 +1508,3 @@
             var result = MessageBox.Show(
-                "情報ﾝのデ・ｽ[・ｽ^・ｽ保存情報ﾜゑｿｽ情報H",
+                "現在のデータを保存しますか？",
                 "確認",
@@ -1640,6 +1519,6 @@
             CurrentInputModel.Reset();
-            this.CurrentModel = null; // AnaModel根入部・ｽZ・ｽb・ｽg
+            this.CurrentModel = null; // AnaModelもリセット
             CurrentFilePath = null;
 
-            // 根入部・ｽﾅ擾ｿｽ根入部ﾔゑｿｽUndo・ｽX・ｽ^・ｽb・ｽN・ｽﾉ積ゑｿｽ
+            // ここで初期状態をUndoスタックに積む
             _undoManager.SaveState(CurrentInputModel.DeepCopy());
@@ -1650,9 +1529,2 @@
 
-        private static ObservableCollection<T> EnsureObservableCollection<T>(IEnumerable<T> source)
-        {
-            if (source is ObservableCollection<T> oc)
-                return oc;
-            return source != null ? new ObservableCollection<T>(source) : new ObservableCollection<T>();
-        }
-
         private void TrySaveUndoSnapshotSafely()
@@ -1668,3 +1540,3 @@
                 {
-                    System.Diagnostics.Debug.WriteLine("TrySaveUndoSnapshotSafely: DeepCopy returned null, skipping undo snapshot.");
+                    //System.Diagnostics.Debug.WriteLine("TrySaveUndoSnapshotSafely: DeepCopy returned null, skipping undo snapshot.");
                 }
@@ -1673,3 +1545,3 @@
             {
-                System.Diagnostics.Debug.WriteLine($"TrySaveUndoSnapshotSafely・ｽ・ｽO: {ex}");
+                //System.Diagnostics.Debug.WriteLine($"TrySaveUndoSnapshotSafely例外: {ex}");
             }
@@ -1677,3 +1549,3 @@
 
-        // ・ｽC・ｽ・ｽ: null ・ｽ`・ｽF・ｽb・ｽN・ｽﾆ具ｿｽR情報N・ｽV根入部根入部情報ﾌ具ｿｽ・ｽﾊ会ｿｽ
+        // 修正: null チェックと空コレクション初期化の共通化
         public bool TryLoadInputModelFileUsingInputModelLoader(string filePath)
@@ -1686,3 +1558,3 @@
                     MessageBox.Show($"ファイルの読込に失敗しました。\n{filePath}", "読込エラー", MessageBoxButton.OK, MessageBoxImage.Error);
-                    System.Diagnostics.Debug.WriteLine($"LoadFromFile returned null: {filePath}");
+                    //System.Diagnostics.Debug.WriteLine($"LoadFromFile returned null: {filePath}");
                     return false;
@@ -1693,4 +1565,19 @@
                 CurrentFilePath = filePath;
-                CurrentInputModel.GridXItems ??= new ObservableCollection<GridDataItem>();
-                CurrentInputModel.GridYItems ??= new ObservableCollection<GridDataItem>();
+                CurrentInputModel.GridXItems ??= [];
+                CurrentInputModel.GridYItems ??= [];
+
+                // 要素分割・解析状態をリセット
+                IsElementSplit = false;
+                IsHorizontalAnalysisDone = false;
+                IsVerticalAnalysisDone = false;
+                IsGroupPileSettlementAnalysisDone = false;
+
+                // M-φキャッシュをクリア（新プロジェクト読込時）
+                PileSection.ClearMphiCache();
+
+                // 群杭沈下解析結果をクリア
+                CurrentInputModel.PileGroupSettlement?.SettlementGridData?.Clear();
+                CurrentInputModel.PileGroupSettlement?.SettlementGridX?.Clear();
+                CurrentInputModel.PileGroupSettlement?.SettlementGridY?.Clear();
+
                 UpdateWindowImmediate();
@@ -1702,4 +1589,4 @@
             {
-                MessageBox.Show($"ファイル読込にエラーが発生しました。\n{ex.Message}", "読込エラー", MessageBoxButton.OK, MessageBoxImage.Error);
-                System.Diagnostics.Debug.WriteLine($"TryLoadInputModelFileUsingInputModelLoader・ｽ・ｽO: {ex}");
+                MessageBox.Show($"ファイル読込中にエラーが発生しました。\n{ex.Message}", "読込エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                //System.Diagnostics.Debug.WriteLine($"TryLoadInputModelFileUsingInputModelLoader例外: {ex}");
                 return false;
@@ -1713,4 +1600,4 @@
             if (ofd.ShowDialog() != true) return;
-            // Undo 保存・ｽﾍ茨ｿｽ・ｽS・ｽw情報p情報g・ｽp
-            TrySaveUndoSnapshotSafelyOptimized();
+            // Undo 保存は安全ヘルパを使用
+            TrySaveUndoSnapshotSafely();
             TryLoadInputModelFileUsingInputModelLoader(ofd.FileName);
@@ -1731,70 +1618,16 @@
                 {
-                    // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi読込・ｽO・ｽﾌ擾ｿｽﾔゑｿｽﾛ托ｿｽ・ｽj
+                    // Undoポイントを追加（読込前の状態を保存）
                     _undoManager.SaveState(CurrentInputModel.DeepCopy());
 
-                    string json = File.ReadAllText(openFileDialog.FileName);
-                    var projectData = JsonSerializer.Deserialize<ProjectData>(json, _jsonOptions);
+                    var projectData = _fileOperationService.LoadProjectData(openFileDialog.FileName);
+
                     if (projectData != null)
                     {
-                        this.CurrentInputModel = projectData.InputModel;
-                        this.CurrentModel = projectData.AnaModel;
-
-                        // --- 情報ﾊ会ｿｽ根入部情報b・ｽv根入部 ---
-                        if (CurrentInputModel?.PileGroupSettlement != null)
-                        {
-                            CurrentInputModel.PileGroupSettlement.SettlementSoilLayers =
-                                EnsureObservableCollection(CurrentInputModel.PileGroupSettlement.SettlementSoilLayers);
-                            CurrentInputModel.PileGroupSettlement.RectLoads =
-                                EnsureObservableCollection(CurrentInputModel.PileGroupSettlement.RectLoads);
-                            CurrentInputModel.PileGroupSettlement.SettlementGridX =
-                                EnsureObservableCollection(CurrentInputModel.PileGroupSettlement.SettlementGridX);
-                            CurrentInputModel.PileGroupSettlement.SettlementGridY =
-                                EnsureObservableCollection(CurrentInputModel.PileGroupSettlement.SettlementGridY);
-                            CurrentInputModel.PileGroupSettlement.SettlementGridData =
-                                EnsureObservableCollection(CurrentInputModel.PileGroupSettlement.SettlementGridData);
-                        }
-                        CurrentInputModel.PileLayoutItems =
-                            EnsureObservableCollection(CurrentInputModel.PileLayoutItems);
-                        CurrentInputModel.Elements =
-                            EnsureObservableCollection(CurrentInputModel.Elements);
-                        CurrentInputModel.GridXItems =
-                            EnsureObservableCollection(CurrentInputModel.GridXItems);
-                        CurrentInputModel.GridYItems =
-                            EnsureObservableCollection(CurrentInputModel.GridYItems);
-                        CurrentInputModel.PileBodies =
-                            EnsureObservableCollection(CurrentInputModel.PileBodies);
-                        CurrentInputModel.GroundsInput =
-                            EnsureObservableCollection(CurrentInputModel.GroundsInput);
+                        CurrentInputModel = projectData.InputModel;
+                        CurrentModel = projectData.AnaModel;
 
-                        if (CurrentInputModel?.EmbedmentInput != null)
-                        {
-                            CurrentInputModel.EmbedmentInput.EmbedmentLayers =
-                                EnsureObservableCollection(CurrentInputModel.EmbedmentInput.EmbedmentLayers);
-                        }
-                        if (CurrentInputModel?.LoadCasesInput != null)
-                        {
-                            CurrentInputModel.LoadCasesInput.LoadCasesLevel1 =
-                                EnsureObservableCollection(CurrentInputModel.LoadCasesInput.LoadCasesLevel1);
-                            CurrentInputModel.LoadCasesInput.LoadCasesLevel2 =
-                                EnsureObservableCollection(CurrentInputModel.LoadCasesInput.LoadCasesLevel2);
-                        }
-                        // ・ｽl・ｽX・ｽg情報黷ｽ・ｽR情報N・ｽV根入部
-                        if (CurrentInputModel?.GroundsInput != null)
-                        {
-                            foreach (var ground in CurrentInputModel.GroundsInput)
-                            {
-                                ground.GroundLayers = EnsureObservableCollection(ground.GroundLayers);
-                                ground.GroundMassesData = EnsureObservableCollection(ground.GroundMassesData);
-                            }
-                        }
-                        if (CurrentInputModel?.PileBodies != null)
-                        {
-                            foreach (var pileBody in CurrentInputModel.PileBodies)
-                            {
-                                pileBody.PileBodySegments = EnsureObservableCollection(pileBody.PileBodySegments);
-                            }
-                        }
-                        // --- 情報ﾊ会ｿｽ根入部・ｽﾜゑｿｽ ---
+                        // コレクションを ObservableCollection に変換
+                        _fileOperationService.ConvertToObservableCollections(CurrentInputModel);
 
-                        // ・ｽK・ｽv・ｽﾈゑｿｽv情報p・ｽe・ｽB・ｽﾏ更・ｽﾊ知
+                        // プロパティ変更通知
                         OnPropertyChanged(nameof(CurrentInputModel));
@@ -1803,14 +1636,26 @@
                     {
-                        // ProjectData・ｽﾅなゑｿｽ・ｽ鼾・ｿｽ・ｽz・ｽ閧ｵ・ｽ・ｽ InputModel ・ｽP・ｽﾌで読めるか根入部
+                        // ProjectDataでない場合を想定して InputModel 単体で読めるか試す
                         var ok = TryLoadInputModelFileUsingInputModelLoader(openFileDialog.FileName);
                         if (!ok)
-                            throw new InvalidOperationException("・ｽt・ｽ@・ｽC情報`根入部・ｽs情報ﾅゑｿｽ・ｽBProjectData ・ｽﾅゑｿｽ InputModel ・ｽﾅゑｿｽ根入部ﾜゑｿｽ・ｽ・ｽB");
+                            throw new InvalidOperationException("ファイル形式が不正です。ProjectData でも InputModel でもありません。");
                         return;
                     }
-                    // ・ｽt・ｽH・ｽ[情報o・ｽb・ｽN: null ・ｽﾌ場合・ｽﾍ具ｿｽR情報N・ｽV根入部・ｽﾅ擾ｿｽ根入部
-                    CurrentInputModel.GridXItems ??= new ObservableCollection<GridDataItem>();
-                    CurrentInputModel.GridYItems ??= new ObservableCollection<GridDataItem>();
 
-                    // VM ・ｽﾄア・ｽ^・ｽb・ｽ`
+                    // VM 再アタッチ
                     CurrentInputModel.AttachViewModel(this);
+                    CurrentFilePath = openFileDialog.FileName;
+
+                    // 要素分割・解析状態をリセット
+                    IsElementSplit = false;
+                    IsHorizontalAnalysisDone = false;
+                    IsVerticalAnalysisDone = false;
+                    IsGroupPileSettlementAnalysisDone = false;
+
+                    // M-φキャッシュをクリア（新プロジェクト読込時）
+                    PileSection.ClearMphiCache();
+
+                    // 群杭沈下解析結果をクリア
+                    CurrentInputModel.PileGroupSettlement?.SettlementGridData?.Clear();
+                    CurrentInputModel.PileGroupSettlement?.SettlementGridX?.Clear();
+                    CurrentInputModel.PileGroupSettlement?.SettlementGridY?.Clear();
 
@@ -1826,3 +1671,3 @@
 
-        // Word ・ｽt・ｽ@・ｽC情報ﾉ保托ｿｽ情報驛・ｿｽ\・ｽb・ｽh
+        // Word ファイルに保存するメソッド
         [RelayCommand]
@@ -1842,3 +1687,10 @@
                     doc.CreateWordDocument(CurrentInputModel, saveFileDialog.FileName);
-                    MessageBox.Show($"docs・ｽt・ｽ@・ｽC根入部・ｽ・ｬ根入部ﾜゑｿｽ情報B\n{saveFileDialog.FileName}\nMSWord・ｽﾅフ・ｽ@・ｽC根入部・ｽJ情報Actrl + a・ｽﾅ全・ｽI根入部根入部, F9・ｽﾉゑｿｽ・ｽt・ｽB・ｽ[情報h情報X・ｽV情報ﾄゑｿｽ根入部情報B");
+                    MessageBox.Show($"docxファイルが作成されました。\n{saveFileDialog.FileName}\nMSWordでファイルを開き、ctrl + aで全選択した後, F9によりフィールドを更新してください。");
+
+                    // 作成したdocxファイルを自動的に開く
+                    System.Diagnostics.Process.Start(new System.Diagnostics.ProcessStartInfo
+                    {
+                        FileName = saveFileDialog.FileName,
+                        UseShellExecute = true
+                    });
                 }
@@ -1846,3 +1698,3 @@
                 {
-                    MessageBox.Show($"Word・ｽo・ｽﾍに趣ｿｽ・ｽs情報ﾜゑｿｽ情報B\n{ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                    MessageBox.Show($"Word出力に失敗しました。\n{ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
                 }
@@ -1851,3 +1703,3 @@
 
-        // ・ｽv情報p・ｽe・ｽB・ｽﾌコ・ｽs・ｽ[
+        // プロパティのコピー
         private static void CopyProperties(object source, object destination)
@@ -1874,3 +1726,3 @@
         }
-        // ・ｽv・ｽZ情報o・ｽﾍウ・ｽB情報h・ｽE・ｽ\根入部・ｽ\・ｽb・ｽh
+        // 計算書出力ウィンドウ表示メソッド
         [RelayCommand]
@@ -1885,3 +1737,3 @@
             {
-                MessageBox.Show($"・ｽv・ｽZ情報o・ｽﾍウ・ｽB情報h・ｽE・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show($"計算書出力ウィンドウの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
@@ -1889,3 +1741,3 @@
 
-        // ・ｽI・ｽv・ｽV根入部・ｽ\根入部・ｽ\・ｽb・ｽh
+        // オプション表示メソッド
         [RelayCommand]
@@ -1900,3 +1752,3 @@
             {
-                MessageBox.Show($"・ｽI・ｽv・ｽV根入部・ｽE・ｽB情報h・ｽE・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show($"オプションウィンドウの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
@@ -1904,3 +1756,3 @@
 
-        // ・ｽ・ｽﾍ鯉ｿｽ・ｽﾊゑｿｽ1・ｽﾂでゑｿｽ情報ﾝゑｿｽ・ｽ驍ｩ
+        // 解析結果が1つでも存在するか
         private bool HasAnyAnalysisResult()
@@ -1908,3 +1760,3 @@
 
-        // ・ｽR・ｽ}情報h・ｽ・ｽﾔ一括・ｽX・ｽV・ｽw情報p
+        // コマンド状態一括更新ヘルパ
         private void RaiseResultCommandsCanExecute()
@@ -1936,3 +1788,3 @@
             {
-                MessageBox.Show($"・ｽO情報t・ｽE・ｽB情報h・ｽE・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show($"グラフウィンドウの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
@@ -1942,10 +1794,10 @@
 
-        // ・ｽt・ｽB・ｽ[情報h・ｽﾇ会ｿｽ
+        // フィールド追加
         private readonly AnalysisResultTableService _tableService = new();
 
-        // ・ｽv情報p・ｽe・ｽB
-        public IReadOnlyList<ResultTable> LatestResultTables { get; private set; } = Array.Empty<ResultTable>();
+        // プロパティ
+        public IReadOnlyList<ResultTable> LatestResultTables { get; private set; } = [];
 
         // Latest analysis logs
-        public IReadOnlyList<string> LatestAnalysisLogs { get; private set; } = Array.Empty<string>();
+        public ObservableCollection<string> LatestAnalysisLogs { get; private set; } = [];
 
@@ -1953,3 +1805,5 @@
         {
-            LatestAnalysisLogs = logs ?? Array.Empty<string>();
+            LatestAnalysisLogs.Clear();
+            foreach (var log in logs)
+                LatestAnalysisLogs.Add(log);
             OnPropertyChanged(nameof(LatestAnalysisLogs));
@@ -1958,6 +1812,6 @@
 
-        // 解析結果 (ログウィンドウ表示に追加)
+        // 解析完了後 (既存処理内末尾に追加)
         private void OnAnalysisFinished(AnalysisStepResult result)
         {
-            // AnaModel 根入部・ｽZ・ｽb・ｽg・ｽﾈら結・ｽﾊテ・ｽ[・ｽu根入部根入部・ｽX・ｽL・ｽb・ｽv
+            // AnaModel が未セットなら結果テーブル生成をスキップ
             if (CurrentModel == null)
@@ -1994,3 +1848,3 @@
             {
-                MessageBox.Show($"Error displaying table window: {ex.Message}", "Error", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show($"テーブルウィンドウの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
@@ -2001,13 +1855,18 @@
         {
-            try
-            {
-                if (!CanOpenLogWindow()) return;
-                var vm = new LogWindowViewModel(LatestAnalysisLogs);
-                var w = new Views.LogWindow { DataContext = vm };
-                w.Show();
-            }
-            catch (Exception ex)
-            {
-                MessageBox.Show($"Error displaying log window: {ex.Message}", "Error", MessageBoxButton.OK, MessageBoxImage.Error);
-            }
+            if (!CanOpenLogWindow()) return;
+            var vm = new LogWindowViewModel(LatestAnalysisLogs);
+            var w = new Views.LogWindow { DataContext = vm };
+            w.Show();
+
+            //try
+            //{
+            //    if (!CanOpenLogWindow()) return;
+            //    var vm = new LogWindowViewModel(LatestAnalysisLogs);
+            //    var w = new Views.LogWindow { DataContext = vm };
+            //    w.Show();
+            //}
+            //catch (Exception ex)
+            //{
+            //    MessageBox.Show($"ログウィンドウの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+            //}
         }
@@ -2016,6 +1875,6 @@
 
-        // 解析結果テーブルで生成
+        // 解析結果テーブル再生成
         public void RefreshResultTablesFromLastStep()
         {
-            // AnaModel ・ｽﾜゑｿｽ・ｽ・ｽ AnalysisStepResults ・ｽ・ｽ null/・ｽ・ｽﾌ場合・ｽﾍ托ｿｽ根入部・ｽ^・ｽ[・ｽ・ｽ
+            // AnaModel または AnalysisStepResults が null/空の場合は早期リターン
             if (CurrentModel == null ||
@@ -2031,18 +1890,50 @@
 
-            // ・ｽﾅ終・ｽX・ｽe・ｽb・ｽv情報ﾊゑｿｽ・ｽ謫ｾ
-            var last = CurrentModel.AnalysisStepResults.LastOrDefault();
-            if (last == null)
+            // デバッグ: AnalysisStepResultsの内容を確認
+            //System.Diagnostics.Debug.WriteLine($"=== RefreshResultTablesFromLastStep ===");
+            //System.Diagnostics.Debug.WriteLine($"AnalysisStepResults.Count = {CurrentModel.AnalysisStepResults.Count}");
+            foreach (var r in CurrentModel.AnalysisStepResults)
+            {
+                //System.Diagnostics.Debug.WriteLine($"  LoadCase={r.LoadCase?.LoadName}, LoadComb={r.LoadCombination?.Name}, IsLiq={r.IsLiquefaction}, Step={r.Step}");
+            }
+
+            // 全ての解析結果から一意の組み合わせ（LoadCase, LoadCombination, IsLiquefaction）を取得
+            // 各組み合わせについて最終ステップのテーブルを生成
+            var allTables = new List<ResultTable>();
+
+            var uniqueCombinations = CurrentModel.AnalysisStepResults
+                .GroupBy(r => new
+                {
+                    LoadCaseName = r.LoadCase?.LoadName ?? "",
+                    LoadCombinationName = r.LoadCombination?.Name ?? "",
+                    r.IsLiquefaction
+                })
+                .Select(g => g.OrderByDescending(r => r.Step).First()) // 各組み合わせの最終ステップを取得
+                .ToList();
+
+            //System.Diagnostics.Debug.WriteLine($"uniqueCombinations.Count = {uniqueCombinations.Count}");
+            foreach (var c in uniqueCombinations)
             {
-                LatestResultTables = [];
-                OnPropertyChanged(nameof(LatestResultTables));
-                RaiseResultCommandsCanExecute();
-                return;
+                //System.Diagnostics.Debug.WriteLine($"  UniqueComb: LoadCase={c.LoadCase?.LoadName}, LoadComb={c.LoadCombination?.Name}, IsLiq={c.IsLiquefaction}, Step={c.Step}");
             }
 
-            LatestResultTables = _tableService.BuildTables(
-                CurrentModel,
-                last.LoadCase,
-                last.LoadCombination,
-                last.IsLiquefaction,
-                last.Step);
+            foreach (var stepResult in uniqueCombinations)
+            {
+                var tables = _tableService.BuildTables(
+                    CurrentModel,
+                    stepResult.LoadCase,
+                    stepResult.LoadCombination,
+                    stepResult.IsLiquefaction,
+                    stepResult.Step);
+
+                //System.Diagnostics.Debug.WriteLine($"  Built {tables.Count} tables for IsLiq={stepResult.IsLiquefaction}");
+                allTables.AddRange(tables);
+            }
+
+            //System.Diagnostics.Debug.WriteLine($"Total tables: {allTables.Count}");
+            foreach (var t in allTables)
+            {
+                //System.Diagnostics.Debug.WriteLine($"  Table: {t.Name}, IsLiq={t.IsLiquefaction}");
+            }
+
+            LatestResultTables = allTables;
 
@@ -2052,3 +1943,3 @@
 
-        // ・ｽw情報v情報E・ｽB情報h・ｽE・ｽ\根入部・ｽ\・ｽb・ｽh
+        // ヘルプメウィンドウ表示メソッド
         [RelayCommand]
@@ -2063,3 +1954,3 @@
             {
-                MessageBox.Show($"・ｽw情報v・ｽE・ｽB情報h・ｽE・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show($"ヘルプウィンドウの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
@@ -2073,4 +1964,4 @@
 
-        // ・ｽﾄ難ｿｽ・ｽh・ｽ~・ｽt情報O・ｽi情報q・ｽI・ｽﾉ茨ｿｽ情報j
-        private int _isChangWindowOpeningFlag = 0;
+        // 再入防止フラグ（原子的に扱う）
+        private readonly int _isChangWindowOpeningFlag = 0;
 
@@ -2079,3 +1970,3 @@
         {
-            // ChangViewModel ・ｽﾉ鯉ｿｽ・ｽﾝゑｿｽ InputModel ・ｽ酷根入部ﾄ作成
+            // ChangViewModel に現在の InputModel を注入して作成
             var vm = new ChangViewModel(CurrentInputModel);
@@ -2084,3 +1975,3 @@
 
-            // ・ｽC・ｽx情報g・ｽn情報h根入部ﾝ抵ｿｽ
+            // イベントハンドラを設定
             if (vm is ICloseable closeableViewModel)
@@ -2093,4 +1984,4 @@
             {
-                // ・ｽ・ｽ ・ｽd・ｽv: ・ｽ_・ｽC・ｽA情報O情報J情報O・ｽﾉ鯉ｿｽ・ｽﾝのフ・ｽH・ｽ[・ｽJ・ｽX情報N情報A
-                // 根入部ﾉゑｿｽ・ｽ IME/TextStore 根入部根入部・ｽACOMException 根入部・ｽ・ｽﾅゑｿｽ・ｽ・ｽ
+                // ★ 重要: ダイアログを開く前に現在のフォーカスをクリア
+                // これにより IME/TextStore が解放され、COMException を回避できる
                 var focusedElement = Keyboard.FocusedElement;
@@ -2098,6 +1989,6 @@
                 {
-                    // ・ｽt・ｽH・ｽ[・ｽJ・ｽX・ｽ・ｽ MainWindow ・ｽﾉ移難ｿｽ
+                    // フォーカスを MainWindow に移動
                     Application.Current.MainWindow?.Focus();
 
-                    // Dispatcher ・ｽ・ｽ UI 情報X・ｽV根入部 IME 根入部根入部骼橸ｿｽﾔゑｿｽ^根入部
+                    // Dispatcher で UI を更新して IME を解放する時間を与える
                     Application.Current.Dispatcher.Invoke(
@@ -2111,6 +2002,6 @@
             {
-                MessageBox.Show($"・ｽ_・ｽC・ｽA情報O・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show($"ダイアログの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
 
-            // 変更済（追加の箇所で適用）
+            // 変更後（以下の箇所で適用）
             UpdateWindowImmediate();
@@ -2120,3 +2011,3 @@
         [RelayCommand]
-        public void OpenPileSectionLibraryWindow()
+        public static void OpenPileSectionLibraryWindow()
         {
@@ -2132,3 +2023,3 @@
             {
-                System.Windows.MessageBox.Show($"Error displaying pile library window: {ex.Message}", "Error", System.Windows.MessageBoxButton.OK, System.Windows.MessageBoxImage.Error);
+                System.Windows.MessageBox.Show($"杭ライブラリ表示に失敗しました: {ex.Message}", "エラー", System.Windows.MessageBoxButton.OK, System.Windows.MessageBoxImage.Error);
             }
@@ -2136,84 +2027,2 @@
 
-        //{
-        //    // 0 -> 1 ・ｽﾉでゑｿｽ・ｽﾈゑｿｽ・ｽ・ｽﾎ奇ｿｽ・ｽﾉ趣ｿｽ・ｽs情報ﾆみなゑｿｽ
-        //    if (System.Threading.Interlocked.CompareExchange(ref _isChangWindowOpeningFlag, 1, 0) != 0)
-        //    {
-        //        Debug.WriteLine("OpenChangWindow: already opening, ignored.");
-        //        return;
-        //    }
-
-        //    try
-        //    {
-        //        Debug.WriteLine("OpenChangWindow: start");
-
-        //        // ChangViewModel ・ｽﾉ鯉ｿｽ・ｽﾝゑｿｽ InputModel ・ｽ酷根入部ﾄウ・ｽB情報h・ｽE・ｽｶ撰ｿｽ・ｽE・ｽ\・ｽ・ｽ
-        //        var vm = new ChangViewModel(this.CurrentInputModel);
-        //        var win = new ChangWindow
-        //        {
-        //            DataContext = vm,
-        //            Owner = Application.Current.MainWindow,
-        //            WindowStartupLocation = WindowStartupLocation.CenterOwner,
-        //            ShowInTaskbar = false
-        //        };
-
-        //        // ViewModel 根入部ﾂゑｿｽ・ｽ・ｽv根入部・ｽｯ趣ｿｽ・ｽ鼾・ｿｽ根入部・ｽﾈゑｿｽw・ｽﾇ（・ｽC情報^・ｽ[・ｽt・ｽF・ｽC・ｽX情報ﾍ環具ｿｽ・ｽﾉ搾ｿｽ・ｽ墲ｹ・ｽﾄ）
-        //        if (vm is ICloseable closeableVm)
-        //        {
-        //            closeableVm.RequestClose += (s, e) =>
-        //            {
-        //                if (win.IsVisible) win.Close();
-        //            };
-        //        }
-
-        //        // 情報[・ｽ_情報\情報i根入部・ｽR・ｽ[・ｽh・ｽﾉ搾ｿｽ・ｽ墲ｹ・ｽ・ｽj
-        //        try
-        //        {
-        //            win.ShowDialog();
-        //        }
-        //        catch (Exception ex)
-        //        {
-        //            MessageBox.Show($"Chang・ｽE・ｽB情報h・ｽE・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
-        //            Debug.WriteLine(ex);
-        //        }
-        //    }
-        //    finally
-        //    {
-        //        System.Threading.Interlocked.Exchange(ref _isChangWindowOpeningFlag, 0);
-        //        Debug.WriteLine("OpenChangWindow: end");
-        //    }
-        //}
-        //{
-        //    var vm = new ChangViewModel();
-        //    //vm.LoadSample();
-        //    var win = new ChangWindow { DataContext = vm };
-        //    win.ShowDialog();
-        //}
-
-        //[RelayCommand]
-        //private void DeleteAnalysisResults()
-        //{
-        //    var result = MessageBox.Show(
-        //        "・ｽ・ｽﾍ鯉ｿｽ・ｽﾊゑｿｽ・ｽ尞懶ｿｽ・ｽ・ｽﾜゑｿｽ・ｽB・ｽ・ｽ・ｵ情報ﾅゑｿｽ情報H",
-        //        "確認",
-        //        MessageBoxButton.YesNo,
-        //        MessageBoxImage.Question);
-        //    if (result == MessageBoxResult.Yes)
-        //    {
-        //        // ・ｽ・ｽﾍ鯉ｿｽ・ｽﾊの削除
-        //        IsHorizontalAnalysisDone = false;
-        //        IsVerticalAnalysisDone = false;
-        //        IsGroupPileSettlementAnalysisDone = false;
-        //        if (CurrentInputModel != null)
-        //        {
-        //            //foreach (var pile in CurrentInputModel.PileLayoutItems)
-        //            //{
-        //            //    pile.ResetAnalysisResults();
-        //            //}
-        //        }
-        //        CurrentModel = null; // AnaModel根入部・ｽZ・ｽb・ｽg
-        //        UpdateWindowAction?.Invoke(); // ・ｽf情報Q・ｽ[・ｽg・ｽ・ｽﾊゑｿｽ・ｽﾄコ・ｽ[・ｽh・ｽr・ｽn・ｽC情報h・ｽﾌ・ｿｽ・ｽ\・ｽb・ｽh情報ﾄび出・ｽ・ｽ
-        //        UpdateTreeView();
-        //    }
-        //}
-
         [RelayCommand]
@@ -2233,3 +2042,3 @@
             {
-                MessageBox.Show($"・ｽV情報[・ｽg・ｽJ・ｽb・ｽg・ｽ齬暦ｿｽE・ｽB情報h・ｽE・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                MessageBox.Show($"ショートカット一覧ウィンドウの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
@@ -2237,9 +2046,2 @@
 
-        // ・ｽf・ｽ[・ｽ^情報ﾍウ・ｽB情報h・ｽE・ｽ\根入部・ｽ\・ｽb・ｽh
-        //[RelayCommand]
-        //private void OpenInputDataAnchorable()
-        //{
-        //    InputDataAnchorable.Show();
-        //}
-
         [RelayCommand]
@@ -2249,3 +2051,3 @@
             {
-                // ・ｽI・ｽ・ｽﾟ点情報ﾈゑｿｽ・ｽ鼾・ｿｽﾍ擾ｿｽ情報・~情報ﾄ・ｿｽ・ｽb・ｽZ・ｽ[・ｽW・ｽ\・ｽ・ｽ
+                // 選択節点がない場合は処理を中止してメッセージ表示
                 if (CurrentInputModel == null ||
@@ -2254,3 +2056,3 @@
                 {
-                    MessageBox.Show("杭配置情報I根入部・ｽ・ｽﾄゑｿｽ・ｽﾜゑｿｽ・ｽ・ｽB", "確認", MessageBoxButton.OK, MessageBoxImage.Information);
+                    MessageBox.Show("杭配置が選択されていません。", "確認", MessageBoxButton.OK, MessageBoxImage.Information);
                     return;
@@ -2258,6 +2060,6 @@
 
-                // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ
+                // Undoポイントを追加
                 _undoManager.SaveState(CurrentInputModel.DeepCopy());
 
-                // MoveWindow情報C情報X・ｽ^情報X根入部・ｽﾄ表・ｽ・ｽ
+                // MoveWindowをインスタンス化して表示
                 MoveCopyWindow moveCopyWindow = new();
@@ -2265,30 +2067,43 @@
                 var tcs = new TaskCompletionSource<bool>();
+                bool operationExecuted = false;
+
                 moveCopyWindow.MoveCopyCompleted += async (sender, e) =>
                 {
+                    operationExecuted = true;
                     await MoveCopyWindow_MoveCopyCompletedAsync(sender, e);
-                    tcs.SetResult(true);
+                    tcs.TrySetResult(true);
                 };
 
-                moveCopyWindow.ShowDialog(); // 情報[・ｽ_情報_・ｽC・ｽA情報O・ｽﾆゑｿｽ・ｽﾄ表・ｽ・ｽ
-
-                // ・ｽ・ｽ ・ｽﾒ機・ｽJ・ｽ[・ｽ\根入部\・ｽ・ｽ
-                Mouse.OverrideCursor = Cursors.Wait;
-                try
+                // ウィンドウが閉じられたら（キャンセル含む）TaskCompletionSourceを完了させる
+                moveCopyWindow.Closed += (sender, e) =>
                 {
-                    await tcs.Task; // ・ｽｯ奇ｿｽ・ｽﾉ奇ｿｽ根入部ﾒゑｿｽ
+                    tcs.TrySetResult(false);
+                };
 
-                    // コレクション自体の変更通知
-                    OnPropertyChanged(nameof(GroupPileSettlementXMin));
-                    OnPropertyChanged(nameof(GroupPileSettlementXMax));
-                    OnPropertyChanged(nameof(GroupPileSettlementYMin));
-                    OnPropertyChanged(nameof(GroupPileSettlementYMax));
+                moveCopyWindow.ShowDialog(); // モーダルダイアログとして表示
 
-                    // 変更: デバウンスで更新
-                    RequestUpdateWindow();
-                    UpdateTreeView();
-                }
-                finally
+                // 操作が実行された場合のみ待機と更新を行う
+                if (operationExecuted)
                 {
-                    // ・ｽ・ｽ ・ｽJ・ｽ[・ｽ\根入部情報ﾉ戻ゑｿｽ
-                    Mouse.OverrideCursor = null;
+                    // ★ 待機カーソルを表示
+                    Mouse.OverrideCursor = Cursors.Wait;
+                    try
+                    {
+                        await tcs.Task; // 非同期に完了を待つ
+
+                        // コレクション自体の変更通知
+                        OnPropertyChanged(nameof(GroupPileSettlementXMin));
+                        OnPropertyChanged(nameof(GroupPileSettlementXMax));
+                        OnPropertyChanged(nameof(GroupPileSettlementYMin));
+                        OnPropertyChanged(nameof(GroupPileSettlementYMax));
+
+                        // 変更: デバウンス付きで更新
+                        RequestUpdateWindow();
+                        UpdateTreeView();
+                    }
+                    finally
+                    {
+                        // ★ カーソルを元に戻す
+                        Mouse.OverrideCursor = null;
+                    }
                 }
@@ -2297,3 +2112,5 @@
             {
-                MessageBox.Show($"杭体移難ｿｽ・ｽE根入部情報ﾉエ情報[根入部根入部・ｽﾜゑｿｽ情報B\n{ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                // 例外発生時もカーソルをリセット
+                Mouse.OverrideCursor = null;
+                MessageBox.Show($"杭の移動・複製中にエラーが発生しました。\n{ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
             }
@@ -2303,70 +2120,12 @@
         {
-            // ・ｽV根入部・ｽE・ｽB情報h・ｽE・ｽﾅの托ｿｽ・ｽ・ｽﾌ鯉ｿｽ・ｽﾊゑｿｽ根入部根入部
+            // 新しいウィンドウでの操作の結果を処理する
             if (e.IsMove)
-                MoveNodes(e.DX, e.DY);// ・ｽﾚ難ｿｽ根入部ﾌ擾ｿｽ・ｽ・ｽ
+                MoveNodes(e.DX, e.DY);// 移動操作の処理
             else if (e.IsCopy)
-                await CopyNodesAsync(e.DX, e.DY, e.RepetitionNumber); // 根入部根入部ﾌ擾ｿｽ・ｽ・ｽ
+                await CopyNodesAsync(e.DX, e.DY, e.RepetitionNumber); // 複製操作の処理
         }
 
-        //private async Task CopyNodesAsync(double dX, double dY, int repetitionNumber)
-        //{
-        //    // ・ｽﾏ更情報s情報O・ｽﾉ、・ｽI根入部・ｽ黷ｽ・ｽA・ｽC・ｽe情報ﾌ・ｿｽ・ｽX・ｽg情報・ｬ
-        //    var selectedItems = CurrentInputModel.PileLayoutItems.Where(p => p.IsSelected).ToList();
-        //    int totalCount = selectedItems.Count * repetitionNumber;
-
-        //    // ・ｽ・ｽ ・ｽ・ｽﾊコ・ｽs・ｽ[情報ﾍ待機・ｽJ・ｽ[・ｽ\根入部\・ｽ・ｽ
-        //    bool showWaitCursor = totalCount > 10;
-        //    if (showWaitCursor)
-        //        Mouse.OverrideCursor = Cursors.Wait;
-
-        //    try
-        //    {
-        //        // ・ｽ・ｽ ・ｽ齊橸ｿｽ・ｽ・ｽX・ｽg・ｽﾉ趣ｿｽ・ｽO・ｽﾉゑｿｽ・ｽﾗて作成・ｽi・ｽe・ｽﾊゑｿｽ情報O・ｽm・ｽﾛゑｿｽ・ｽﾄ搾ｿｽ根入部・ｽj
-        //        var newItems = new List<PileLayoutDataItem>(totalCount);
-
-        //        foreach (var pileLocation in selectedItems)
-        //        {
-        //            for (int i = 0; i < repetitionNumber; i++)
-        //            {
-        //                var newItem = new PileLayoutDataItem
-        //                {
-        //                    X = pileLocation.X + dX * (i + 1),
-        //                    Y = pileLocation.Y + dY * (i + 1)
-        //                };
-        //                newItem.SetMainWindowViewModel(this);
-        //                newItems.Add(newItem);
-        //            }
-        //            pileLocation.IsSelected = false;
-        //        }
-
-        //        // ・ｽ・ｽ UI・ｽX情報b・ｽh・ｽﾅ一括・ｽu情報iCollectionChanged・ｽ・ｽ1・ｽｾゑｿｽ情報ﾎ）
-        //        await Application.Current.Dispatcher.InvokeAsync(() =>
-        //        {
-        //            // 根入部・ｽA・ｽC・ｽe情報ﾆ新・ｽK・ｽA・ｽC・ｽe根入部根入部根入部・ｽV根入部・ｽR情報N・ｽV根入部情報・ｬ
-        //            var combined = new ObservableCollection<PileLayoutDataItem>(
-        //                CurrentInputModel.PileLayoutItems.Concat(newItems));
-
-        //            // ・ｽR情報N・ｽV根入部・ｽS・ｽﾌゑｿｽu情報iCollectionChanged・ｽ・ｽ1・ｽ・ｽﾌみ）
-        //            CurrentInputModel.PileLayoutItems = combined;
-
-        //            // SoilPiles ・ｽ・ｽ1・ｽｾゑｿｽ・ｽﾄ撰ｿｽ・ｽ・ｽ
-        //            if (!IsElementSplit)
-        //                RequestGenerateSoilPiles();
-
-        //            UpdatePileLayoutNo();
-        //            RequestUpdateWindow();
-        //            UpdateTreeView();
-        //        });
-        //    }
-        //    finally
-        //    {
-        //        if (showWaitCursor)
-        //            Mouse.OverrideCursor = null;
-        //    }
-        //}
-
         private async Task CopyNodesAsync(double dX, double dY, int repetitionNumber)
         {
-            var ct = GetLongOperationCancellationToken();
-            // ・ｽI根入部・ｽ黷ｽ・ｽA・ｽC・ｽe情報ﾌ・ｿｽ・ｽX・ｽg情報・ｬ
+            // 変更を行う前に、選択されたアイテムのリストを作成
             var selectedItems = CurrentInputModel.PileLayoutItems.Where(p => p.IsSelected).ToList();
@@ -2374,2 +2133,3 @@
 
+            // ★ 大量コピー時は待機カーソルを表示
             bool showWaitCursor = totalCount > 10;
@@ -2380,32 +2140,17 @@
             {
-                var newItems = new List<PileLayoutDataItem>(totalCount);
-
-                foreach (var pileLocation in selectedItems)
-                {
-                    ct.ThrowIfCancellationRequested();
+                // サービスを使ってコピー実行
+                var combined = _pileLayoutService.CopySelectedPiles(
+                    CurrentInputModel.PileLayoutItems,
+                    dX,
+                    dY,
+                    repetitionNumber,
+                    item => item.SetMainWindowViewModel(this));
 
-                    for (int i = 0; i < repetitionNumber; i++)
-                    {
-                        ct.ThrowIfCancellationRequested();
-
-                        var newItem = new PileLayoutDataItem
-                        {
-                            X = pileLocation.X + dX * (i + 1),
-                            Y = pileLocation.Y + dY * (i + 1)
-                        };
-                        newItem.SetMainWindowViewModel(this);
-                        newItems.Add(newItem);
-                    }
-
-                    // ・ｽe情報A・ｽC・ｽe根入部・ｽﾆに選根入部・ｽ・ｽ
-                    pileLocation.IsSelected = false;
-                }
-
-                // Dispatcher ・ｽﾄび出情報O・ｽﾉ最終・ｽ`・ｽF・ｽb・ｽN
-                ct.ThrowIfCancellationRequested();
+                // ★ UIスレッドで一括置換（CollectionChangedを1回だけ発火）
                 await Application.Current.Dispatcher.InvokeAsync(() =>
                 {
-                    var combined = new ObservableCollection<PileLayoutDataItem>(CurrentInputModel.PileLayoutItems.Concat(newItems));
+                    // コレクション全体を置換（CollectionChangedは1回のみ）
                     CurrentInputModel.PileLayoutItems = combined;
 
+                    // SoilPiles を1回だけ再生成
                     if (!IsElementSplit)
@@ -2414,14 +2159,5 @@
                     UpdatePileLayoutNo();
-                    RequestUpdateWindow();
-                    UpdateTreeView();
+                    NotifyUIChanged();
                 });
             }
-            catch (OperationCanceledException)
-            {
-                StatusMessage = "根入部根入部情報L根入部・ｽZ根入部・ｽﾜゑｿｽ情報B";
-            }
-            catch (Exception ex)
-            {
-                MessageBox.Show($"杭体包ｿｽ根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ情報B\n{ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
-            }
             finally
@@ -2433,45 +2169,17 @@
 
-        // ・ｽﾚ難ｿｽ根入部・ｽ・ｽs・ｽ・ｽ
+        // 移動操作を行う
         private void MoveNodes(double dX, double dY)
         {
-            foreach (PileLayoutDataItem pileLocation in CurrentInputModel.PileLayoutItems)
-            {
-                if (pileLocation.IsSelected)
-                {
-                    pileLocation.X += dX;
-                    pileLocation.Y += dY;
-                    pileLocation.IsSelected = false;
-                }
-            }
+            _pileLayoutService.MoveSelectedPiles(CurrentInputModel.PileLayoutItems, dX, dY);
         }
 
-        // ・ｽR・ｽs・ｽ[情報・ｬ情報ﾄ托ｿｽ根入部s・ｽ・ｽ
-        // ・ｽR・ｽs・ｽ[情報・ｬ情報ﾄ托ｿｽ根入部s・ｽ・ｽ
+        // コピーを作成して操作を行う
         private void CopyNodes(double dX, double dY, int repetitionNumber)
         {
-            var selectedItems = CurrentInputModel.PileLayoutItems.Where(p => p.IsSelected).ToList();
-            int totalCount = selectedItems.Count * repetitionNumber;
-
-            // ・ｽ・ｽ ・ｽ齊橸ｿｽ・ｽ・ｽX・ｽg・ｽﾉ趣ｿｽ・ｽO・ｽﾉゑｿｽ・ｽﾗて作成
-            var newItems = new List<PileLayoutDataItem>(totalCount);
-
-            foreach (var pileLocation in selectedItems)
-            {
-                for (int i = 0; i < repetitionNumber; i++)
-                {
-                    var newItem = new PileLayoutDataItem
-                    {
-                        X = pileLocation.X + dX * (i + 1),
-                        Y = pileLocation.Y + dY * (i + 1)
-                    };
-                    newItem.SetMainWindowViewModel(this);
-                    newItems.Add(newItem);
-                }
-                pileLocation.IsSelected = false;
-            }
-
-            // ・ｽ・ｽ ・ｽ鼕・ｿｽu・ｽ・ｽ
-            var combined = new ObservableCollection<PileLayoutDataItem>(
-                CurrentInputModel.PileLayoutItems.Concat(newItems));
-            CurrentInputModel.PileLayoutItems = combined;
+            CurrentInputModel.PileLayoutItems = _pileLayoutService.CopySelectedPiles(
+                CurrentInputModel.PileLayoutItems,
+                dX,
+                dY,
+                repetitionNumber,
+                item => item.SetMainWindowViewModel(this));
 
@@ -2480,3 +2188,3 @@
 
-        // 杭配置・ｽﾌ編集・ｽE・ｽﾇ会ｿｽ・ｽR・ｽ}情報h
+        // 杭配置の編集・追加コマンド
         [RelayCommand]
@@ -2489,3 +2197,3 @@
             editPileLayoutWindow.ShowDialog();
-            // ・ｽﾏ更: ・ｽ_・ｽC・ｽA情報O・ｽ・ｽﾍ托ｿｽ根入部・ｽs
+            // 変更: ダイアログ後は即時実行
             UpdateWindowImmediate();
@@ -2495,87 +2203,61 @@
         {
-            ObservableCollection<PileLayoutDataItem> selectedItems = [];
-
-            foreach (PileLayoutDataItem pileLocation in CurrentInputModel.PileLayoutItems)
-            {
-                if (pileLocation.IsSelected)
-                    selectedItems.Add(pileLocation);
-            }
-
-            if (e.IsApplicablePileRefNo)
-                foreach (var selectedItem in selectedItems)
-                    selectedItem.PileBodyNo = e.SelectedPileRefNo;
-
-            if (e.IsApplicableGroundRefNo)
-                foreach (var selectedItem in selectedItems)
-                    selectedItem.GroundNo = e.SelectedGroundRefNo;
-
-            if (e.IsApplicablePileTopLevel)
+            var options = new PileLayoutService.BulkEditOptions
             {
-                bool isAdd = e.IsAddPileTopLevel;
-                foreach (var selectedItem in selectedItems)
-                {
-                    selectedItem.X = selectedItem.Point3D.X;
-                    selectedItem.Y = selectedItem.Point3D.Y;
-                    selectedItem.Z = isAdd ? selectedItem.Point3D.Z + e.PileTopLevel : e.PileTopLevel;
-                }
-            }
-
-            if (e.IsApplicablePileGroupFactor)
-            {
-                bool isAdd = e.IsAddPileGroupFactor;
-                foreach (var selectedItem in selectedItems)
-                    selectedItem.GroupPileFactor = isAdd ? selectedItem.GroupPileFactor + e.PileGroupFactor : e.PileGroupFactor;
-            }
-
-            if (e.IsApplicableVL)
-            {
-                bool isAdd = e.IsAddVL;
-                foreach (var selectedItem in selectedItems)
-                    selectedItem.AxialForceVL0 = isAdd ? selectedItem.AxialForceVL0 + e.VL : e.VL;
-            }
+                ApplyPileBodyNo = e.IsApplicablePileRefNo,
+                PileBodyNo = e.SelectedPileRefNo,
 
-            if (e.IsApplicableVLadd)
-            {
-                bool isAdd = e.IsAddVLadd;
-                foreach (var selectedItem in selectedItems)
-                    selectedItem.AxialForceVLAdditional = isAdd ? selectedItem.AxialForceVLAdditional + e.VLadd : e.VLadd;
-            }
+                ApplyGroundNo = e.IsApplicableGroundRefNo,
+                GroundNo = e.SelectedGroundRefNo,
 
-            // E1_1 ~ E2_4 ・ｽﾌ擾ｿｽ情報i・ｽﾈ暦ｿｽ情報j
-            Action<bool, bool, double, int, bool> applyLevel1 = (applicable, isAdd, val, idx, _) =>
-            {
-                if (!applicable) return;
-                foreach (var item in selectedItems)
-                    item.AxialForceLevel1s[idx] = isAdd ? item.AxialForceLevel1s[idx] + val : val;
+                ApplyPileTopLevel = e.IsApplicablePileTopLevel,
+                IsAddPileTopLevel = e.IsAddPileTopLevel,
+                PileTopLevel = e.PileTopLevel,
+
+                ApplyPileGroupFactor = e.IsApplicablePileGroupFactor,
+                IsAddPileGroupFactor = e.IsAddPileGroupFactor,
+                PileGroupFactor = e.PileGroupFactor,
+
+                ApplyAxialForceVL = e.IsApplicableVL,
+                IsAddAxialForceVL = e.IsAddVL,
+                AxialForceVL = e.VL,
+
+                ApplyAxialForceVLAdditional = e.IsApplicableVLadd,
+                IsAddAxialForceVLAdditional = e.IsAddVLadd,
+                AxialForceVLAdditional = e.VLadd,
+
+                ApplyLevel1 =
+                [
+                    e.IsApplicableE1_1, e.IsApplicableE1_2, e.IsApplicableE1_3, e.IsApplicableE1_4
+                ],
+                IsAddLevel1 =
+                [
+                    e.IsAddE1_1, e.IsAddE1_2, e.IsAddE1_3, e.IsAddE1_4
+                ],
+                Level1Values =
+                [
+                    e.E1_1, e.E1_2, e.E1_3, e.E1_4
+                ],
+
+                ApplyLevel2 =
+                [
+                    e.IsApplicableE2_1, e.IsApplicableE2_2, e.IsApplicableE2_3, e.IsApplicableE2_4
+                ],
+                IsAddLevel2 =
+                [
+                    e.IsAddE2_1, e.IsAddE2_2, e.IsAddE2_3, e.IsAddE2_4
+                ],
+                Level2Values =
+                [
+                    e.E2_1, e.E2_2, e.E2_3, e.E2_4
+                ]
             };
 
-            Action<bool, bool, double, int, bool> applyLevel2 = (applicable, isAdd, val, idx, _) =>
-            {
-                if (!applicable) return;
-                foreach (var item in selectedItems)
-                    item.AxialForceLevel2s[idx] = isAdd ? item.AxialForceLevel2s[idx] + val : val;
-            };
+            _pileLayoutService.BulkEditSelectedPiles(CurrentInputModel.PileLayoutItems, options);
 
-            applyLevel1(e.IsApplicableE1_1, e.IsAddE1_1, e.E1_1, 0, false);
-            applyLevel1(e.IsApplicableE1_2, e.IsAddE1_2, e.E1_2, 1, false);
-            applyLevel1(e.IsApplicableE1_3, e.IsAddE1_3, e.E1_3, 2, false);
-            applyLevel1(e.IsApplicableE1_4, e.IsAddE1_4, e.E1_4, 3, false);
-
-            applyLevel2(e.IsApplicableE2_1, e.IsAddE2_1, e.E2_1, 0, false);
-            applyLevel2(e.IsApplicableE2_2, e.IsAddE2_2, e.E2_2, 1, false);
-            applyLevel2(e.IsApplicableE2_3, e.IsAddE2_3, e.E2_3, 2, false);
-            applyLevel2(e.IsApplicableE2_4, e.IsAddE2_4, e.E2_4, 3, false);
-
-            if (e.IsApplicableIsFrontPile1)
-                foreach (var item in selectedItems)
-                    item.IsFrontPiles[0] = e.IsFrontPile1;
-            if (e.IsApplicableIsFrontPile2)
-                foreach (var item in selectedItems)
-                    item.IsFrontPiles[1] = e.IsFrontPile2;
-            if (e.IsApplicableIsFrontPile3)
-                foreach (var item in selectedItems)
-                    item.IsFrontPiles[2] = e.IsFrontPile3;
-            if (e.IsApplicableIsFrontPile4)
-                foreach (var item in selectedItems)
-                    item.IsFrontPiles[3] = e.IsFrontPile4;
+            // IsFrontPile フラグの処理
+            var selectedItems = CurrentInputModel.PileLayoutItems.Where(p => p.IsSelected).ToList();
+            ApplyIsFrontPileFlags(
+                selectedItems,
+                [e.IsApplicableIsFrontPile1, e.IsApplicableIsFrontPile2, e.IsApplicableIsFrontPile3, e.IsApplicableIsFrontPile4],
+                [e.IsFrontPile1, e.IsFrontPile2, e.IsFrontPile3, e.IsFrontPile4]);
         }
@@ -2585,3 +2267,3 @@
         {
-            _undoManager.Undo();
+            _undoManager.UndoSnapshot();
             if (_undoManager.CurrentState is InputModel state)
@@ -2591,5 +2273,4 @@
 
-                // ・ｽﾏ更: 根入部情報s
-                UpdateWindowImmediate();
-                UpdateTreeView();
+                // 変更: 即時実行
+                NotifyUIChanged(immediate: true);
                 OnPropertyChanged(nameof(CurrentInputModel));
@@ -2601,3 +2282,3 @@
         {
-            _undoManager.Redo();
+            _undoManager.RedoSnapshot();
             if (_undoManager.CurrentState is InputModel state)
@@ -2607,5 +2288,4 @@
 
-                // ・ｽﾏ更: 根入部情報s
-                UpdateWindowImmediate();
-                UpdateTreeView();
+                // 変更: 即時実行
+                NotifyUIChanged(immediate: true);
                 OnPropertyChanged(nameof(CurrentInputModel));
@@ -2622,3 +2302,3 @@
 
-            // 変更: デバウンスで更新
+            // 変更: デバウンス付きで更新
             RequestUpdateWindow();
@@ -2642,3 +2322,3 @@
             CurrentInputModel.PileLayoutItems = uniquePileLayoutDataItems;
-            // ・ｽﾏ更: ・ｽ_・ｽC・ｽA情報O・ｽ・ｽﾍ托ｿｽ根入部・ｽs
+            // 変更: ダイアログ後は即時実行
             UpdateWindowImmediate();
@@ -2646,8 +2326,8 @@
 
-        public void DeleteDuplicatedElements()
+        public static void DeleteDuplicatedElements()
         {
-            // ・ｽd情報v・ｽf・ｽﾌ削除情報W・ｽb・ｽN
+            // 重複要素の削除ロジック
         }
 
-        // ・ｽE・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        // ウィンドウを開くメソッド
         private void OpenDialogWindow<TViewModel, TWindow>(MainWindowViewModel mainWindowViewModel)
@@ -2677,3 +2357,3 @@
 
-            // ・ｽﾏ更: ・ｽ_・ｽC・ｽA情報O・ｽ・ｽﾍ托ｿｽ根入部・ｽs
+            // 変更: ダイアログ後は即時実行
             UpdateWindowImmediate();
@@ -2682,3 +2362,3 @@
 
-        // ・ｽ・ｽ{・ｽﾝ抵ｿｽE・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        // 基本設定ウィンドウを開くメソッド
         [RelayCommand]
@@ -2686,9 +2366,6 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi読込・ｽO・ｽﾌ擾ｿｽﾔゑｿｽﾛ托ｿｽ・ｽj
-            _undoManager.SaveState(CurrentInputModel.DeepCopy());
-
-            OpenDialogWindow<FundamentalViewModel, FundamentalWindow>(this);
+            OpenDialogWindowWithUndo<FundamentalViewModel, FundamentalWindow>();
         }
 
-        // ・ｽﾗ重根入部・ｽE・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        // 荷重条件ウィンドウを開くメソッド
         [RelayCommand]
@@ -2696,11 +2373,10 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi読込・ｽO・ｽﾌ擾ｿｽﾔゑｿｽﾛ托ｿｽ・ｽj
-            _undoManager.SaveState(CurrentInputModel.DeepCopy());
-
-            OpenDialogWindow<LoadCaseViewModel, LoadCaseWindow>(this);
-            UpdateLoadCaseOption();
-            UpdateLoadCombinationOption();
+            OpenDialogWindowWithUndo<LoadCaseViewModel, LoadCaseWindow>(() =>
+            {
+                UpdateLoadCaseOption();
+                UpdateLoadCombinationOption();
+            });
         }
 
-        // 地盤ウ・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        // 地盤ウィンドウを開くメソッド
         [RelayCommand]
@@ -2708,9 +2384,6 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi読込・ｽO・ｽﾌ擾ｿｽﾔゑｿｽﾛ托ｿｽ・ｽj
-            _undoManager.SaveState(CurrentInputModel.DeepCopy());
-
-            OpenDialogWindow<GroundLayerViewModel, GroundWindow>(this);
+            OpenDialogWindowWithUndo<GroundLayerViewModel, GroundWindow>();
         }
 
-        // 杭体ウ・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        // 杭体ウィンドウを開くメソッド
         [RelayCommand]
@@ -2718,9 +2391,6 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi読込・ｽO・ｽﾌ擾ｿｽﾔゑｿｽﾛ托ｿｽ・ｽj
-            _undoManager.SaveState(CurrentInputModel.DeepCopy());
-
-            OpenDialogWindow<PileBodyViewModel, PileBodyWindow>(this);
+            OpenDialogWindowWithUndo<PileBodyViewModel, PileBodyWindow>();
         }
 
-        // 情報ﾍチ・ｽF・ｽb・ｽN
+        // 軸力チェック
         [RelayCommand]
@@ -2730,3 +2400,3 @@
             {
-                MessageBox.Show("杭配置根入部・ｽﾝゑｿｽ・ｽﾜゑｿｽ・ｽ・ｽB", "確認", MessageBoxButton.OK, MessageBoxImage.Information);
+                MessageBox.Show("杭配置が存在しません。", "確認", MessageBoxButton.OK, MessageBoxImage.Information);
                 return;
@@ -2735,3 +2405,3 @@
             bool hasWarning = false;
-            string warningMessage = "・ｽﾈ会ｿｽ・ｽﾌ搾ｿｽ・ｽﾚに厄ｿｽ閧ｪ根入部ﾜゑｿｽ:\n";
+            string warningMessage = "以下の項目に問題があります:\n";
 
@@ -2740,6 +2410,8 @@
                 var force = pileLayout.AxialForceVL;
+                var pileNo = pileLayout.PileNo;
 
-                for (int i = 0; i < CurrentInputModel.PileBodies[pileLayout.PileBodyNo - 1].PileBodySegments.Count; i++)
+                var pileBody = CurrentInputModel.PileBodies[pileLayout.PileBodyNo - 1];
+                for (int i = 0; i < pileBody.PileBodySegments.Count; i++)
                 {
-                    var pileSection = CurrentInputModel.PileBodies[pileLayout.PileBodyNo - 1].PileBodySegments[i].PileSection;
+                    var pileSection = pileBody.PileBodySegments[i].PileSection;
 
@@ -2748,3 +2420,3 @@
                         hasWarning = true;
-                        warningMessage += $"- 杭配置・ｽﾔ搾ｿｽ{i + 1} ・ｽﾗ重・ｽP・ｽ[・ｽX:VL:\n ・ｽg・ｽp情報E情報ﾍ適・ｽp・ｽﾍ茨ｿｽMax{pileSection.FactoredServiceNMax:N0}kN < {force:N0}kN\n";
+                        warningMessage += $"- 杭配置番号{pileNo} セグメント{i + 1} 荷重ケース:VL:\n 使用限界軸力適用範囲Max{pileSection.FactoredServiceNMax:N0}kN < {force:N0}kN\n";
                     }
@@ -2753,3 +2425,3 @@
                         hasWarning = true;
-                        warningMessage += $"- 杭配置・ｽﾔ搾ｿｽ{i + 1} ・ｽﾗ重・ｽP・ｽ[・ｽX:VL:\n {force:N0}kN < ・ｽg・ｽp情報E情報ﾍ適・ｽp・ｽﾍ茨ｿｽMin{pileSection.FactoredServiceNMin:N0}kN\n";
+                        warningMessage += $"- 杭配置番号{pileNo} セグメント{i + 1} 荷重ケース:VL:\n {force:N0}kN < 使用限界軸力適用範囲Min{pileSection.FactoredServiceNMin:N0}kN\n";
                     }
@@ -2759,8 +2431,8 @@
             if (hasWarning)
-                MessageBox.Show(warningMessage, "・ｽx・ｽ・ｽ", MessageBoxButton.OK, MessageBoxImage.Information);
+                MessageBox.Show(warningMessage, "警告", MessageBoxButton.OK, MessageBoxImage.Information);
             else
-                MessageBox.Show("・ｽe杭配置・ｽﾌ趣ｿｽ・ｽﾍは各・ｽf・ｽﾊの趣ｿｽ・ｽﾍ適・ｽp・ｽﾍ囲難ｿｽ・ｽﾅゑｿｽ・ｽB", "情報", MessageBoxButton.OK, MessageBoxImage.Information);
+                MessageBox.Show("各杭配置の軸力は各断面の軸力適用範囲内です。", "情報", MessageBoxButton.OK, MessageBoxImage.Information);
         }
 
-        // ・ｽv・ｽf根入部・ｽE・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        // 要素分割ウィンドウを開くメソッド
         [RelayCommand]
@@ -2770,10 +2442,32 @@
             {
-                // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi読込・ｽO・ｽﾌ擾ｿｽﾔゑｿｽﾛ托ｿｽ・ｽj
-                _undoManager.SaveState(CurrentInputModel.DeepCopy());
+                // 杭下端より下方に土層・土質点が存在するかチェック
+                var validationError = ValidatePileAndGroundDepth();
+                if (!string.IsNullOrEmpty(validationError))
+                {
+                    MessageBox.Show(validationError, "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                    return;
+                }
+
+                // 待機カーソルを表示（重い処理があるため）
+                var previousCursor = System.Windows.Input.Mouse.OverrideCursor;
+                System.Windows.Input.Mouse.OverrideCursor = System.Windows.Input.Cursors.Wait;
+
+                ElementDivisionWindow window = null;
+                try
+                {
+                    // Undoポイントを追加（読込前の状態を保存）
+                    _undoManager.SaveState(CurrentInputModel.DeepCopy());
+
+                    GenerateSoilPilesImmediate();  // 即時実行に変更
+                    CurrentInputModel.GenerateSoilEmbedment();
 
-                GenerateSoilPilesImmediate();  // 根入部情報s・ｽﾉ変更
-                CurrentInputModel.GenerateSoilEmbedment();
+                    window = new ElementDivisionWindow(this);
+                }
+                finally
+                {
+                    // カーソルを元に戻す
+                    System.Windows.Input.Mouse.OverrideCursor = previousCursor;
+                }
 
-                var window = new ElementDivisionWindow(this);
-                window.ShowDialog();
+                window?.ShowDialog();
 
@@ -2784,3 +2478,59 @@
 
-        // 根入部・ｽE・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        /// <summary>
+        /// 杭下端より下方に土層・土質点が存在するかを検証する
+        /// </summary>
+        /// <returns>エラーメッセージ（問題なければnull）</returns>
+        private string? ValidatePileAndGroundDepth()
+        {
+            var errors = new System.Text.StringBuilder();
+
+            foreach (var pileLayout in CurrentInputModel.PileLayoutItems)
+            {
+                int groundNo = pileLayout.GroundNo;
+                int pileBodyNo = pileLayout.PileBodyNo;
+
+                if (groundNo < 1 || groundNo > CurrentInputModel.GroundsInput.Count) continue;
+                if (pileBodyNo < 1 || pileBodyNo > CurrentInputModel.PileBodies.Count) continue;
+
+                var groundInput = CurrentInputModel.GroundsInput[groundNo - 1];
+                var pileBody = CurrentInputModel.PileBodies[pileBodyNo - 1];
+
+                // 杭下端標高を計算
+                double pileTopAltitude = pileLayout.Z;
+                double pileLength = pileBody.PileBodySegments.Sum(seg => seg.SegmentLength);
+                double pileBottomAltitude = pileTopAltitude - pileLength;
+
+                // 土層の最下層標高をチェック
+                if (groundInput.GroundLayers != null && groundInput.GroundLayers.Count > 0)
+                {
+                    double groundBottomAltitude = groundInput.GroundLayers.Min(layer => layer.BottomAltitude);
+                    if (pileBottomAltitude < groundBottomAltitude)
+                    {
+                        errors.AppendLine($"杭配置No.{pileLayout.PileNo}: 杭下端標高({pileBottomAltitude:F2}m)が土層の最下層標高({groundBottomAltitude:F2}m)より下にあります。");
+                    }
+                }
+                else
+                {
+                    errors.AppendLine($"杭配置No.{pileLayout.PileNo}: 地盤No.{groundNo}に土層データがありません。");
+                }
+
+                // 土質点の最深深度をチェック
+                if (groundInput.GroundMassesData != null && groundInput.GroundMassesData.Count > 0)
+                {
+                    double massBottomAltitude = groundInput.GroundMassesData.Min(mass => mass.AltitudeDepth);
+                    if (pileBottomAltitude < massBottomAltitude)
+                    {
+                        errors.AppendLine($"杭配置No.{pileLayout.PileNo}: 杭下端標高({pileBottomAltitude:F2}m)が土質点の最深標高({massBottomAltitude:F2}m)より下にあります。");
+                    }
+                }
+                else
+                {
+                    errors.AppendLine($"杭配置No.{pileLayout.PileNo}: 地盤No.{groundNo}に土質点データがありません。");
+                }
+            }
+
+            return errors.Length > 0 ? errors.ToString() : null;
+        }
+
+        // 沈下ウィンドウを開くメソッド
         [RelayCommand]
@@ -2792,3 +2542,3 @@
                 {
-                    MessageBox.Show("杭配置根入部・ｽﾝゑｿｽ・ｽﾜゑｿｽ・ｽ・ｽB");
+                    MessageBox.Show("杭配置が存在しません。");
                     return;
@@ -2798,3 +2548,3 @@
                     if (IsElementSplit == false)
-                        System.Windows.MessageBox.Show("・ｽv・ｽf根入部情報s情報ﾄゑｿｽ根入部情報B");
+                        System.Windows.MessageBox.Show("要素分割を行ってください。");
                     else
@@ -2805,5 +2555,5 @@
 
-        // 根入部・ｽﾗ重・ｽ・ｽﾍウ・ｽB情報h・ｽE情報J根入部・ｽ\・ｽb・ｽh
+        // 水平荷重解析ウィンドウを開くメソッド
         [RelayCommand]
-        public void OpenLateralLoadAnalysisWindow()
+        public async Task OpenLateralLoadAnalysisWindowAsync()
         {
@@ -2813,3 +2563,3 @@
                 {
-                    MessageBox.Show("杭配置根入部・ｽﾝゑｿｽ・ｽﾜゑｿｽ・ｽ・ｽB");
+                    MessageBox.Show("杭配置が存在しません。");
                     return;
@@ -2820,3 +2570,3 @@
                     {
-                        System.Windows.MessageBox.Show("・ｽv・ｽf根入部情報s情報ﾄゑｿｽ根入部情報B");
+                        System.Windows.MessageBox.Show("要素分割を行ってください。");
                     }
@@ -2824,13 +2574,6 @@
                     {
-                        // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ・ｽi読込・ｽO・ｽﾌ擾ｿｽﾔゑｿｽﾛ托ｿｽ・ｽj
-                        _undoManager.SaveState(CurrentInputModel.DeepCopy());
-
-                        var viewModel = new HorizontalCalculationViewModel(this);
-                        var window = new HorizontalCalculationWindow { DataContext = viewModel };
-
-                        if (viewModel is ICloseable closeableViewModel)
-                        {
-                            if (window.IsLoaded && window.IsVisible)
-                                window.Close();
-                        }
+                        // 砂時計カーソルを表示
+                        Mouse.OverrideCursor = Cursors.Wait;
+                        // UIを更新させるために短時間待機
+                        await Task.Delay(50);
 
@@ -2838,2 +2581,17 @@
                         {
+                            // Undoポイントを追加（読込前の状態を保存）
+                            _undoManager.SaveState(CurrentInputModel.DeepCopy());
+
+                            var viewModel = new HorizontalCalculationViewModel(this);
+                            var window = new HorizontalCalculationWindow { DataContext = viewModel };
+
+                            if (viewModel is ICloseable closeableViewModel)
+                            {
+                                if (window.IsLoaded && window.IsVisible)
+                                    window.Close();
+                            }
+
+                            // 砂時計を戻してからダイアログを表示
+                            Mouse.OverrideCursor = null;
+
                             window.ShowDialog();
@@ -2842,8 +2600,14 @@
                         {
-                            MessageBox.Show($"・ｽ_・ｽC・ｽA情報O・ｽﾌ表根入部・ｽﾉエ情報[根入部根入部・ｽﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                            MessageBox.Show($"ダイアログの表示中にエラーが発生しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                        }
+                        finally
+                        {
+                            // 念のため砂時計を戻す
+                            Mouse.OverrideCursor = null;
                         }
 
-                        // ・ｽﾏ更: 根入部情報s
+                        // 変更: 即時実行
                         UpdateWindowImmediate();
                         UpdateTreeView();
+
                     }
@@ -2853,3 +2617,3 @@
 
-        // ・ｽ・ｽﾍ擾ｿｽ根入部・ｽﾅゑｿｽ・ｽﾄゑｿｽ・ｽ驍ｩ・ｽ・ｽ確認情報驛・ｿｽ\・ｽb・ｽh
+        // 解析準備ができているかを確認するメソッド
         private bool IsPreparedForAnalysis()
@@ -2858,3 +2622,3 @@
             {
-                System.Windows.MessageBox.Show("杭配置根入部・ｽﾝゑｿｽ・ｽﾜゑｿｽ・ｽ・ｽB");
+                System.Windows.MessageBox.Show("杭配置が存在しません。");
                 return false;
@@ -2875,3 +2639,3 @@
             {
-                MessageBox.Show("地盤デ・ｽ[・ｽ^根入部・ｽﾝゑｿｽ・ｽﾜゑｿｽ・ｽ・ｽB");
+                MessageBox.Show("地盤データが存在しません。");
                 return;
@@ -2890,14 +2654,2 @@
                 {
-                    double poissonsRatio = 0.33;
-                    switch (layer.GranularityClass)
-                    {
-                        case "・ｽS情報y":
-                            poissonsRatio = 0.4;
-                            break;
-                        case "根入部・ｽy":
-                        case "・ｽI情報y":
-                            poissonsRatio = 0.3;
-                            break;
-                    }
-
                     CurrentInputModel.PileGroupSettlement.SettlementSoilLayers.Add(new SettlementSoilLayer
@@ -2906,3 +2658,3 @@
                         Ek = layer.Es,
-                        PoissonsRatio = poissonsRatio,
+                        PoissonsRatio = DeterminePoissonsRatio(layer.GranularityClass),
                         Thickness = 0
@@ -2912,14 +2664,7 @@
 
-            for (int i = 0; i < CurrentInputModel.PileGroupSettlement.SettlementSoilLayers.Count; i++)
-            {
-                if (i == 0)
-                    CurrentInputModel.PileGroupSettlement.SettlementSoilLayers[i].Thickness =
-                        loadingPlaneAltitude - CurrentInputModel.PileGroupSettlement.SettlementSoilLayers[i].BottomAltitude;
-                else
-                    CurrentInputModel.PileGroupSettlement.SettlementSoilLayers[i].Thickness =
-                        CurrentInputModel.PileGroupSettlement.SettlementSoilLayers[i - 1].BottomAltitude -
-                        CurrentInputModel.PileGroupSettlement.SettlementSoilLayers[i].BottomAltitude;
-            }
+            CalculateLayerThicknesses(
+                CurrentInputModel.PileGroupSettlement.SettlementSoilLayers,
+                loadingPlaneAltitude);
 
-            // ・ｽﾏ更: 根入部情報s
+            // 変更: 即時実行
             UpdateWindowImmediate();
@@ -2927,3 +2672,3 @@
 
-        // AutoOverturningMomentCommand - ・ｽ]・ｽ|情報[根入部・ｽg根入部・ｽv・ｽZ
+        // AutoOverturningMomentCommand - 転倒モーメント自動計算
         [RelayCommand]
@@ -2931,3 +2676,3 @@
         {
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ
+            // Undoポイントを追加
             _undoManager.SaveState(CurrentInputModel.DeepCopy());
@@ -2946,3 +2691,3 @@
             UpdateSumAndOTM();
-            // ・ｽﾏ更: 根入部情報s
+            // 変更: 即時実行
             UpdateWindowImmediate();
@@ -2951,3 +2696,3 @@
 
-        // AutoActionPointXYCommand - ・ｽ・ｽp・ｽ_XY根入部・ｽﾝ抵ｿｽ
+        // AutoActionPointXYCommand - 作用点XY自動設定
         [RelayCommand]
@@ -2955,3 +2700,3 @@
         {
-            // ・ｽ・ｽp・ｽ_・ｽ・ｽ杭配置・ｽﾌ重・ｽS・ｽﾉ移難ｿｽ
+            // 作用点を杭配置の重心に移動
             OnMoveForceActionPointToAverageCenter();
@@ -2960,3 +2705,3 @@
         /// <summary>
-        /// ・ｽI根入部・ｽ黷ｽ・ｽY情報尞懶ｿｽ情報R・ｽ}情報h
+        /// 選択された杭を削除するコマンド
         /// </summary>
@@ -2969,6 +2714,6 @@
 
-            // Undo・ｽ|・ｽC情報g・ｽ・ｽﾇ会ｿｽ
+            // Undoポイントを追加
             _undoManager.SaveState(CurrentInputModel.DeepCopy());
 
-            // Undo・ｽp・ｽﾉまとめゑｿｽ
+            // Undo用にまとめる
             var scope = new PileDesign.Common.Undo.CompositeUndoAction("Delete piles");
@@ -2985,3 +2730,3 @@
 
-            // 情報尞・
+            // 実削除
             foreach (var item in itemsToRemove)
@@ -2994,3 +2739,3 @@
         /// <summary>
-        /// 情報ﾗての杭・ｽﾌ選根入部根入部根入部R・ｽ}情報h
+        /// すべての杭の選択を解除するコマンド
         /// </summary>
@@ -3006,6 +2751,6 @@
         /// <summary>
-        /// Canvas3D ・ｽﾌ画像・ｽ保存根入部R・ｽ}情報h
+        /// Canvas3D の画像を保存するコマンド
         /// </summary>
         [RelayCommand]
-        private void ImageSave()
+        private void ImageSave(string scaleParam)
         {
@@ -3013,2 +2758,9 @@
 
+            // スケールファクターをパラメータから取得（デフォルト1.0）
+            double scale = 1.0;
+            if (!string.IsNullOrEmpty(scaleParam) && double.TryParse(scaleParam, out double parsedScale))
+            {
+                scale = parsedScale;
+            }
+
             var dialog = new Microsoft.Win32.SaveFileDialog
@@ -3024,10 +2776,13 @@
                 {
-                    // Canvas ・ｽ・ｽ RenderTargetBitmap ・ｽﾅキ情報v・ｽ`・ｽ・ｽ
-                    var bounds = VisualTreeHelper.GetDescendantBounds(Canvas3DLayout);
+                    int width = (int)(Canvas3DLayout.ActualWidth * scale);
+                    int height = (int)(Canvas3DLayout.ActualHeight * scale);
+
+                    // Canvas を RenderTargetBitmap でキャプチャ
                     var rtb = new RenderTargetBitmap(
-                        (int)Canvas3DLayout.ActualWidth,
-                        (int)Canvas3DLayout.ActualHeight,
-                        96, 96,
+                        width,
+                        height,
+                        96 * scale, 96 * scale,
                         PixelFormats.Pbgra32);
 
+                    // DrawingVisualを使用して背景とCanvasを合成
                     var dv = new DrawingVisual();
@@ -3035,4 +2790,10 @@
                     {
+                        // 背景を白で塗りつぶし（Canvasの背景がTransparentなため）
+                        dc.DrawRectangle(Brushes.White, null,
+                            new Rect(0, 0, Canvas3DLayout.ActualWidth, Canvas3DLayout.ActualHeight));
+
+                        // VisualBrushでCanvasを描画
                         var vb = new VisualBrush(Canvas3DLayout);
-                        dc.DrawRectangle(vb, null, new Rect(new Point(), new Size(Canvas3DLayout.ActualWidth, Canvas3DLayout.ActualHeight)));
+                        dc.DrawRectangle(vb, null,
+                            new Rect(0, 0, Canvas3DLayout.ActualWidth, Canvas3DLayout.ActualHeight));
                     }
@@ -3040,3 +2801,3 @@
 
-                    // ・ｽG情報R・ｽ[・ｽ_・ｽ[・ｽ・ｽI・ｽ・ｽ
+                    // エンコーダーを選択
                     BitmapEncoder encoder = System.IO.Path.GetExtension(dialog.FileName).ToLower() switch
@@ -3053,3 +2814,3 @@
 
-                    StatusMessage = $"・ｽ鞫懶ｿｽ保存情報ﾜゑｿｽ・ｽ・ｽ: {dialog.FileName}";
+                    StatusMessage = $"画像を保存しました ({width}x{height}): {dialog.FileName}";
                 }
@@ -3057,4 +2818,241 @@
                 {
-                    System.Windows.MessageBox.Show($"・ｽ鞫懶ｿｽﾌ保托ｿｽ・ｽﾉ趣ｿｽ・ｽs情報ﾜゑｿｽ・ｽ・ｽ: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                    System.Windows.MessageBox.Show($"画像の保存に失敗しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+                }
+            }
+        }
+
+        /// <summary>
+        /// Canvas3D の画像をクリップボードにコピーするコマンド
+        /// </summary>
+        [RelayCommand]
+        private void ImageCopy(string scaleParam)
+        {
+            if (Canvas3DLayout == null) return;
+
+            try
+            {
+                // スケールファクターをパラメータから取得（デフォルト1.0）
+                double scale = 1.0;
+                if (!string.IsNullOrEmpty(scaleParam) && double.TryParse(scaleParam, out double parsedScale))
+                {
+                    scale = parsedScale;
+                }
+                int width = (int)(Canvas3DLayout.ActualWidth * scale);
+                int height = (int)(Canvas3DLayout.ActualHeight * scale);
+
+                // Canvas を RenderTargetBitmap でキャプチャ
+                var rtb = new RenderTargetBitmap(
+                    width,
+                    height,
+                    96 * scale, 96 * scale,
+                    PixelFormats.Pbgra32);
+
+                // DrawingVisualを使用して背景とCanvasを合成
+                var dv = new DrawingVisual();
+                using (var dc = dv.RenderOpen())
+                {
+                    // 背景を白で塗りつぶし（Canvasの背景がTransparentなため）
+                    dc.DrawRectangle(Brushes.White, null,
+                        new Rect(0, 0, Canvas3DLayout.ActualWidth, Canvas3DLayout.ActualHeight));
+
+                    // VisualBrushでCanvasを描画
+                    var vb = new VisualBrush(Canvas3DLayout);
+                    dc.DrawRectangle(vb, null,
+                        new Rect(0, 0, Canvas3DLayout.ActualWidth, Canvas3DLayout.ActualHeight));
+                }
+                rtb.Render(dv);
+
+                // クリップボードにコピー
+                System.Windows.Clipboard.SetImage(rtb);
+
+                StatusMessage = $"画像をクリップボードにコピーしました ({width}x{height})";
+            }
+            catch (Exception ex)
+            {
+                System.Windows.MessageBox.Show($"画像のコピーに失敗しました: {ex.Message}", "エラー", MessageBoxButton.OK, MessageBoxImage.Error);
+            }
+        }
+
+        /// <summary>
+        /// アイソメトリック表示でモデル全体（杭先端含む）をキャプチャし、PNGバイト配列を返す。
+        /// Word出力用。キャプチャ後にカメラ状態は元に戻す。
+        /// </summary>
+        public byte[]? CaptureIsometricModelImageBytes()
+        {
+            if (Canvas3DLayout == null || CurrentInputModel == null || CurrentInputModel.PileLayoutItems.Count == 0)
+                return null;
+
+            // --- 1. 現在の状態を保存 ---
+            var savedTht = CanvasThreeDView.Tht;
+            var savedPhi = CanvasThreeDView.Phi;
+            var savedScale = CanvasThreeDView.Scale;
+            var savedViewTransition = CanvasThreeDView.ViewTransition;
+            var savedCt = CanvasThreeDView.Ct;
+            var savedDv0 = CanvasThreeDView.Dv0;
+            var savedTickMark = IsTickMarkVisible;
+            var savedAxes = IsXYZAxesVisible;
+
+            try
+            {
+                // SetCt自動上書きをスキップするフラグをON
+                IsCapturingForExport = true;
+
+                // --- 2. 杭頭＋杭先端＋地盤範囲を含む全3D点を収集 ---
+                var allPoints = new System.Collections.ObjectModel.ObservableCollection<Point3D>();
+                foreach (var pile in CurrentInputModel.PileLayoutItems)
+                {
+                    allPoints.Add(pile.Point3D); // 杭頭
+
+                    int idx = pile.PileBodyNo - 1;
+                    if (idx >= 0 && CurrentInputModel.PileBodies != null && idx < CurrentInputModel.PileBodies.Count)
+                    {
+                        var pileBody = CurrentInputModel.PileBodies[idx];
+                        if (pileBody.PileBodySegments != null && pileBody.PileBodySegments.Count > 0)
+                        {
+                            double totalLen = pileBody.PileBodySegments.Sum(s => s.SegmentLength);
+                            allPoints.Add(new Point3D(pile.Point3D.X, pile.Point3D.Y, pile.Point3D.Z - totalLen));
+                        }
+                    }
+
+                    int gIdx = pile.GroundNo - 1;
+                    if (gIdx >= 0 && CurrentInputModel.GroundsInput != null && gIdx < CurrentInputModel.GroundsInput.Count)
+                    {
+                        var ground = CurrentInputModel.GroundsInput[gIdx];
+                        allPoints.Add(new Point3D(pile.Point3D.X, pile.Point3D.Y, ground.GroundTopAltitude));
+                        if (ground.GroundLayers != null && ground.GroundLayers.Count > 0)
+                        {
+                            double btmAlt = ground.GroundLayers[^1].BottomAltitude;
+                            allPoints.Add(new Point3D(pile.Point3D.X, pile.Point3D.Y, btmAlt));
+                        }
+                    }
                 }
+
+                // --- 3. 装飾要素を設定（通り芯は残す） ---
+                IsTickMarkVisible = false;
+                IsXYZAxesVisible = false;
+
+                // --- 4. 全点を中心にカメラ設定 ---
+                CanvasThreeDView.SetCt(allPoints);
+                CanvasThreeDView.ViewTransition = new Point(0, 0);
+
+                // --- 5. アイソメ視点に設定（SetCtはスキップされる） ---
+                CanvasThreeDView.Tht = -45;
+                CanvasThreeDView.Phi = 45;
+                Canvas3DLayout.UpdateLayout();
+                Canvas3DLayout.Dispatcher.Invoke(
+                    System.Windows.Threading.DispatcherPriority.Render, new Action(() => { }));
+
+                // --- 6. 実際のCanvasサイズを取得 ---
+                double canvasW = Canvas3DLayout.ActualWidth;
+                double canvasH = Canvas3DLayout.ActualHeight;
+                if (canvasW <= 0 || canvasH <= 0) return null;
+
+                // --- 7. 全点の2Dバウンディングボックスを計算 ---
+                double xMax = double.MinValue, yMax = double.MinValue;
+                double xMin = double.MaxValue, yMin = double.MaxValue;
+                foreach (var pt3d in allPoints)
+                {
+                    Point pt2d = CanvasThreeDView.Transformation(pt3d);
+                    if (pt2d.X > xMax) xMax = pt2d.X;
+                    if (pt2d.Y > yMax) yMax = pt2d.Y;
+                    if (pt2d.X < xMin) xMin = pt2d.X;
+                    if (pt2d.Y < yMin) yMin = pt2d.Y;
+                }
+                double bbW = xMax - xMin;
+                double bbH = yMax - yMin;
+                if (bbW <= 0 || bbH <= 0) return null;
+
+                // --- 8. スケールをフィットさせ、中央に配置 ---
+                double gridMargin = GridSymbolZoneWidth * 2; // 通り芯符号用マージン
+                double availW = canvasW - gridMargin * 2;
+                double availH = canvasH - gridMargin * 2;
+                double fitRatio = Math.Min(availW / bbW, availH / bbH);
+
+                // 中央補正: スケール変更後のBB中心がCanvas中心に来るようVTを設定
+                double bbCenterX = (xMin + xMax) / 2;
+                double bbCenterY = (yMin + yMax) / 2;
+                double orgX = canvasW / 2;
+                double orgY = canvasH / 2;
+                CanvasThreeDView.ViewTransition = new Point(
+                    (orgX - bbCenterX) * fitRatio,
+                    (orgY - bbCenterY) * fitRatio);
+                CanvasThreeDView.Scale *= fitRatio; // re-render triggered
+
+                Canvas3DLayout.UpdateLayout();
+                Canvas3DLayout.Dispatcher.Invoke(
+                    System.Windows.Threading.DispatcherPriority.Render, new Action(() => { }));
+
+                // --- 9. コンテンツ領域をVisualBrushで切り出してキャプチャ ---
+                // 最終的な2D BBを再計算（スケール・VT適用後）
+                xMax = double.MinValue; yMax = double.MinValue;
+                xMin = double.MaxValue; yMin = double.MaxValue;
+                foreach (var pt3d in allPoints)
+                {
+                    Point pt2d = CanvasThreeDView.Transformation(pt3d);
+                    if (pt2d.X > xMax) xMax = pt2d.X;
+                    if (pt2d.Y > yMax) yMax = pt2d.Y;
+                    if (pt2d.X < xMin) xMin = pt2d.X;
+                    if (pt2d.Y < yMin) yMin = pt2d.Y;
+                }
+
+                // 通り芯符号用のマージンを追加
+                double captureMargin = GridSymbolZoneWidth * 1.5;
+                double cropX = Math.Max(0, xMin - captureMargin);
+                double cropY = Math.Max(0, yMin - captureMargin);
+                double cropR = Math.Min(canvasW, xMax + captureMargin);
+                double cropB = Math.Min(canvasH, yMax + captureMargin);
+                double cropW = cropR - cropX;
+                double cropH = cropB - cropY;
+                if (cropW <= 0 || cropH <= 0) return null;
+
+                double capScale = 2.0;
+                int outW = (int)(cropW * capScale);
+                int outH = (int)(cropH * capScale);
+                var rtb = new RenderTargetBitmap(outW, outH, 96 * capScale, 96 * capScale, PixelFormats.Pbgra32);
+
+                // 白背景
+                var bgVisual = new DrawingVisual();
+                using (var dc = bgVisual.RenderOpen())
+                {
+                    dc.DrawRectangle(Brushes.White, null, new Rect(0, 0, cropW, cropH));
+                }
+                rtb.Render(bgVisual);
+
+                // VisualBrushでコンテンツ領域を切り出し
+                var contentVisual = new DrawingVisual();
+                using (var dc = contentVisual.RenderOpen())
+                {
+                    var vb = new VisualBrush(Canvas3DLayout)
+                    {
+                        Viewbox = new Rect(cropX, cropY, cropW, cropH),
+                        ViewboxUnits = BrushMappingMode.Absolute,
+                        Stretch = Stretch.Uniform
+                    };
+                    dc.DrawRectangle(vb, null, new Rect(0, 0, cropW, cropH));
+                }
+                rtb.Render(contentVisual);
+
+                var encoder = new PngBitmapEncoder();
+                encoder.Frames.Add(BitmapFrame.Create(rtb));
+                using var ms = new System.IO.MemoryStream();
+                encoder.Save(ms);
+                return ms.ToArray();
+            }
+            finally
+            {
+                // --- フラグをOFF ---
+                IsCapturingForExport = false;
+
+                // --- 装飾復元 ---
+                IsTickMarkVisible = savedTickMark;
+                IsXYZAxesVisible = savedAxes;
+
+                // --- カメラ復元 ---
+                CanvasThreeDView.Dv0 = savedDv0;
+                CanvasThreeDView.Ct = savedCt;
+                CanvasThreeDView.ViewTransition = savedViewTransition;
+                CanvasThreeDView.Scale = savedScale;
+                CanvasThreeDView.Tht = savedTht;
+                CanvasThreeDView.Phi = savedPhi;
+                UpdateCanvas3DAction?.Invoke();
             }
