<Window x:Class="PileDesign.Views.HelpChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:common="clr-namespace:PileDesign.Common"
        mc:Ignorable="d"
        Title="ヘルプ チャット" Height="600" Width="560"
        common:EscapeKeyBehavior.Enable="True">
    <DockPanel LastChildFill="True" Background="#F7F7F9">

        <!-- 上部: 説明 -->
        <Border DockPanel.Dock="Top" Background="{StaticResource PrimaryAccentBrush}" Padding="10,6">
            <TextBlock Text="知りたい機能や用語を入力してください (例: M-θ 関係, 任意地盤変位, 杭頭結合)"
                       Foreground="White" TextWrapping="Wrap"/>
        </Border>

        <!-- 下部: 入力欄 -->
        <Border DockPanel.Dock="Bottom" Background="White" BorderBrush="#DDD" BorderThickness="0,1,0,0" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="InputTextBox"
                         Grid.Column="0"
                         FontSize="14"
                         Padding="6,4"
                         VerticalContentAlignment="Center"
                         AcceptsReturn="False"
                         KeyDown="InputTextBox_KeyDown"/>
                <Button x:Name="SendButton"
                        Grid.Column="1"
                        Content="送信"
                        MinWidth="64"
                        Margin="6,0,0,0"
                        Padding="6,2"
                        Click="SendButton_Click"/>
            </Grid>
        </Border>

        <!-- 中央: チャット履歴 -->
        <ScrollViewer x:Name="ChatScroll" VerticalScrollBarVisibility="Auto" Padding="10">
            <StackPanel x:Name="ChatPanel"/>
        </ScrollViewer>
    </DockPanel>
</Window>
