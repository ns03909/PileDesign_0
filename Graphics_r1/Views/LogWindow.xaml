<Window x:Class="PileDesign.Views.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:common="clr-namespace:PileDesign.Common"
        mc:Ignorable="d"
        Title="解析ログ" Height="600" Width="900"
        MaxWidth="{x:Static SystemParameters.MaximizedPrimaryScreenWidth}"
        MaxHeight="{x:Static SystemParameters.MaximizedPrimaryScreenHeight}"
        WindowStartupLocation="CenterOwner"
        DataContextChanged="OnDataContextChanged"
        common:EscapeKeyBehavior.Enable="True">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Window.InputBindings>
        <!-- Ctrl+C: 選択行があればそれを、なければ全件をコピー -->
        <KeyBinding Modifiers="Control" Key="C"
                    Command="{Binding CopyLogCommand}"
                    CommandParameter="{Binding SelectedItems, ElementName=LogListBox}"/>
        <!-- Ctrl+Shift+C: 全件を高速コピー (選択不要、Ctrl+A→Ctrl+C より速い) -->
        <KeyBinding Modifiers="Control+Shift" Key="C"
                    Command="{Binding CopyAllLogCommand}"/>
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Padding="5">
            <Button Content="コピー (Ctrl+C)"
                    Command="{Binding CopyLogCommand}"
                    CommandParameter="{Binding SelectedItems, ElementName=LogListBox}"
                    Padding="10,2"/>
            <Button Content="全件コピー (Ctrl+Shift+C)"
                    Command="{Binding CopyAllLogCommand}"
                    Padding="10,2"
                    ToolTip="選択不要で全行をコピー (Ctrl+A→Ctrl+C より高速)"/>
            <Separator/>
            <Button Content="ファイル出力" Command="{Binding ExportLogCommand}" Padding="10,2"/>
            <Separator/>
            <Button Content="クリア" Command="{Binding ClearLogCommand}" Padding="10,2"/>
            <Separator/>
            <CheckBox Content="自動スクロール" IsChecked="{Binding AutoScroll}" VerticalAlignment="Center" Margin="5,0"/>
            <Separator Visibility="{Binding IsCategorySelectorVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <ComboBox ItemsSource="{Binding LogCategories}"
                      SelectedItem="{Binding SelectedLogCategory}"
                      Visibility="{Binding IsCategorySelectorVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                      MinWidth="150" VerticalAlignment="Center"/>
        </ToolBar>

        <!-- Log Content (ListBox with virtualization for large log sets)
             SelectionMode=Extended で Ctrl+A / Shift+Click / Ctrl+Click がネイティブに効く -->
        <ListBox Grid.Row="1" x:Name="LogListBox"
                 ItemsSource="{Binding LogLines}"
                 SelectionMode="Extended"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="#F5F5F5"
                 Foreground="#333333"
                 BorderThickness="0"
                 HorizontalContentAlignment="Stretch"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                 ScrollViewer.CanContentScroll="True"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="4,0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="NoWrap"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
